# Deepcast — Project Brief

*Written 24 July 2026, the day the watch page redesign shipped; last reconciled 6 August 2026 — the day the data confirmed the network's first organic onward share. For strategic advisors: decisions and reasoning, not implementation. Supersedes the 22 July brief.*

## What Deepcast is

Deepcast optimizes for depth over reach. It is an invite-only network where substantive films spread through trusted relationships — no feed, no algorithm, no public link. A film travels only because one person chose one other person and handed them a ticket; trust built person to person carries it through trusted networks toward deep impact and resonance. Deepcasting, not broadcasting. Depth is the heart of the platform — the tickets, the constellation, and invite-only itself all exist to serve it. Founded and built by Ien Chi, a filmmaker who helped grow a YouTube channel to five million subscribers and a billion views, and watched platform incentives bend everything toward clickbait; Deepcast is the counter-bet.

## State of the network

**THE FIRST REAL TRANSMISSION (31 July 2026, evening):** the founder minted Faith Circle's ticket №2 and personally sent it to its first genuine recipient, Oliver. One human chose one other human and handed him a ticket — the act the entire machine was built for. As of this reconciliation the ticket is in flight (sent, not yet claimed; database-verified). The same day, the founder's mother, Young Chi, claimed a real ticket on The New Narrative. **The platform has real users again, and the protection lists in code carry them again** — Young Chi tonight, Oliver the moment he claims. New standing law: Faith Circle is LIVE — no test data on it ever again, no deletions touching its real users, no change to an in-flight recipient's experience without the founder's approval. *(What followed: Oliver claimed on 3 August, watched, and passed the film onward — see the 6 August section below. The "Oliver at first claim" list-step, honestly recorded there, never executed; film-level protection replaced it.)*

## State of the network — 6 August reconciliation

**THE MECHANIC WORKED. THE DATA PROVES IT.** Oliver claimed №2 on 3 August and watched the film. Then — with zero prompting from the founder, who has only ever shared this film with Oliver — **he passed it onward three times the same night**: №3 to Steve (in flight), №4 to Brian (who claimed and watched within hours), №5 to Katie (in flight). Every link is verified in the database, not narrated: all three tickets carry Oliver's account as sender and his ticket №2 as parent. **This is the platform's first organic onward share — depth over reach working exactly as designed: watch, feel it, choose the few people who need it next.** The film's verified ledger as of this reconciliation: №1 the filmmaker · №2 Oliver (claimed, watched) · №3 Steve (in flight) · №4 Brian (claimed, watched) · №5 Katie (in flight); ticket counter at 5.

**The film is now "Circles"** (renamed 6 August; formerly "Faith Circle," before that "The Faith Dialogues") and plays the founder's new cut — 29 minutes, swapped 6 August with the poster deliberately pinned to the original video's frame (that original Mux asset must never be deleted). The claim page also simplified the same day: it asks for the invitee's email alone — the full-name field, added at launch, came off by founder decision.

**New standing law — FILM-LEVEL PROTECTION (6 August ruling):** Circles is live and public; EVERY person associated with it — claimed accounts and unclaimed in-flight tickets alike, present and future — is a real, protected user, and all test activity of any kind belongs to The New Narrative only. The protection is now built into the code at the film level: the admin removal flow refuses to touch anyone on a protected film, refuses to delete its in-flight ticket links, and refuses to delete the account of anyone associated with it even during cleanup on another film; the test-data reset aborts outright if its scope ever touches one. **Recorded honestly: the older per-email mechanism ("add Oliver to the lists the moment he claims") was never executed — Oliver and Brian both claimed while unprotected. That gap, discovered in this reconciliation, is exactly why the protection is now film-level and automatic rather than a hand-maintained list.** Adding a film to the protected set is now a launch-day step for every future film going live.

**The 24 July "everything is a test" ruling, now scoped:** every account from the test era — the previously protected named users (Jon, Trace, Tina, Austin, Georgie) included — remains deletable test data, and the founder-run cleanup of The New Narrative's test pile remains queued (now explicitly excluding Young Chi). The ruling does NOT extend to the launch-era real users. The old "remove the stale protection lists" task is subsumed: the lists are live infrastructure again — updated, never blanket-deleted — and the agent never executes production deletions, per standing law. Three films exist — The New Narrative, A Sacred Pause, and **Faith Circle** (renamed from "The Faith Dialogues" on 24 July). **As of 31 July, Faith Circle is the launch film:** everything on it is real — portrait, director's statement, location, transmission hook — its seeded ghosts are off viewer surfaces, and its ticket ledger has been wiped of tests and reset to open at №1 (see below). The New Narrative remains the designated film for minting test claims, so permanent ticket numbers don't scatter across the catalog. The machine is built, hardened, and polished end to end — and the milestone the whole brief pointed at is no longer ahead: **the first genuine share went out the evening of 31 July. Faith Circle's №2 belongs to Oliver.** The next milestone is his claim — and the first reason a stranger gives for passing the film onward.

**Faith Circle's ledger — RESET, OPENED, AND NOW CARRYING ITS FIRST REAL TICKET (database-verified 31 July evening):** №1 the filmmaker, **№2 Oliver — minted at the reset counter's first click, in flight.** The reset that made this clean opening possible, recorded fully: on launch day the founder removed every remaining test ticket and test account on the film himself via admin Remove — zero tickets and zero test wallets now exist — and then **reset the ticket counter to 1 by explicit ruling.** The reasoning, recorded so it is never misread: the immutability law protects *existing* tickets; with none existing, a reset renumbers nothing and breaks nothing — it simply opens the film's real ledger. The filmmaker holds №1; **the first genuine share will mint №2.** The seeded ghosts were also switched off viewer surfaces the same day (the deliberately-sequenced-last flip), so no real recipient will ever see a ghost. On any film with existing tickets, a counter reset remains forbidden — the old gap-riddled demo ledgers were the law working, and The New Narrative still carries its test pile (№6–31 plus legacy test accounts) awaiting the founder's own cleanup.

## What shipped 23–24 July: the watch page redesign

The full post-claim experience, rebuilt through ~30 design rounds and implemented in one disciplined branch:

- **The page:** masthead, poster-framed player (posters now mandatory product policy — Mux auto-thumbnail with a per-film override), and a right rail carrying the film's public record: the honest viewer count, a gold progress bar toward a fixed tier ladder (100 / 250 / 500 / 1,000 …), milestones that appear only once genuinely crossed (absent, not empty, before then), the solid-gold "Pass it on" (the page's only solid-filled object), and the rule line: *"This film passed through {N} pairs of hands to reach you. You are its newest link — or its last."*
- **Pass-it-on is now a ritual in a modal**, replacing the always-docked panel (founder override of the original constraint). Inside: a fixed-geometry lineage constellation of the viewer's real chain — real first names, YOU with a halo, a dashed line to a hollow "?" that becomes the recipient's name when a ticket is minted (hollow until claimed; solid = arrived is sacred grammar) — five ticket stubs dimming newest-first, the charge (*"Who needs to see this? Not anyone — the one it will matter to."*), and the replacement-model reveal: the bare link renders exactly where the form was, zero scrolling. Constellation forks light only when data confirms that hand truly shared onward — fabricated humans are a brand violation.
- **The creed band:** three gold marks over three founder statements — private-invite-and-real-humans-only / won't-reach-anyone-unless-you-pass-it-on / each-ticket-admits-one-person-once. **The story section:** "Filmmaker · {name} · {location}" (header restructured 25 July — the name moved into the header and the sign-off was cut), epigraph, note — real for Faith Circle since 27 July; still placeholder text with real names on the other two films (see TEMP ledger).
- **Design laws now binding** (docs/watch-page-spec.md is the tracked constitution): gold only as act or mark plus exactly three approved copy-emphasis words; Phoenix Light only for body text at reading sizes; one shared hairline value; numerals always, percentages never; no engagement mechanics of any kind — no pulsing, badges, celebrations, urgency. Honest numbers always: no padding, seeding, or clamping — a film with 19 viewers says 19.

**Also now live:** the dashboard's phantom-scrollbar fix. One canonical tickets line on both share surfaces: "…Who else needs it?" And the Render migration Oregon → Virginia is complete — the cross-country database round trip, the largest known speed lever, is resolved.

## What shipped the evening of 24 July

- **Faith Circle is born.** The third film was renamed from "The Faith Dialogues" and received its real video (34 minutes) in an owner-approved production update. Two deliberate protections rode along: the runtime line stays honest because it is computed from the stored duration, never typed; and the watch-page poster is **pinned to the previous video's frame**, so swapping the film could not silently change the face of the page. That pin creates a standing dependency: **the old video's Mux asset must never be deleted from the Mux account**, or the poster breaks.
- **The filmmaker story is real for the first time.** Faith Circle's story section carries Ien's actual portrait (the cropped file the founder supplied) at a properly weighted size — larger circle, header seated on its bottom edge per the founder's eye, face gently zoomed by style rather than by editing the image. The words remained placeholder by design; the location remains temporary. Only the founder writes the real note. *(Superseded 27 July — the real note shipped; see below.)*
- **The admin popovers now behave like instruments.** Three defects diagnosed and fixed at the root: the delete-confirmation email is permanent visible text (it used to vanish at the first keystroke); a person claimed on two films opens exactly one popover instead of stacked twins that dismissed each other; and popovers close only on the *user's* scrolling — the page's own layout shifts can never dismiss them mid-action.
- **The prologue speaks its final words.** The three-line interstitial now carries the founder-approved 24 July wording, verbatim in the copy ledger.
- The day's nine legacy-flow test failures later passed unchanged — reclassified as environment flake; the investigation stays queued rather than being declared resolved by a green run.

## What shipped 27 July

- **Faith Circle's story section now speaks in the founder's own words.** The placeholder note was replaced with Ien's real director's statement — an epigraph and four paragraphs, verbatim — making Faith Circle the first film whose story section is fully real: real portrait, real name, real words. Still temporary: the location line *(confirmed real 31 July — nothing temporary remains on this film)*. The other two films keep their placeholders until their filmmakers' real notes arrive.

## What shipped 31 July — launch day

Two branches, twelve commits, all live. The theme: everything a first real viewer might touch got hardened, and the film they'll receive became fully real.

- **The blank screen can't happen again.** The 28 July incident — the founder claiming a ticket on his phone and getting a permanently dark screen — was root-caused: two deploys landed mid-visit, renaming the app's code files; the page's next file fetch failed, and with no safety net React removed everything. Now one boundary wraps the app: a failed fetch triggers a single self-healing reload (which always picks up the fresh deploy), guarded against loops even in private-mode browsers; if it still fails, a quiet on-brand error screen with a "Reload the page" button appears — never a dark void. Proven by tests that simulate the exact incident.
- **Claiming asks who you are.** The claim form now leads with "Your full name" (the button reads "Claim your ticket"; the email box, "Your email"). The first word becomes the person's first name everywhere — their own name, not the placeholder their sharer typed — and the rest is stored quietly as data; every screen stays first-name-only. The claim also quietly records timezone, browser language, and device class (phone/tablet/desktop) — owner-approved columns, capture that can never break a claim. The invisible sign-in that rides the claim now has an 8-second deadline, so a hung network call can no longer strand anyone.
- **Name boxes got mechanically strict.** One shared rule on every name surface now refuses emails, digits, links (including shortener shapes like bit.ly — a dot followed by two-plus letters reads as a domain; "J.R." still passes because initials don't), and over-long input. No cleverness about what "looks like" a name — a required-to-pass list of real names (accents, hyphens, apostrophes, non-Latin) guards that promise in the tests.
- **Small things a first viewer would hit:** the gold button's ring now actually reads on phones (founder verified on device); the watch page's "Your dashboard →" link shows at every screen size (phones previously had to scroll to the footer); and the constellation follows a new founder principle — **the names are the product** — every name that fits on screen renders, at every size; a name hides only when it would physically collide with another, and zooming in makes room for more. The blanket phone-hiding rule is gone.
- **Faith Circle became the launch film.** Real portrait, real statement, real location, founder-final hook, ghosts off viewer surfaces, and a clean ledger opened at №1 (see the ledger section above).

## What shipped 31 July, evening — after the launch

- **The reduced-motion prologue waits.** Field-tested on a phone with animations off: the three-line prologue auto-advanced in four seconds — too fast to read. It now holds until the viewer taps. Patient stillness over a timer.
- **Name editing matches the claim.** The dashboard's editor became "Edit your full name" — one shared split rule with the claim form (first word displays everywhere; the rest is quiet data) — with the founder-stamped helper "Only your first name appears on the network."
- **The protection lists are live infrastructure again** (see the top of this brief): Young Chi in code, Oliver at first claim.
- Newly queued: **graceful dead-session handling** — someone whose account was deleted but whose browser still holds a session should simply land on the sign-in page, never a raw "Invalid session" error. Surfaced by the launch-day cleanups.

## What shipped 6 August

- **The recut.** Circles' new 29-minute cut went live in an owner-approved production update — duration read straight from Mux so the runtime line stays honest, story and poster entries re-keyed with their content untouched, and the poster pin holding to the original video's frame through its second swap.
- **The rename.** "Faith Circle" became "Circles." The title lives in one database field and every page reads it from data, so the rename was one approved write plus a handful of comments and test names; links, slugs, tickets, and all founder copy untouched. Historical records keep the old names as written.
- **The claim form went email-only.** The launch-day full-name field came off the landing page by founder decision — the letter now asks one thing: your email. Account naming falls back to the name the sharer typed, then the email; the dashboard's "Edit your full name" editor is unchanged.
- **Film-level protection** (see the standing law in the 6 August state section): built, unit-tested on every refusal path, and proven not to block the founder's queued New Narrative cleanup — a person holding both a Circles claim and New Narrative test data can still be cleaned up on the test film, with their account and every Circles row structurally untouchable.
- **The ghost seeder's identity guard** was re-pointed at the recut's video id, so it refuses to run for the right reason again.

## The false-green incident (31 July) — recorded honestly

Launch morning's deploy failed on Vercel: a unit test written five days earlier had *never passed locally*, because the agent's test gates piped their output through a display filter that both hid the failure lines and replaced the failing exit code with success — so "unit green, build green" was reported, twice, without being true. Vercel's own gate — which runs the same tests unfiltered — caught it, and the fix (plus the honest account) shipped within the hour. Two things came out of it: a **new standing doctrine** — every test/build gate runs unmasked, exit codes checked, reports quote the actual summary lines rather than paraphrasing them — and a reaffirmation of an old truth of this project: independent gates (Vercel's build, the founder's push control, the founder's own eyes) exist precisely because narration, including the agent's, is not proof.

## Standing product laws (unchanged, do not re-derive)

- **Shares are custom unique links, not emails.** The sender types only a first name and gets a bare link to deliver personally — the delivery is the human moment; no pre-written message, ever. The old email-invite system is retired (links already sent keep working forever).
- **Every ticket has a permanent number.** Per film, issued in link-creation order, immutable: a deleted ticket leaves a gap forever, and nothing ever renumbers. The filmmaker always holds №1 on their own film; invitees start at №2.
- **Claiming signs you in.** A new claimant becomes a full signed-in user instantly. Security boundary held deliberately: instant sign-in happens only when the claim just created the account — typing an email can never open an account that already exists.
- **One claim per person per film.** A duplicate claim voids the link, returns the sender's ticket, and says, verbatim: *"You already hold this film."* The sender's list keeps the row as history: *"Already held this film — ticket returned."* People are people, not rows.
- **One definition of "who exists."** Every surface reads a single shared rule for which tickets are real. Voided tickets count nowhere; seeded demo ghosts appear only per each film's show_ghosts rule and are never numbered. New surfaces must read the shared rule, never invent their own.
- **The journey sentence counts your whole downstream.** *"This film has reached [X] people. [Y] of them through you."* — Y includes shares to any depth. Numerals always.
- **Copy is founder-approved verbatim or it doesn't ship.** First names only on every display, everywhere; an email address is never displayed as a person's name. Name boxes refuse emails, digits, and links mechanically (31 July hardening) — the sharer's box with *"Just their first name — no email needed."* *(The claim form's "Your full name" field carried the same rule from 31 July until 6 August, when the form went email-only; the rule lives on in the dashboard's name editor.)* A person's last name is data — never display.
- **Protected films (6 August).** A live film's people — every claimant and every in-flight ticket, present and future — are protected in code at the film level, automatically. Adding a film to the protected set is a launch-day step. Named-email protection stays as the second line, never the first.

## Decisions and reasoning worth keeping from this week

- **Sparse-and-honest won.** Early films show single-digit counts against a 100 goal with no milestones row. Accepted deliberately: absence reads as nothing, and the row's first appearance at 100 is itself a quiet earned event.
- **The emblem's sparse state: designed, chosen, blessed — in one day.** Real chains of 1–2 read empty against a mock composed for depth 47. On 25 July the founder iterated the replacement through eight rendered rounds (including one full rejection and revert) and blessed the final form; the interim centering still ships until the chosen composition is built into the live modal.
- **Process held under pressure.** The agent once committed a fix directly to main — caught by the push gate, reprimanded, redone on a branch. A suspected regression (unpinned dashboard sidebar) was proven by rendering both branches to have never existed as a feature at all — now a properly scoped parked task with a pre-written test defining done. "Never touch main," "diagnose before fix," and "narration is not proof" all earned their keep.
- **The rail counts generosity, not consumption (25 July).** The watch page's big number switched from viewers-who-claimed to tickets shared — the act the page exists to inspire is the act it now measures. The milestone ladder didn't move; 100 stays the first rung. Same day, the founder ratified the standing vocabulary: "ticket" is the noun, "shared" is the verb, "Pass it on" belongs to the button alone — every viewer surface now says "shared," never "given." The invitation letter was also revised: the private-invitation stamp now opens it, the headline reads "shared a ticket with you," and the single-use line came off the page entirely.

## Parked and queued (approved, waiting — do not re-debate)

- ~~**V5 dashboard internally-scrolling columns**~~ — SHIPPED 25 July: desktop now uses the creator-dashboard pattern (viewport-height grid, main column scrolls internally, sidebar genuinely still); the app-wide overflow guards were not touched; the parked done-definition test moved into the live suite and passes on all three engines. Mobile behavior unchanged.
- **Sparse lineage emblem composition — CHOSEN AND FOUNDER-BLESSED (25 July, harness v8.1); building it into the live pass-it-on window is now a QUEUED task.** The blessed form, from the local-only page at /dev/emblem: a branching tree growing left to right in the founder's grammar — gold solid for the viewer's realized path, gold dotted to the unminted next ticket, gray dotted unlabeled branches for everyone else — showing at most three named stops (the filmmaker, your direct sharer, you); everyone between them condenses into one small gold node named for the hand nearest your sharer ("Priya", or "Priya + 4"), and the depth-3 drawing serves every deeper chain with only that label changing. Riders on the implementation task: the dashboard constellation's filmmaker label still lacks its approved parentheses; the link data already carries every hand's name (verified), to be confirmed again at build time; and the name-validation hardening once attached as a ride-along candidate has since shipped on its own (31 July).
- **Ticket expiry refund** — approved and specced (docs/ticket-expiry-refund-spec.md), not built: after 14 days unclaimed, the sharer's ticket refunds automatically; the link itself never dies, and a late claim still admits the person fully, viewing and sharing alike. No expiry copy appears anywhere in the product until the mechanism exists, because it would be false.
- **Session-based duplicate detection** — approved direction: a signed-in user merely opening a claim link for a film they already hold is treated as a duplicate immediately (recognition message, void, refund), extending the existing duplicate machinery. Detecting duplicates by IP or by name was considered and explicitly rejected: false positives, and a membership privacy leak. No technique built yet.
- ~~**Landing-page reorder**~~ — SHIPPED 25 July, amended in the same session: the invitation stamp — "By private invitation only · Ticket No. {N}" — now opens the letter, and "This invitation admits one person, once." was CUT from the page entirely (superseding the 24 July plan to move it above the email field); nothing renders below the button. The headline also changed the same day: "{Receiver}, {Sharer} shared a ticket with you." replaced "gifted you a film."
- **"(filmmaker)" gains parentheses** wherever the label renders (founder-approved 24 July, not built).
- ~~**Name-validation hardening**~~ — SHIPPED 31 July, exactly as approved (mechanical only, real names never rejected), plus a launch-morning fix that made the link check catch shortener domains by shape.
- ~~**show_ghosts off for Faith Circle**~~ — DONE 31 July, founder-executed in the sequenced spot: launch day, before the first real share.
- ~~**Mobile constellation names**~~ — SHIPPED 31 July, and the design pass produced a stronger principle than the diagnosis asked for: **the names are the product.** Every name that fits renders at every screen size; a name hides only on physical collision (gold path always wins, never hidden), and zooming in surfaces more. No thresholds, no blanket rules.
- **Constellation aesthetic pass** (queued 31 July, founder's three-point diagnosis): type hierarchy gold-first, a center exclusion zone, tighter dot-label anchoring. A design session, not a code errand.
- **The New Narrative test-pile cleanup** (queued, founder-run; scope updated launch evening): the old test tickets and test accounts on the test film — **excluding Young Chi's real ticket, protected in code.** The former companion task ("remove the stale protection lists") is subsumed: the lists carry real users again and are pruned, never removed.
- **Oregon service suspension** (overdue — checked 6 August, still responding): the rollback-net server is a week past its planned end-of-July window; the founder suspends it manually.
- **E2e flake investigation** (widened 25 July; new data 6 August) — the 24 July legacy-flow failures AND the full-suite load flicker (media-resume specs mostly, each passing alone). 6 August added three more single-spec full-run failures across three branches, every one a documented-flake spec passing solo — including one on a freshly started server, so the "long-lived server" lead from launch evening is at most part of the story. One investigation covers all of it.
- **TEMP content ledger** (all in src/content/filmStory.js, one-pass swappable): story copy still placeholder for The New Narrative and A Sacred Pause (Circles' is real as of 27 July), all filmmaker locations temporarily "Atlanta, Georgia" except Circles' (real), photos (Circles' portrait is real; the other two frames are empty), hand-picked poster timestamps via POSTER_OVERRIDES.
- Backlog unchanged: Safari private-browsing re-test, NetworkMap restructuring, webkit flake.

## How the work runs

Ien (non-technical) directs; an AI coding agent builds. The rules that keep that safe, battle-tested again this week: the agent commits but never pushes — Ien pushes, and production deploys from that; no production change of any kind without Ien's explicit approval in that session (a permissions prompt is not approval); diagnose before fixing, report-only first; one logical change per commit, full suite before it lands — and, since the 31 July false-green incident, every test gate runs unfiltered with its exit code checked, and reports quote the suite's actual summary lines; Ien verifies with his own eyes before any merge — narration, screenshots, and green checkmarks are evidence, not proof. All films are test films now (founder ruling, 24 July), but The New Narrative remains the designated test-claim film — and a local run writes to the real production database, so a "test" claim still mints a real, permanent ticket number. Durable rules live in code and documents, never in an agent's promise to remember: CLAUDE.md (implementation layer) and this brief (strategy layer) are both reconciled against shipped reality at the end of every working day.
