# Ledger — The constellation: layout laws and every pass

> Moved verbatim out of CLAUDE.md on 2026-09-16 (the split). The constellation rule text (the 27 KB layout paragraph: every law, knob, measurement and supersession) and the dated batches from 10 September to the 17 September FINAL LOCK. Read only when working on constellationLayout/constellationLabels/ConstellationMap, by grep. Read by grep + the section you need, never whole.

**The 2026-09-17 FINAL LOCK (branch `constellation-final`, cut from 86a8940 EXACTLY; PR #18 — PR #17 `constellation-wheel` ABANDONED and closed into it, PR #16 with it): THE THIRD PASS IS THE LOOK.** Founder decision 17 September: the +50 none-sharing render of commit 86a8940 (`constellation-diffusion`, third pass — sharers to the rim, and cohesion) is THE look. **The layout's geometry is LOCKED:** `src/lib/constellationLayout.js`, `src/lib/constellationLabels.js` and `constellationLayout.golden.json` are byte-identical to 86a8940 (md5-verified), and a per-node dump of today / +15 / +50 none-sharing / +50 ten-share-3 / +100 (the sanitized Circles fixture and the four simulations) differs from 86a8940's by **0 map units on every node, label and canvas** — the reference dump is kept beside the renders in `screenshots.local/constellation-final/geometry/`. Every later change to the layout stays OUT: 3d (the rotated spiral, the lift, the width-aware spread, sharers off the spiral, the limb floor), 3e (r0, the sparse opening, the reach scale) and 3g (the wheel). Cherry-picked from the abandoned passes ONLY what moves no dot — renderer, tests and e2e files only: (1) the phone opening frame fitted to the largest canvas-shaped rectangle inside the map box (`fitW`/`fitH` in `ConstellationMap.jsx` — a canvas taller than the box was letterboxed to a fraction of the plan's scale); (2) `data-layout-hidden` on every person group; (3) **ZOOM REVEALS EVERYTHING** — from 2× against the whole-graph view (`RETRY_SIDES_FROM_ZOOM`) a name the collision pass hid retries the other sides of its dot, inward first, then the two perpendiculars (`radialLabel` + `PERP_OFFSET`, the renderer's per-view choice recorded as `data-side`; the plan's side and every dot untouched); (4) the two laws' e2e on the fifty-with-ten-sharers tree (110 fictional rows in `e2e/constellation-fan.spec.js`). Already present at 86a8940, so no cherry-pick was needed: the visibility tier and YOU's always-on rule (a layout-hidden YOU still paints, its own-line touch reported, never counted), the pan clamp, the +100 single-rung guard (`labelFloorPx: 9.5` for scenario xi) and the 120 s per-test budget. NOT carried, by the founder's list: the wheel's stroke law, the ring/crossing/wade rules and their svg attributes. **Measured, identical on chromium, webkit and firefox:** YOU ALWAYS PAINTS — Priya's and Lena's dashboards paint YOU at the opening view and at 1:1, on a desktop (67 of 110 names painted) and on a phone (74 of 110 at the opening view; 22 and 21 at 1:1); ZOOM REVEALS EVERYTHING — the creator modal on the same tree paints 67 → 94 → 99 → 103 of 110 from 1× to 4× (8 names on a retried side at 4×), never fewer at a deeper zoom; at 4× the four names in view still unpainted (Cleo, Esme, Hugo, Isla — first-ring field leaves whose dot sits on another leaf's ray) are every one layout-hidden — the pinned exception, ONE DIRECTION FOR NAMES vs ZOOM REVEALS, the founder's to decide. Painted at rest in the desktop creator modal (the 9px rung): today 42 of 43 · +15 52 of 58 · +50 none-sharing 50 of 93 · +50 ten-share-3 46 of 123 · +100 52 of 143; Krist's phone opens on 42 of 43, Arielle's on the +50 ten-share-3 tree on 57 of 123. Renders: `screenshots.local/constellation-final/`. The golden is 86a8940's, unchanged (its two tests pass without a re-record).

**The 2026-09-16 third pass (branch `constellation-diffusion`, the PR that supersedes #15 and carries its defect fixes; golden re-recorded): SHARERS TO THE RIM, AND COHESION.** Founder decisions from the diffusion renders: spread ×1.0 is the rule (knob retired); the starburst spokes are accepted; and four rules — **THE RIM RULE** (a direct recipient who shared onward leaves the spiral for its outer edge + one field step, sharers evenly round the compass from 12 o'clock in ticket order, fans outward; a sharer reserves a spiral point so nobody after them moves; a new sharer moves to the rim once and the rim re-spaces; the spiral skips every point on a sharer's spoke), **CENTRED** (canvas centred on the filmmaker, equal margins; the field starts one name-height clear of the centre labels — `plan.fieldR0`, ≈105 map units at 11px), **ONE DIRECTION FOR NAMES** (field names outward, inward only when outward cannot clear, never perpendicular; `plan.fieldFlips` = 0 on every scenario), **THE VISIBILITY TIER** unchanged. The field measures with the BASE canvas's geometry per rung (deterministic; the canvas feedback and a canvas-dependent skip are gone). Painted at rest: Circles today 6/7 sharers · +50 none 6/7 · +50 ten share 3 10/17 · +100 3/7 — the unpainted sharers are sharers INSIDE fans (Alexander's 91-unit name in Krist's fan of ten) and, on the densest trees, rim sharers whose 98-unit perpendicular names reach back over the field (the rim step is ≈ 23 units); pinned per scenario with the reasons, no law bent. Record: `docs/watch-page-spec.md` §5b, the third-pass block. Renders at ×1.0: `screenshots.local/constellation-diffusion/`.

**The 2026-09-16 evening batch (branch `constellation-diffusion`, off `constellation-fifty`; DESIGN GATE — renders only, no golden re-record, no merge, no PR yet): THE DIFFUSION FIELD.** Founder direction after the fifty renders: "at any count the graph must read as an even spreading-out from the filmmaker, never a ring/cell." The first ring and its rows are GONE; the filmmaker's direct recipients sit on a sunflower spiral in ticket order (`FIELD_R0` 52 + c·√k, k × `GOLDEN_ANGLE`), skipping points within the clearance of anything placed, so adding a person never moves anyone placed before them (`fieldIndex`; unit-tested); sharers' branches are placed the moment the sharer is, every fan law unchanged; c is DERIVED from the rung's label size ((box height + clearance)/√π; the founder's options × 1 / 1.25 / 1.6 via the `spread` option — the render harness sets `globalThis.__dcConstellationSpread`, no product surface); THE VISIBILITY TIER (a sharer's name never hidden while a leaf's is painted) lives in `ConstellationMap.jsx`'s tiers. **The bent law, plainly:** straight spokes from the centre cannot keep "a dot never sits on a line" past ~30 direct recipients (a spoke's forbidden corridor at radius 100 is ≈ 12°); the field prefers a strict point within 40 indices and then applies the founder's point-only rule, counting every spoke across an inner dot (`plan.raysAcrossDots`: +50 → 26, +100 → 80; unchanged by the spread). Every sharer painted at rest — the founder's target — is missed on every scenario today (Oliver, at the field's first point beside the centre, never finds a side) and pinned in `constellationGrowth.test.js`. Full numbers and the two rule-level fixes found on the way (shelved sideless names; an over-full fan spreads evenly at the cap) in `docs/watch-page-spec.md` §5b's evening block. Renders: `screenshots.local/constellation-diffusion/`.

**The 2026-09-16 batch (branch `constellation-fifty`, PR pending the design gate — the founder sees the renders first): THE SWEEP MADE HONEST, THE FIRST-RING ROWS, AND THE FIFTY MEASURED (full record in `docs/watch-page-spec.md` §5b, the dated block).** Two defects on the LIVE rows (43 tickets, №2–№44; the fixture `screenshots.local/constellation-fifty/circles-fixture.sanitized.json`, read-only through `db-read.js`, no email column): five of Krist's ten dots painted on top of each other (Mom/Rachael/Taylor 1–2px apart, Grace on Brooks — the founder read it as a Brooks branch; Brooks has no recipients in the data) and Stacy's dot on the Krist → Alexander line — the law "a dot never sits on a line". Root causes, fixed at the rule level in `src/lib/constellationLayout.js`: the sweep's BISECTION assumed clearance is monotone in the gap (with a sharer among leaves it is not: 52.9° for Patti → Alexander where 15° clears — `smallestGap` now walks to the FIRST clear gap, `GAP_SCAN` 2°, then narrows); `widenFor` at the cap RESCALED every gap with no floor (the tail collapsed to 0.3° per gap — a pair now gets only the room left inside the cap); the plan's audit never counted DOTS on lines (`plan.dotsOnLines`, in `settled` and the ladder's ranking); `LINE_DOT_GAP` 0.6 → 1; a sharer sibling is measured on the perpendicular it will take, the one clear of its own incoming line (`resetSide`/`incomingClear`); a fan's stagger pattern is decided ONCE per build (`staggerMemo`, near-ties keep the earlier pattern) — with it every single addition and +15 first-ring people keep every other fan's rows (the Alexander pin closed; r0/r3 now under 20%). **Live rows at the desktop: 10.5px, 40 of 43 painted, 0 colliding, 0 dots on lines; Ien → Arielle 95px, Arielle → Krist 139px; hidden: Oliver, Charles (first-ring sharers whose perpendicular names cross the neighbouring rays) and Grace.** **FIRST-RING ROWS (founder rule 11 Sep, confirmed 15 Sep):** `FIRST_RING_ROWS` [1, 1.35, 1.7] × the base radius; a non-sharing first-ring person takes the lowest row at which name and dot clear (`rerow`, outward at any row before a perpendicular); sharers stay on the base radius; a leaf's row is the relaxation's first move on the ring and a leaf may be lifted to free a ring sharer's name (`liftFor`); **the base radius NEVER grows — `RING_ARC`, `RING_BUMP`, `MAX_BUMPS` are RETIRED** (on +15 first-ring people that costs 3 names against the 11 September 227-unit ring: 40 of 54 vs 43 — reported). **THE FIFTY DOES NOT FIT under the laws as they stand — no fourth mechanism was added; STOPPED and reported:** Circles + 50 non-sharing first-ring people → 11px, 41 hidden + 16 colliding (32 of 93 painted); + 50 with ten sharing 3 each → 9.5px, 72 hidden + 11 colliding (42 of 123); both pinned as KNOWN GAPS in `constellationGrowth.test.js` (ix)/(x). The wall in numbers: 63 slots at the fixed 118 radius are 11.6 units of arc apart; names are horizontal strips 26–81 units wide, so near 12 and 6 o'clock a name needs 34–88 units of arc and three rows give it 35; the ring sharers' perpendicular names cross 4–7 rays; the sharers' fans (Arielle's eight at 185–218) sit in the lifted rows' band (159/200). The blocking law is the fixed base radius (three rows would need ≈ 230 units at the poles). **The golden snapshot's two tests are `it.skip`ped pending the founder's approval of the renders (re-record then); `PERP_OFFSET_MAX` stays 52 (80 was measured: it frees Oliver's ray but hides Arielle and Themba).** Renders: `screenshots.local/constellation-fifty/` (creator modal + Krist's thread, desktop and phone, both simulations desktop and phone).

**The 2026-09-11 batch (branch `constellation-reach`, PR #11, NOT merged — the founder sees the renders first): THE FOUNDER'S LAWS "A FAN NEVER BALLOONS", "THE FIRST RING GROWS WITH ITS COUNT" and "SHRINK BEFORE HIDE" (spec §5b rewritten; `src/lib/constellationLayout.js`, `src/lib/constellationLabels.js`, `src/components/ConstellationMap.jsx`).** Context: on the LIVE v4 graph one new invitation (2026-09-10) flew Arielle's branch far from her with long lines and hidden names; the recovered 10 September evening amendment (lines dot to dot, names beside dots — committed first as `d41277d`) read Arielle → Krist at 155px for the same reason. Now: a crowded fan solves its crowding IN PLACE — spread within `FAN_MAX_SPAN` **140°** (was 120°) with each adjacent gap the smallest that clears (a left-to-right sweep, not incremental widening), then STAGGER its leaves into two rows (near d / far `STAGGER_RATIO` 1.55 × d; a sharer always keeps the near row, so every child stays beyond its parent; four alternation orders, the one with the fewest broken pairs wins; decided once by the fan's own names), then move the NAMES (out → in → perpendicular; a perpendicular name now HANGS along the limb — a leaf's outward, a sharer's inward — never centred straddling both neighbours' lines; a name no side clears asks its fan for room), then move the whole fan outward by at most `EXTRA_MAX` **0.5 × d** — a hard cap (`extra` per node); every other outward trigger is deleted. A line also keeps 0.6 of the clearance from any dot it is not attached to (the stagger made a far-row line run over a near-row dot). The first ring's radius is at least count × `RING_ARC` (34) / 2π. **SHRINK BEFORE HIDE:** names are sized PER FILM — the largest of `LABEL_SIZE_LADDER` 11 / 10.5 / 10 / 9.5 / 9px at which every name paints at the reference view (`plan.labelPx`; `MIN_LABEL_ON_SCREEN_PX` is now the ladder's bottom, 9; `MAX_LABEL_ON_SCREEN_PX` 11); when no rung is clean the rung losing the fewest names holds (builder's reading, pending the founder's word); the renderer re-walks the same ladder at its own view (`pickLabelSize`, translation-invariant so panning never changes the rung; `data-label-px` / `data-plan-label-px` on the svg) — the phone follows it at its own width; the center labels ride the same rung. `REACH_K` **18 → 31**: with the outward move capped the limb is the reach rule alone and the founder's px targets fix K (30 + 31 × 4 = 154 units = 124px on the 33-ticket tree). A plan that cannot settle keeps its LAST placement round rather than re-placing on the base canvas (which hid 21 of 54 names on tomorrow's first ring; the last round paints 38); a fan of more than `HOPELESS_KIDS` 16 children skips the ladder's steps; `MAX_FLIP_PASSES` bounds name flip-flops. **Measured on Circles as of 2026-09-11 (39 tickets, fixture regenerated read-only through `db-read.js` — `screenshots.local/constellation-reach-2026-09-11/circles-fixture.sanitized.json`, no email column, grep-verified; №39 Alexandria claimed under Arielle on the 10th, №40 Tony first ring on the 10th):** at the founder's desktop the plan chooses **11px and loses 3 of 39 names** (2 hidden by the layout, 1 colliding); the renderer steps to 10.5px and paints 37/39 on both desktop surfaces; **Ien → Arielle 95.1px, Arielle → Krist 143.4px** (Arielle's eight take the capped 24-unit move; Krist's ten sit 30.5 units out in two rows, spanning the full 140°), Krist → Alexander 110px; tightest painted gaps on the desktop: name–name 6.79px, name–dot 7.58px, name–line 9.68px; the phone opens on the thread at 11px (37/39 painted), 14/39 at 1:1. **THE GROWTH TABLE and STABILITY are in `src/lib/constellationGrowth.test.js`, every miss pinned as a KNOWN GAP (a pin FAILS when the gap closes, so the list is pruned on purpose):** none of the eight scenarios settles clean at ≥ 9.5px — (i) today 11px, 3 lost · (ii) +1 first ring 10px, 4 lost · (iii) +15 first ring 10.5px, 11 lost · (iv) +15 and four share 3 each 9.5px, 14 lost · (v) Stacy +3 9px, 3 · (vi) four quiet first-ring +4 each 9px, 6 · (vii) Krist's other nine +2 each 9px, 10 · (viii) one leaf under ten people 9px, 11; the "longest segment ≤ 2.5 leaf reaches" target cannot hold on any tree with Krist (his limb alone is 2.52 by the reach rule); stability: no OTHER fan's rows change for a single addition (the joined fan, and the fan of a leaf who becomes a sharer, may) ✓, but the worst dot move is 20.3–34% for eight of nine additions (12% for the ninth) — the ladder re-chooses the whole film's rung (11 → 9) and every fan's gaps move with it, and the relaxation turns a neighbouring fan; +15 first-ring people rotate the ring evenly at 227 units ✓ but re-pattern Alexander's fan and drift Krist's children in Arielle's frame. **Red-team round (11 September, one subagent, the delta only):** fixed — the renderer measured the viewer's node as the three-letter "YOU" and its ladder counted hidden names instead of naming them (a viewer who was one of the hidden two lost a first-ring name instead; the layout now emits `measureName` and the ladder requires ids), a sibling no gap could clear took all the fan's remaining room and starved the rest (now an even share), the hopeless shortcut skipped the stagger (a 17-child fan drew worse than a 16-child one), a stale K-31 comment claimed the live film settled clean, and the hang had no behavioural test; reported, not fixed — the 131-person test tree takes ~4–6 s (the ladder multiplies the cost on films that never settle; Circles itself ~250 ms), and the stability law above. **The settle is a knife edge:** K 30 and 32 each lost one or two more names than K 31 on the 33-ticket tree. Renders + geometry: `screenshots.local/constellation-reach-2026-09-11/` (creator modal desktop/phone, viewer Krist/Arielle desktop/phone, Krist phone 1:1, both dashboards, and the creator modal for scenarios iii and iv). Performance: Circles ~250 ms for the whole ladder; the 131-person test tree ~4 s (2 rungs — reported, the ladder multiplies the cost on films that never settle). Timings of the day's gates are in the commit messages.

**The 2026-09-10 batch (branch `constellation-reach`, PR opened by the verifier, NOT merged — the founder sees the renders first):** the constellation's layout is the founder's rule **"a branch's length is its reach"** (spec §5b rewritten; the constellation paragraph under "Dashboard behaviour"): v4's rings-and-levels and the rejected v5 (PR #10) are gone; the first ring is v4's; everyone else sits beyond their sharer at `30 + 16 × √subtree` map units inside a ≤120° fan; fans separate by least movement; the generation rings are dropped; the canvas is fitted to the drawing; names at **11px**; the line law, the per-element recede at v4's faintness (line 0.16 × 0.5), non-scaling one-pixel strokes, the creator's phone camera and the Charles → Jacob clip fix kept from v5; **one ground: ink** on both dashboards and the modal. Renders + geometry from the sanitized Circles fixture in `screenshots.local/constellation-reach-2026-09-10/`. **The founder's evening amendment (same branch, 10 September): LINES CONNECT DOT TO DOT — no segment is ever clipped; a name that would sit on a line moves (out → in → perpendicular) or the safety net hides it; the limbs read a tiny bit longer (base canvas 715, `REACH_K` 18: Ien → Arielle 95px); names at 10.5px, the largest size at which Circles then settles with every name shown.** Growth scenarios at 11px (Stacy +3; four quiet first-ring +4 each; Krist's other nine +2 each) all fall back to hiding — reported, not hidden. The other two films, read-only through `db-read.js`: The New Narrative (31 drawn, ghosts hidden) settles and paints 31/31 at the desktop in 9 ms; A Sacred Pause (57 drawn, ghosts shown) does not settle at 11px and paints 50/57 at rest, 86 ms (it did not settle on v4 either). Red-team round (10 September): the plan no longer re-runs on larger canvases once a placement could not satisfy the rules (a 131-person tree went from 2.3 s to 0.4 s; A Sacred Pause from 1.2 s to 86 ms), the pair loop has a bounding-box broad phase, the timing gate is a bounded-work assertion, and the dead `LINE_OPACITY_FLOOR` alias is gone. Pre-existing, noted by the review, unchanged: `NetworkGraphModal` gates ghosts on `show_ghosts` even on the admin surface.

- **The constellation** replaces NetworkGraph on the viewer dashboard ONLY — `NetworkGraph.jsx` still serves the creator dashboard's team-member "Network impact" block, the Profile page's film previews, and the legacy `/i/:token` screening pages (`graphLayout.js` also serves `server/index.js`). The Network Map page was DELETED 2026-09-03; the creator dashboard's "See network graph" modal (the VIEWER constellation) replaced it. **THE LAYOUT RULE IS THE FOUNDER'S "A BRANCH'S LENGTH IS ITS REACH" (10 September 2026, approved on a drawing of Circles' real rows; built on branch `constellation-reach`; binding spec `docs/watch-page-spec.md` §5b; unit-tested in `src/lib/constellationLayout.test.js`), which SUPERSEDES the 9 September v4 rules recorded below (kept as the record) and REJECTS v5 (PR #10, rings-as-generations — "less cohesive than v4"; several of its drawing rules were kept, see the spec):** (1) the first ring is exactly v4 — the filmmaker's own invitations evenly around the full circle in ticket order, the centre emblem and labels unchanged, a crowded ring bumped outward as a whole; (2) every other person sits BEYOND the person who gave them the film, inside a fan centred on that parent's outward direction (from the centre through a first-ring parent; for a deeper parent, from their own parent through them), at `REACH_BASE + REACH_K × √(their own subtree size)` — `REACH_BASE` 30, `REACH_K` 18 (a leaf 48 units from its sharer; Krist, subtree 16, 102 from Arielle before any fan move; the base canvas's height 715, not 800, paints the first ring at 95px on the founder's desktop — the evening amendment's "limbs a tiny bit longer") — so a big sharer's branch reads as a long limb (Ien → Arielle → Krist → Alexander on Circles) and quiet tickets stay close; (3) within a fan siblings spread only as far as the clearance rules require (each adjacent pair's gap on its own), never wider than `FAN_MAX_SPAN` ≈ 120°, the whole fan moving outward from its parent (`extra`, reported per node) only when it cannot fit that cap; fans of different parents that would collide separate by the LEAST MOVEMENT — both turn about their own parents, equally, damped — never by changing anyone's distance; **the triggers of a fan's outward move, named for the founder to strike any (red team, 10 September; two of the original four went with the evening amendment): (a) its own names need more than the cap; (c) a sharer's branch wedged between that sharer's own siblings makes the outer fan spread and, at its cap, move outward — on Circles Krist's ten sit 48 + 150 units from him, Arielle's seven 48 + 90, Alexander's five 48 + 30, the reach order inside every fan intact;** v4's "step out to the next radius level" is GONE; (4) one drawing on every surface — the viewer's presence changes NO position (the layout measures the viewer's REAL name; "YOU" only on output), the only per-viewer differences being the gold thread and, on a phone, the opening camera. The generation rings are DROPPED (`layout.rings` no longer exists). The canvas is FITTED to the drawing (a reach drawing is lopsided by nature; a canvas mirrored about the filmmaker wasted half the reference view), never smaller than the base 900×800, a small drawing centred inside it — so the filmmaker sits wherever the drawing's balance puts him. Every clearance rule of v4/v5 stays and is measured on real rendered boxes at the reference view (label-vs-label, label-vs-dot with a hollow dot's stroke counted, label-vs-line); **LINES CONNECT DOT TO DOT (founder amendment, 10 September evening): every segment runs from dot centre to dot centre, whole, never trimmed around a name (the segment-clip rule and the Charles → Jacob clip fix are GONE); a name never sits on a line — six px from an unattached one, never touching its own — and the remedy is MOVING THE NAME, never the line: out → in → perpendicular to the limb (16–52 units from the dot, the smallest that clears; a sharer's name is perpendicular by construction, the fan leaning away from it within the beyond-the-parent limit); a name no side can clear is `hidden` (the safety net, `plan.hidden` counts them — Circles hides none at 10.5px);** the plan loop (assume a canvas, place, grow until it fits; `plan.settled`; the base-canvas fallback with the renderer hiding what would touch) is unchanged. **Name size: `MIN_LABEL_ON_SCREEN_PX` = 10.5 (11 that morning, 9.5 in v4)** — under the evening amendment the largest size at which Circles settles with every name shown on both Circles-shaped trees (10.75 on the fixture alone; 11 hides Alexander; 11.5 hides Oliver) — the founder's "stop at the size, don't shrink the limb". **Kept from v5 (founder, 10 September):** THE LINE LAW — solid = arrived (the recipient claimed), dotted = in flight, `arrived` on every edge, a viewer's thread solid gold to YOU and dashed gold to hollow gold dots for their in-flight invitations; the recede PER ELEMENT (`opacity` on each off-thread `<g data-node>` and `<line>`, nothing re-mounts on hover, an explored lineage lifts to full strength); **THE PINNED FAINTNESS — off-thread on a viewer's dashboard is exactly the live v4 look: base greys line `LINE_ALPHA` 0.16 / name 0.45 / dot 0.7 × `RECEDE_OPACITY` 0.5 → a line at 0.08, NOT v5's half-strength line;** LINES NEVER VANISH (`vector-effect: non-scaling-stroke`, one device pixel at any zoom, the grey never under `LINE_ALPHA`); ~~the Charles → Jacob clip fix~~ (withdrawn with the evening amendment — lines are never clipped); `PERSON_DOT_OBSTACLE_R`; THE CREATOR'S PHONE opens on the film and the whole first ring centred on the filmmaker (`firstRingFrame`), the viewer's phone on their thread as before, every desktop on the whole graph, + / − / 1:1, pinch and drag everywhere. **The whole-graph name policy is the existing safety net ONLY (hide what would touch; zoom reveals) — the founder decides the opening view for large films later, with real numbers.** Growth scenarios at 11px, reported honestly: Stacy +3, four quiet first-ring people +4 each, and Krist's other nine +2 each all fall back to hiding (the first two settle at 9.5px). Measured on Circles' rows at the founder's desktop (creator modal 956×576; viewer column 1038×576): 37 of 37 names painted at 11px; tightest name-name 6.17px (Joiselle / Cal), name-dot 6.01px (Marcus's name / Daniel's dot), name-line 9.38px; Ien → Arielle 95.1px, Arielle → Krist 154.7px, Krist → Alexander 188.6px at 10.5px names (the evening amendment; that morning 85 / 103.7 / 136.2 at 11px); the plan settles in 2 rounds on a 935×715 canvas. The `-v4` measurements below are the record of the superseded rule. **The golden snapshot `src/lib/constellationLayout.golden.json` was DELIBERATELY RE-RECORDED on 2026-09-10 for the reach rule** (no rings; `dir`, `dist`, `extra`, `subtreeSize` per node; `arrived` per edge; `firstRingFrame`; the fitted canvas). E2e: `e2e/constellation-fan.spec.js` (the reach assertions, the line law, the line floor, the per-element recede, the creator phone opening, the ink ground on both dashboards and the modal, on all three engines). **ONE GROUND: INK (founder, 10 September 2026):** the map box, the viewer's screening cards, the creator's film cards and its "Network impact" box sit on `ink` with their hairlines (`ink-2` and `bg-card` are gone from these surfaces); the + / − / 1:1 buttons unchanged; `--color-ink-2` stays defined (AboutModal, ShareLinkModal, the dev emblem harness use it); the auth-form fields keep `bg-card` by scope. **11 September 2026 — the founder's three laws supersede rule 3's outward moves and the fixed name size below: A FAN NEVER BALLOONS (spread ≤ 140° → stagger into two rows → move the names → at most `EXTRA_MAX` 0.5 × d outward, nothing else ever moves a fan out), THE FIRST RING GROWS WITH ITS COUNT (radius ≥ count × 34 / 2π), SHRINK BEFORE HIDE (the size ladder 11 → 9px per film and per view). Spec §5b and the CURRENT STATE entry for 2026-09-11 carry the rules, the measured Circles numbers, the growth table and the stability gaps; `REACH_K` is 31.** **16 September 2026 (branch `constellation-fifty`): ~~THE FIRST RING GROWS WITH ITS COUNT~~ is RETIRED by the founder's rows rule (11 Sep, confirmed 15 Sep) — FIRST-RING PEOPLE NO LONGER SHARE ONE RADIUS: non-sharers may sit at 1.35 or 1.7 × the base radius (`FIRST_RING_ROWS`), assigned by the solver; sharers keep the base radius; the base radius (118) never grows. The same batch made the fan sweep honest (first-clear gaps, no cap rescale, dots-on-lines audited, a fan's rows decided once per build) — see the CURRENT STATE entry for 2026-09-16.** Its four layout rules are BINDING and unit-tested in `src/lib/constellationLayout.test.js` — **AMENDED by the founder's reversal of 5 September 2026 (recorded 9 September, built on branch `constellation-fan`), which REVERSED the 22 July rule "sector ∝ subtree, children spread within the parent's sector":** on Circles that rule handed Arielle's branch most of the circle, smeared Krist's ten evenly across it, and made the outer ring read as its own evenly-spaced circle with the filmmaker visually off-center. The rules now: (1) the filmmaker sits at the EXACT center, rings stay concentric by generation, and the FIRST RING — the filmmaker's own direct tickets — is spaced EVENLY around the full circle regardless of anyone's branch size (chronological order, first ticket at 12 o'clock on every surface — team-member nodes, which hold no ticket, sort first; ~~the viewer's rotation turns the whole ring~~ — there is no rotation since 2026-09-09); (2) every deeper generation CLUSTERS at its parent's angle — siblings fan out in a tight fixed angular step (`FAN_STEP`, ≈4.3°) centered on the parent, and the fan widens ONLY as far as needed to keep the siblings from colliding — name on name, or a name on the neighbouring sibling's status dot (red-team finding: at the fixed step a gold invitee's name painted over the next invitee's dot) — under the existing collision estimate (the layout asks `constellationLabels.js` the same design-scale question the renderer asks — ~~`LABEL_SIZES` and the `DOT_RADII` moved there~~ since 2026-09-09 ONE label size and ONE dot size, `PERSON_LABEL_SIZE` and `PERSON_DOT_R`, live there so the renderer, the collision rects, and the layout share them; the layout measures the viewer's REAL name — the label reads "YOU" only on output — so the viewer's presence cannot widen a fan differently than the modal, the red-team blocker of 2026-09-09); ~~gold tangential names lie along the ring, so a fan of gold invitees at the top or bottom is necessarily wider than a dim-web fan at the side (measured on Circles' rows at this commit: Krist's ten span ≈39° as a dim-web fan in the creator modal and ≈142° as his own gold fan on his dashboard)~~ — moot since 2026-09-09: every name is radial at one size, so a fan is the same width whoever is looking (Krist's ten span ≈39° on every surface); a fan NEVER fills a proportional sector; ~~where two parents' fans would overlap on one ring they are nudged apart MINIMALLY — an exact least-movement packing (`packLine`, isotonic regression on the seam-cut ring; two overlapping neighbours move equally, untouched fans stay put), with a bounded symmetric relaxation only for the wrap-around seam — and only a ring that cannot hold its fans at their steps has the steps compressed to fit~~ **SUPERSEDED the same day by the verifier's finding (9 September, second round — Arielle's and Krist's names sat a few pixels apart and collided): LABEL CLEARANCE IS A HARD RULE, not an estimate — no two label boxes on the map may overlap or come within 6 screen px of each other, and no label may cross another person's dot. A fan widens as far as that rule requires, using the empty arc beside it — everything up to the neighbouring fans already placed at the SAME radius (their dots plus their names' reach; a fan is never nudged off its parent's angle); if the fixed step does not fit that arc the step tightens to the clearance minimum; if even that is not enough the fan is PUSHED OUTWARD for that branch to the next radius LEVEL (`RING_BUMP` = 46 further out, up to `MAX_BUMPS` = 24), where the free arc is measured again against the fans at THAT level, the generations below it following one ring step beyond their parent — rather than let names touch. (The first form of this rule measured a fan's arc as halfway to the neighbouring PARENT — radius-blind: two sibling parents four degrees apart could never share an arc and their fans were flung 24 pushes out; the red-team found it, and the level form replaced it the same evening.) A global pass then checks EVERY label pair on the map and every label against every other dot (and the filmmaker's two center labels), moving the fan responsible a level out until the rule holds; a first ring too crowded for its names moves outward as a whole, still even. The label box the rule measures is the renderer's own estimator, made exact 2026-09-09: glyph by glyph from the Phoenix Regular font file (`GLYPH_WIDTHS` — W is 0.909 of the font size, I is 0.142; anything outside the table counts as W) and 1.2× the font tall (ascender to descender); every node is measured no narrower than "YOU" (the viewer's node paints that word, and measuring everyone the same way keeps the geometry viewer-independent). **The rule is planned for the REFERENCE VIEW — the founder's desktop, `REFERENCE_VIEW` = a 960×576 map box, the creator modal's (the narrower of the two desktop surfaces, so a pair planned at 6px never paints under 6px on either) — in the units that view paints:** a name's box is what the renderer's floor makes it there (~~`fontScaleFor`, the rendered width over the canvas width — the 2026-07-31 formula kept on purpose~~ — SUPERSEDED in the v4 round: `fontScaleFor` is gone, the floor is 9.5px against the TRUE scale on every surface, the founder's call made) and the clearance is 6 screen px in map units (`mapScaleFor`, the true scale). Both depend on the canvas and the canvas on the placement, so the layout ASSUMES a canvas, places for it, and grows the assumption until the placement fits inside it — a SETTLED plan: the output canvas is the assumed one and every gap was measured for exactly the scale it is shown at. Bigger boxes need a bigger canvas which paints bigger boxes, a feedback that does not always close (a first attempt fed the canvas straight back and reached a 7,655-unit canvas on Circles + one share): after `MAX_PLAN_ROUNDS` = 6 the plan FALLS BACK to the base canvas's boxes (fixed, no feedback) and reports `plan.settled = false` — the renderer then paints names larger than planned at the reference view and hides what would touch, and zooming reveals. The renderer enforces the same rule at every other view the same way: `LABEL_GAP_PX` is 6 (was 3) and every other person's dot is an obstacle a painted name may not cross (`labelVisibility`'s `obstacles`); it now measures its rendered height as well as its width so collision boxes land where names land (`mapScaleFor`). `layout.plan` = `{ fontMap, clearance, settled, rounds }` (a plan that did not settle is console-warned by the renderer, like a gold-gold overlap); the unit tests assert the rule in those units, and that the fixtures — the Circles-shaped tree, Circles + Stacy's three, Circles + four quiet first-ring sharers × four — SETTLE; `e2e/constellation-fan.spec.js` measures the painted rule from real text boxes on both desktop surfaces (every name painted, smallest gap ≥ 6px, no name over a dot). Measured on Circles' rows at the -v3 round (rendered in chromium, real text boxes; the v4 numbers below supersede these): canvas 1,035², plan settled in 2 rounds, smallest painted gap 6.30px on the creator modal (desktop) and 7.19px on a viewer's dashboard (desktop), 8.04px on the viewer phone (12 of 37 names painted at rest, the rest on zoom) and 9.14px on the modal phone (14 of 37); zero labels across a dot on desktop — ONE known exception on the viewer phone: YOU's always-on label crosses Mom's dot (the marker is exempt from hiding by the founder's decision; his call whether it should yield on phones); Krist's ten span 54°; the pushed branches sit at 239 (Arielle's seven), 313 (Krist's ten), 434 (Alexander's five). The film's next realistic shares (Stacy's three; four quiet first-ring people sharing four each; Krist's other nine sharing two each) still settle; whether a denser burst settles depends on the names — with the builder's filler names, fifteen more tickets under Alexander's five or twenty more under Arielle fell back to hiding, with the verifier's they settled — the fallback is the safety net, not the expectation. ~~Settling on random realistic trees: every tree up to ~60 people settles or falls back cleanly with zero rule violations among settled plans~~ — the red team's v4 audit (9 September, 100 random realistic trees, 8–79 people) measured the truth: 16 of 17 trees under 20 people settle, 17 of 30 at 20–39, 2 of 27 at 40–59, none at 60–79; every settled plan is clean at its units and every fallback is a real placement (see the v4 paragraph); dense films fall back to hiding, and the renderer's hiding then carries the rule.** (3) ONE label rule — every person's name placed RADIALLY at ONE size (`PERSON_LABEL_SIZE`, 8) on every surface, every person the same dot (`PERSON_DOT_R`, 2.4; solid = claimed, hollow = in flight); ~~tangential gold-path labels, per-kind label sizes (you 11.5 / path 9 / invitee 9.5 / downstream 8), the larger YOU node with its halo, per-kind dot radii~~ are gone; (4) NO rotation — YOU sits wherever the geometry puts it, marked by its solid node and its label, and the viewer's presence changes NO position, angle, ring or label (unit-asserted node by node against the no-viewer layout, including a viewer among long-named siblings in a top-of-ring fan; e2e-asserted for two personas — one in a side fan, one in a top fan — on both surfaces on all three engines). The ONE permitted per-viewer difference is COLOUR: the viewer's thread — the path from the filmmaker to them (segments and nodes) AND their own branch, every ticket they created and everything that grew from those, all generations — is drawn gold (`layout.threadIds`, lit at rest by the one renderer; same stroke weight, same type, same sizes as the rest). **Label visibility follows the same decision (the founder's own words: "no enlarged or stacked labels on phones — the phone shows the same picture scaled to fit its width, with the modal's zoom controls available if names get small" and "if a rule exists only for the viewer surface, it goes"): the 2026-07-31 clause "gold-path names always render" is SUPERSEDED — thread names thin by collision exactly as in the modal, and only YOU's marker (plus the filmmaker's center labels) is always on. Recorded here as a supersession by the builder's reading of that decision; the founder confirms it when he sees the `-v2` phone render.** The map's `aria-label` (screen-reader description) is builder-written descriptive text, not founder copy — the 2026-09-03 modal sentence and the 2026-09-09 viewer variant ("Your own thread is gold: the path the film took to reach you, and everyone it reached through you.") both; a founder-stamped version replaces them whenever he writes one. Phones show the same picture scaled to the width — ~~enlarged gold labels~~ no viewer-specific sizes, and ~~gold-path names always render~~ no viewer-specific visibility: the thread's names thin by collision exactly as in the modal (only YOU's marker is always on), under the modal's readability floor, with the modal's zoom controls. ~~hover lights the whole web gold~~ is gone; hover/tap lights one person's lineage on every surface. Everything else — colors, fonts, background, dashed rings, solid = claimed / hollow = in flight, the collision-based label rule, the explore hover/tap — is UNCHANGED. The legacy map (`graphLayout.js`) was the reference for the fan behaviour (children in a block centered on the parent, neighbours pushed apart), not code to copy — its push was one-directional, this nudge is symmetric. The per-node `sector` field is gone with the rule it served. **THE LAW — "nothing competes" (founder, 9 September 2026, the v4 round; recorded in full in `docs/watch-page-spec.md` §5b):** a viewer's thread — the path from the filmmaker to them AND their own branch, every generation — is what the drawing is about, and nothing may intrude on it. (a) DRAW ORDER: the renderer paints the rings, then every non-thread segment, dot and label (the `g.off-thread` group), then the thread's segments, dots and labels last (`g.on-thread`) — gold is never under grey. (b) CONTRAST: with a thread, the off-thread group is painted at `RECEDE_OPACITY` (0.5 — the builder's proposal, the founder tunes it), one constant for segments, dots and labels alike; without a thread (creator surfaces) opacity 1. (c) COLLISIONS: a label box may not touch ANY line it is not attached to, and a label may not sit on the line leaving or entering its own dot — resolved by LABEL SIDE (`labelSide` per node: a name turns to the INWARD side of its dot, away from the outgoing branch — `radialLabel(θ, x, y, side)`; people who shared onward prefer the inward side when it is clear) or by starting the segment beyond the label box and ending it before an inward name's box (`clipSegment`, in the renderer, by the clearance at the current scale; a first-ring segment starts beyond the film node's emblem and center labels), never by hiding. Label-vs-label, label-vs-dot, label-vs-line — all 6 screen px minimum, all in the layout's placement rule (`segmentTouchesRect` in the global pass; a surviving line conflict asks the fan the line leaves from to open on both sides of that member, `DEMAND_STEP` per restart, `MAX_RESTARTS`) and in the renderer's visibility pass (`labelVisibility` takes every other person's dot AND every unattached painted line as obstacles). Lines crossing lines is permitted. THE PHONE CAMERA: on a viewer's phone (a map box narrower than `PHONE_MAX_WIDTH_PX` = 768) the map opens on `layout.threadFrame.full` — the film, the path to YOU and YOU's whole branch with every name's planned box — at no smaller a scale than the plan's own (`plan.scale`, so nothing inside the frame hides), or on `threadFrame.firstGeneration` when the whole thread cannot fit legibly; the creator's phone and every desktop open on the whole graph; pinch (two pointers) zooms, drag pans, + / − / 1:1 on every surface, 1:1 = the whole graph. YOU's label never yields on any surface. NAME SIZE: `MIN_LABEL_ON_SCREEN_PX` is 9.5 and the floor counter-scales against the TRUE scale (`mapScaleFor`) on every surface — the founder asked for desktop names enlarged by half (≈7 → ≈10.5px) and, if Circles no longer settled at that size, for the largest size at which it does: 9.5px is that size (10 and above fall back). VISIBILITY TIERS: the always-on labels (the filmmaker's two, YOU's marker), then the thread's names — never hidden by a non-thread name — then the rest. THE CANVAS is centered on the filmmaker with each axis from its own extent (a film whose deep branch runs sideways needs a wide canvas, not a tall one; the reference view then paints it larger — this is what lets Circles settle at 9.5px). The filmmaker's center labels sit below the emblem by the clearance at the current scale (`centerLabelLayout`, shared by the layout's first-ring check and the renderer) — the -v3 phone defect where they painted over the emblem. Measured on Circles' rows at 9.5px: the plan settles in 3 rounds on a 1,311×806 canvas; every category clean at the reference view (label-label, label-dot, label-line all ≥ the clearance); the branch under Arielle sits at 331 / 451 / 572. Painted (chromium, real text boxes, every `-v4` render from the ONE sanitized fixture `screenshots.local/constellation-fan-2026-09-09/circles-fixture.sanitized.json` — no emails): both desktop surfaces 37 of 37 names at 9.5px, tightest name-name 6.34px (Steve / Brian), name-dot 14.06px (Donna / Steele's dot), name-line 10.72px (Christina); Arielle's phone opens on her whole thread (23 of 23 thread names, 37 of 37 painted, tightest 7.89px); Krist's phone opens at the plan's scale (17 of 17 thread names painted): his first generation is WIDER than a phone at the legible size, so the camera centres on the PATH (`threadFrame.path` — film → Arielle → YOU, the origin never off-screen) and the viewer pans to the rest; at 1:1 (the whole graph fitted) a phone paints 6 of 37 names (31 hidden — 3 of Krist's 17 thread names), the creator's phone 5 of 37 — every line painted at every view. **The red team's v4 round (9 September) and what it changed:** the fallback after the demand restarts is now a REAL placement (the old stand-in returned the base canvas with the nodes wherever the aborted attempt left them — 63 of 67 nodes off-canvas on one tree, a 16-unit phone frame); a demand is CAPPED (`MAX_DEMAND` 0.3 rad per side — past it the fan is pushed outward; two other remedy orders were measured and rejected, see the module); the renderer clips a segment only around the names it PAINTS (clipping around hidden names erased a viewer's own thread at 1:1 on a phone) and never drops a line — a first-ring line may pass under the film node's own labels when starting beyond them would leave nothing (Jan on Circles at one plan scale), and at worst a line paints whole; the layout refuses a placement whose line an outward parent name and an inward child name would swallow (`segmentKeepsLength`), and checks first-ring names against every other first-ring line; the filmmaker's two centre labels sit a hair past the clearance (an exact-clearance stack read as an always-on collision through float error, on every surface); the phone camera keys on the VIEWPORT width, not the map box (a laptop with the sidebar beside the map opens on the whole graph); YOU's name is placed by the same rule as everyone (measured with the real name) and always painted — on a settled plan it collides with nothing, on a fallback plan a collision is reported, never hidden. **What does NOT settle at 9.5px, reported for the founder's decision: Circles' next realistic shares (Stacy sharing three; four quiet first-ring people sharing four each; Krist's other nine sharing two each) fall back to hiding at this size and at every size down to 8.5px — the physics: Krist's ten need ≈40° of sweep at their radius, so under law (c) Arielle's fan must open to ≈80° around him, which runs her own lines across her first-ring neighbours' names in their 40° slots; opening fans, pushing outward and turning names inward were each tried and each moves the collision rather than removing it.** **The golden snapshot `src/lib/constellationLayout.golden.json` was DELIBERATELY RE-RECORDED four times on 2026-09-09 — for the fan reversal, for the one-graph decision, for the hard clearance rule, and for the nothing-competes law** (it now guards the un-rotated, one-label-rule shape with the viewer's `threadIds`; any future shape change re-records it on purpose). E2e: `e2e/constellation-fan.spec.js` renders a Circles-shaped tree (fictional names) on both surfaces (all three engines) and asserts the same person lands at the same angle and radius on the viewer dashboard and in the modal, with the same rings and label sizes, and that only the viewer's thread is gold.
