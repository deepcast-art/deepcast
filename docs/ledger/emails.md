# Ledger — Emails: dispatcher doctrine, ticket email, reminders, legacy invite email

> Moved verbatim out of CLAUDE.md on 2026-09-16 (the split). The email-sending doctrine, the ticket email and return link, the hourly reminder sweep, and the LEGACY email-first invite flow (GIF retired, send flow, /i/:token). Read before touching anything that sends mail. Read by grep + the section you need, never whole.

## Email-sending doctrine

- **Every outgoing email goes through the one dispatcher** (`deliverEmail` in `server/index.js`, built on `server/emailDelivery.js`): sends are strictly sequential and throttled below Resend's rate limit, and each send is automatically retried with backoff before giving up. Never call Resend directly from a route.
- **Acceptance is verified, per recipient.** The dispatcher resolves only once Resend confirmed it accepted the email. `/api/invites/send` awaits this before answering; on permanent failure it rolls back the invite row and the allocation and returns an error, so a retry starts clean (not blocked by the duplicate-invite check).
- **The UI never claims success for a recipient whose email was not confirmed accepted.** Multi-recipient sends report per-recipient truth: failures are shown clearly and the failed recipients stay in the form for retry (`handleSendLetter` in `InviteScreening.jsx`, `InviteForm.jsx`). `server/emailDelivery.test.js` proves the throttling, the retry, and the honest-failure behaviour.
- **The ticket email and the reminders (2026-09-15/16) follow this doctrine to the letter** — both through `deliverEmail`; the ticket email is the ONE exception to "await before answering": it is scheduled after the claim's response on purpose (the claim must never wait on mail), so its acceptance is recorded by the `[ticket-email] Resend accepted` log line, never by the client.
- The Resend API key is **send-only** — it cannot read send history, so past sends can't be audited through the API. Acceptance must be captured at send time: the `emailId` in the `/api/invites/send` response and the `[email] Resend accepted` server log line.

- **THE TICKET EMAIL (founder decisions 2026-09-15/16, second pass; `server/ticketEmail.js`, unit-tested verbatim):** the claim route schedules it with `setImmediate` AFTER `res.json` — for BOTH landing actions — through `deliverEmail` (the ONE dispatcher), from `RESEND_FROM_EMAIL`, reply-to the filmmaker's email (`users` by `films.creator_id`, best effort). Never blocks the claim, never surfaces a failure to the viewer (logged `[ticket-email] …`). Subject **`{ReceiverFirstName}, {SharerFirstName} gifted you a film`** (no receiver name → `{SharerFirstName} gifted you a film`). **It mirrors the LANDING PAGE's anatomy** on a plain `#080c18` ground (no background image), a 480px table, everything centred: (a) `BY PRIVATE INVITATION ONLY · TICKET NO. {n}` — 11px tracked caps `#9a9890` (the landing's eyebrow; the number segment drops when the row has none) · (b) ONE paragraph — **`{ReceiverFirstName}, {SharerFirstName} gifted you a film. Watch any time, no expiration.`** (no name → `{SharerFirstName} gifted you a film. Watch any time, no expiration.`) — Georgia italic 30px, 24px at ≤480px with 20px side padding through the template's media query (follow-up 3; nothing else between the eyebrow and the divider) · (d) the landing's divider, hairline — ✳ — hairline, accent at 50% (the blend baked in as `#5d574c`, email clients do not blend) · (e) `{FilmTitle}` — Georgia italic 28px · (f) the watch-page poster (`filmPosterUrl`), full width, 16:9, wrapped in a link to `/r/{token}` · (g) **the synopsis — the SAME field the landing renders under the title: `films.transmission_hook`** (`ClaimLanding.jsx` reads it as `transmissionHook` and renders `hook`; the server passes `film.transmission_hook`) — Georgia italic 17px · (h) the landing's clock icon (`public/email/clock@2x.png`, accent, 14px rendered) + `{RuntimeMinutes} MINUTES` in tracked caps `#b1a180`; **no "by {FilmmakerName}" anywhere** · (i) **`Watch for free`** → `/r/{token}` · (j) the wordmark (`public/email/deepcast-wordmark@2x.png`, 120px) and directly beneath it **`From broadcasting to deepcasting.`** (follow-up 4; was `Deep stories for deep souls.`, before that `Private. Trusted. Human.`) — 11px tracked caps `#9a9890`, the shared footer of BOTH templates built here (the legacy invite and sign-in emails in `server/index.js` keep their own "© deepcast — MVP v1.0" footer — not touched; the founder said EVERY email, so that is a queued follow-up). Every colour inline, a plain-text twin with the same words. The return token is minted AFTER the response (in the same deferred step as the send); if its columns are missing the button links to the bare slug and the log says so. Render: `node server/preview-email.js ticket` (the REAL builder → `server/email-preview.html`; `--base <url>` points the two image assets elsewhere, e.g. a local `file://` root). The preview's sample mirrors the REAL Circles row (title, synopsis, runtime, poster) with fictional names, so the fixture and the film cannot diverge.

- **THE REMINDERS — an in-process hourly sweep (founder decision 2026-09-16; replaces the 15th's GitHub-workflow + `REMINDER_SECRET` design, both deleted):** `server/index.js` runs `reminderSweepTick` every hour, first 60 s after boot (`setInterval`, timers `unref`'d), and SENDS only when `process.env.REMINDERS_LIVE === '1'` (`remindersLive`); otherwise it logs the counts (masked addresses). Rules in `server/reminderRules.js` (unit-tested): unwatched claims only (`status='claimed'`); a "Watch later" claim (`watch_later_at` set) gets **reminder 1 at ≥ 1 day** and **reminder 2 at ≥ 3 days**; a "now" claim gets reminder 2 only; each at most once (`reminder1_sent_at` / `reminder2_sent_at`, STAMPED FIRST by a conditional update, then sent — `sendReminderRow`, four outcomes tested; a rejected send clears the stamp for the next sweep); one email per row per sweep — 2 when due, else 1, never 1 after 2; nothing beyond 7 days after the claim; never a seeded ghost (the shared `isDemoGhostInvite`, both domains, plus the claimed address); never a `show_ghosts` film (A Sacred Pause's claimants get the ticket email but no reminder — recorded, not decided); oldest first, 50 per sweep; the sweep never overlaps itself. Each reminder RE-MINTS the return token (only its hash is stored, so the plaintext of the last email cannot be reused — the older link then reads `unknown` → the sign-in page); **if Resend rejects the reminder, the PREVIOUS token state is restored** (`readReturnTokenState` / `restoreReturnTokenState`), so a failed send never kills the person's only live link (red-team, 2026-09-16). The same template for reminders 1 and 2: subject **`{ReceiverFirstName}, watch the film {SharerFirstName} gifted you`** (no name → `Watch the film {SharerFirstName} gifted you`); the ticket email's eyebrow (a) at the very top (follow-up 4), then **`Hey {ReceiverFirstName}, just a friendly reminder that {SharerFirstName} gifted you a film.`** (the ticket headline's own Georgia italic 30px, 24px on phones; no name → `Just a friendly reminder that {SharerFirstName} gifted you a film.`), then the ticket email's (d)–(j) exactly — divider, title, poster link, synopsis, clock + minutes, `Watch for free`, wordmark + `Private. Trusted. Human.`. **`POST /api/admin/reminders/run`** stays for the owner (the `ADMIN_USER_ID` pin, verified session — so the verifier can read the would-send list from the founder's Chrome): default `?dry=1` lists the due rows, masked; `?dry=0` runs a sweep now, which still sends only under `REMINDERS_LIVE=1`; 503 with a plain message until the 20260916 migration is applied. Reply-to is the film's `creator_id` account — today the founder's for all three films (A Sacred Pause's caption reads "by Ien Chi", not Jon Bregel — founder to decide); a future film whose creator is another person makes their email the reply-to — a launch-day consent step. Render: `node server/preview-email.js reminder`. **Tier 3 for this batch:** the migration `supabase/migrations/20260916_invites_return_and_reminders.sql` (additive, nullable — the verifier may apply it on the founder's "approved" under the 16 September protocol amendment) and, on Render, `REMINDERS_LIVE=1` when the founder wants sends to begin (until then every sweep only logs) plus `APP_URL=https://deepcast.art` (the emailed links need the public site; the ticket email falls back to the request origin, the sweep refuses to send without it).

## Email attribution (founder decision 16–17 September 2026, branch `email-attribution`)

- **Every automated email is measurable by what it actually caused.** One append-only row per ACCEPTED send in `email_events` (`invite_id` → invites on delete cascade, `kind`, `sent_at`, `return_token_hash`, `arrived_at`), locked exactly like `comments` (RLS on, no policies, anon/authenticated grants revoked; migration `supabase/migrations/20260917_email_events_and_pass_it_on.sql`, additive, never run by the builder). The ONE dispatcher writes it: `deliverEmail(payload, { event })` records after — and only after — Resend accepted (`server/emailDelivery.js` `recordEvent`, `server/emailEvents.js` validates the row; kinds `ticket`, `reminder1`, `reminder2`, `pass_it_on`, `invite_letter`). Team and sign-in emails carry no invite and leave no row. The legacy invitation letter records a row with a null hash (its link is the invite token, not a return token, so it never "arrives").
- **Arrived** = that email's OWN `/r/` link was spent: `POST /api/invites/return` stamps `email_events.arrived_at` where the hash matches, after its conditional spend (`stampEmailArrival`). Every mint is a fresh random token, so a hash names exactly one email. **No open pixels, no Resend click-tracking, ever** — link rewriting and pixels are exactly what this audience distrusts; the first-party return link already tells the truth.
- **Watched** = `invites.watched_at`, the SERVER's clock: `POST /api/invites/mark-watched { inviteId }` (`server/markWatchedRules.js`, unit-tested): only a row whose status is `claimed` moves to `watched`; already watched answers 200 untouched; `created`, void, any other status or an unknown id is refused; a claim holding an account needs that account's verified session (401 / 403), and the no-session path exists ONLY for `claimed_by` null. `ClaimWatch.jsx` calls it at 70% and keeps the old anon status write only as the fallback when the route cannot be reached.
- **Numbers begin at ship; nothing is backfilled or estimated.** `src/lib/emailStats.js` (`computeEmailStats`, unit-tested) is the ONE computation: per film and kind — Sent · Arrived by this email · Watched after arriving (arrival AND `watched_at` at or after it) · Shared after (a non-void onward invite on the same film by `parent_invite_id` OR `sender_id` = the claimant's account, created at or after the send). Served by `GET /api/admin/email-stats` (the `ADMIN_USER_ID` pin, fails closed; 503 until the migration) and shown as the compact table on the creator dashboard film card (`src/components/EmailStatsTable.jsx` — headers exactly as above, a quiet "Counting since {first event date}" line, an empty line when no events).
- Every route survives the window before the migration exists: a missing table or column is logged and skipped (`isEmailEventsMissing`), never a failed send, never a failed watch.

## THE PASS-IT-ON EMAIL (founder decision 16 September 2026 — A DELIBERATE REVERSAL, recorded, not to be re-debated)

- **The reversal.** "Never an unrequested email to a recipient" (PROJECT-BRIEF standing decision) and the 16 September rejection of unasked reminders are deliberately REVERSED for this one email, by the founder, on 16 September 2026: a person who watched and never shared receives ONE email, once, ever, asking them to pass it on. Recorded here; do not re-debate.
- **Who** (`server/passItOnRules.js`, pure, unit-tested; the loader `server/passItOnSweep.js` is the ONE read path shared by the hourly tick, the owner's route and the read-only CLI `node server/pass-it-on-dry-run.js`): status in `WATCHED_STATUSES`, a `claimed_email` AND an account (`claimed_by`), not void, not a seeded ghost (both domains + `isDemoGhostInvite`), film not `show_ghosts`, holder not the film's creator nor role-unlimited (creator / team_member / team-linked — `isRoleUnlimitedSharer`), at least one invitation left on this film or unlimited (`filmTicketsRemaining`, the one computation; a missing wallet is the virtual 5), NO non-void onward invite on this film by `parent_invite_id` OR `sender_id`, `pass_it_on_sent_at` null, and ≥ 3 days since the LATEST of `watched_at`, `claimed_at`, `reminder1_sent_at`, `reminder2_sent_at` (a legacy row with no `watched_at` qualifies from its claim; nobody is nudged within 3 days of any other email). Oldest anchor first, 25 per sweep.
- **How:** the same hourly tick as the reminders, after them; stamp FIRST (`pass_it_on_sent_at`, conditional), then send through the one dispatcher (with its `pass_it_on` attribution event), a rejected send clears the stamp and restores the previous return token (`sendReminderRow` reused, `which = 'pass_it_on'`). Its OWN switch: sends only when `PASS_IT_ON_LIVE === '1'` (anything else = dry run, masked log); `REMINDERS_LIVE` does not open it. Owner route `POST /api/admin/pass-it-on/run` (`?dry=1` default: the would-send list, masked, plus every exclusion reason tallied; `?dry=0` runs a sweep, still gated by the switch). The first live run's audience on 17 September: nine rows — eight on Circles and The New Narrative №17 (the founder's own test address, deliberately not special-cased).
- **The email** (`buildPassItOnEmail`, `server/ticketEmail.js`, unit-tested verbatim; render `node server/preview-email.js pass-it-on [--hands "Ien Chi,Maya Ortiz"] [--base file:///…/public]`) — **founder's THIRD pass, 17 September (copy verbatim + the drawing fix), supersedes the first two:** eyebrow (a), then **`Hey {Receiver}, thanks for watching. Just a friendly reminder that if you don’t pass this story on, its journey will end with you.`** (no name → `Thanks for watching. Just a friendly reminder that …`; preheader = the opener), then — ONLY when the path holds three or more people counting the recipient (`chainHands` length ≥ 2; otherwise nothing renders and the Films line follows the opener directly) — **the recipient's path exactly as the rail draws it**: the SAME nodes from `src/lib/railPath.js` over `chainHands` (first names, the "{n} OTHERS" collapse, "(FILMMAKER)" caption, YOU, then always "?"), built for mail in `pathRows` as ONE fixed-layout table: row 1 holds every dot and every run in the same row (per node three cells, half-run · dot image · half-run; every cell height 8px, vertical-align middle; dot cells a fixed 12px width, run cells none, so fixed layout spaces the dot centres equally and the 1px runs sit on one line — measured in the render harness, `screenshots.local/_scratch/measure-path.mjs`: one y for every dot centre, equal x spacing at desktop and 360px, for a 3-hand path and a collapsed one), row 2 the labels each spanning their node's three cells, row 3 the caption; solid hairline between arrived people, dashed only into "?"; gold for YOU and "?" only; node glyphs are images `public/email/node-{hand,you,next}@2x.png` — no SVG, no flex, no positioning, no webfont — then ONE quiet line **`Films on Deepcast spread by private invite and real humans only. No algorithms.`**, the divider, the title, the poster (a link), **`You experienced this film because {Sharer} thought specifically of you. Who needs it next?`** ({Sharer} = the direct sharer's first name through `safeFirstName` — the row's `sender_name`, the filmmaker's own when he gifted it directly; never an email; no usable name → `someone`), the button **`Pass it on`** → `/r/{token}?pass=1`, the wordmark, `Private. Trusted. Human.`. The invitations-count line is gone (finite and unlimited alike). Subject **`{Receiver}, {FilmTitle} is waiting for you to pass it on`** (no name → `{FilmTitle} is waiting for you to pass it on`). The plain-text twin carries the rail's accessible sentence for the path (`How this reached you: IEN (filmmaker) → MAYA → YOU → ?` — that joined form is still PENDING the founder's stamp, as on the page). The receiver is the account's current name (the canonical-name rule), the typed name only when the account has none.
- **The lineage is ONE server computation** — `server/lineage.js` `buildLineage` (2026-09-17), extracted from the link route and read by BOTH `GET /api/invites/link/:slug` and the sweep's loader (`pathHands` in `server/passItOnSweep.js`, which selects the same film rows): never a second lineage query shape. The dry run's preview carries `path` per row.
- **The words live in ONE module** — `src/content/howFilmsTravel.js`: the page's three lines (byte-identical: `e2e/claim-landing.spec.js` and `e2e/creator-watch-page.spec.js` pin them) and, beside them, **`HOW_FILMS_TRAVEL_EMAIL`** — the inbox variant (founder, 17 September: "here" has no meaning in an inbox, so the email says "on Deepcast" and "this story"), so the page and the email each have exactly one source and neither can drift.
- **The arrival:** `/r/{token}?pass=1` → `ReturnLink.jsx` spends the token, writes the stash, and (because the reader has already watched — the landing would bounce a watched owner to the dashboard) opens `/watch/{slug}?pass=1` directly; `ClaimWatch.jsx` opens the pass-it-on modal by itself once the link payload is in.
- **Tier 3 for this batch:** the 20260917 migration (the verifier applies it before merge on the founder's "approved") and, on Render, `PASS_IT_ON_LIVE=1` when the founder wants the first live run.
- **THE FOUNDER'S SKIP (amendment, 17 September 2026):** `invites.pass_it_on_skipped_at` (same migration file) — a durable per-invite skip, set by the founder by SQL (Tier 3, his; no UI). A row with it set is excluded with the reason "skipped by the founder" (`passItOnRules.js`), listed under "excluded" in every dry run; the loader tolerates each 20260917 column being absent on its own (`OPTIONAL_COLUMNS`, one dropped per retry; a send still requires the stamp column). **Before go-live the founder sets it on Circles №4, №42, №43 and The New Narrative №17** (`update invites set pass_it_on_skipped_at = now() where id in (…)` on those four rows, after a SELECT shows them); **expected first live send = Circles №11, 14, 15, 30, 34 only** (five people). Until the skips are set, the dry run still reads nine.
- **Logs never print a full address (17 September):** the reminder and pass-it-on sweeps, the dispatcher's accepted / error / retry lines and the legacy send route all log through `maskEmail`. **Open item, deliberately left alone:** the dispatcher's retry can double-send when Resend accepted but the response timed out (one `email_events` row is written, after the final acceptance).
- **Go-live ORDER (red-team item 12, 17 September):** 1. the verifier applies the 20260917 migration; 2. the branch deploys; 3. the founder reads `POST /api/admin/pass-it-on/run?dry=1` (or the verifier runs `node server/pass-it-on-dry-run.js`) and confirms the list; 4. only then `PASS_IT_ON_LIVE=1` on Render. A deploy with the switch set and the migration absent only logs an error each hour (the sweep refuses to send when the columns are missing); a deploy with BOTH set sends the whole due list 60 s after boot. Because no row has `watched_at` yet, the first run anchors on `claimed_at` / the last reminder — someone who claimed weeks ago and watched yesterday is due today; accepted by the rule as written. Arriving through `/r/{token}?pass=1` signs that browser in as the claimant (the return link's in-band session), so opening The New Narrative №17's email on the founder's own browser replaces his creator session with the test account's.
- **Red-team record (17 September, branch `email-attribution`):** HIGH — the watch page's fallback write used to undo every refusal the mark-watched route makes (any thrown request → the old anon flip); fixed: the anon write survives ONLY for a transport failure (`TypeError` from fetch), a 401/403/409 is final. Also fixed: the holder's email is no longer selected by the sweep (nothing read it); the pre-migration button links to `/watch/{slug}?pass=1` (the landing would bounce a watched owner to the dashboard); `?pass=1` opens the modal once and is cleared on close (no reopen on reload); a refusal names no status; `isEmailEventsMissing` requires the "does not exist / schema cache" wording (a permission error is never mistaken for "not migrated"); the candidate window is 2,000 rows; the once-ever database filter and the `?pass=1` arrival are tested (`server/passItOnSweep.test.js`, `e2e/return-link.spec.js`). Accepted as-is: `email_events` rows cascade with an invite delete (derived data — the admin Remove preview lists invites, not their attribution rows); mark-watched answers 401 for an account-holding claim without a session and 404 for an unknown id (invite ids are UUIDs, not enumerable); no e2e crosses 70% on the claim-link page (the two playback specs that do run the legacy `/i/` page). Pre-existing, outside the diff: the reminder sweep's log line prints the full recipient address (the pass-it-on path masks); the dispatcher's retry can double-send when Resend accepted but the response timed out (one event row is still written).

## LEGACY: email-first invite flow (`/i/:token`) — PROTECTED, do not retire the acceptance path

Everything below this line about invite EMAILS and the send flow describes the LEGACY system. Invite links never expire, so already-sent email invites must keep working indefinitely: `/i/:token`, its status machinery (`pending→opened→watched→signed_up`), `buildInviteEmailHtml`/`PlainText`, and the resend routes stay live and untouched. CREATION of new email invites is retired from every viewer-facing surface (the viewer dashboard's email modal was DELETED with the V5 ship, 2026-07-21); the A5 remainder is the two `InviteForm.jsx` mounts on the creator-only Upload and Profile pages, and the legacy screening's pass-it-on letter (reachable only from old `/i/:token` invites).

## Invite email content & MUX GIF — GIF mechanism RETIRED (2026-07-22)

**The GIF preview mechanism is retired product-wide.** New films ship with NULL `gif_start`/`gif_end`, no GIF window is ever chosen for them, and no GIF pre-warming step exists anywhere (the claim prologue warms the watch route chunk, Mux preconnects, and the poster — never a GIF; the link-preview card is the one static site-wide og image in `index.html`). `gif_start`/`gif_end` are legacy columns kept only for the two original films' historical values. Never set GIF timecodes for a new film. Caveat that keeps this section alive: the legacy creator-only email path (`/api/invites/send` via the Upload/Profile `InviteForm` mounts, and the Network Map resend) still embeds a GIF when it runs — for a film with NULL gif fields it would render Mux's default opening-seconds window, one of the reasons legacy email invites must never be used for new films (see the Circles rules in the Films section).

(Delivery/throttling is the doctrine above; the rest of this section describes how the LEGACY email's content is built.)

- **The invite email is film-data-driven.** Synopsis = `films.description`; the animated preview GIF URL is built by `buildFilmGifUrl(film, filmId)` from `films.mux_playback_id` + `films.gif_start`/`gif_end`. The HTML body is assembled in `buildInviteEmailHtml` (`server/index.js`). Per-film content needs only the film row, not code — the A Sacred Pause italics/fps are the gated demo exception noted in the Films section.
- **`films.description` is HTML-escaped and its newlines are dropped.** It is inserted via `escapeHtml`, with no `\n`→`<br>` conversion, so any markup or line breaks in the data are lost. **Formatting must be done in the template, not the data** — wrap the already-escaped strings in tags we control; never unescape, never allow data-supplied HTML through.
- **GIF params:** requested at `width=380` (displayed at `520`), `fps=10` (`fps=15` for A Sacred Pause). `&start=`/`&end=` are appended only when `gif_start`/`gif_end` are set.
- **MUX's animated-GIF endpoint ignores `fit_mode`/`crop`** — letterbox bars baked into a source video CANNOT be removed via URL params. Fix the source video, not the URL.
- **`server/preview-email.js` is a SEPARATE, drifted copy** — it does NOT import the real `buildInviteEmailHtml`; it keeps its own copy that has already diverged (header label, personal-note block, paddings). It is an approximation for eyeballing layout only and does NOT reflect real email changes. To trust a change, render the real builder, not the preview.

## Invite send flow (multi-film) — LEGACY (see the claim-link section above; A5 retirement pending)

- **Sending is multi-film.** [UPDATED 2026-07-21: the creator dashboard's email surfaces are GONE (Piece A) and the viewer dashboard's email modal is DELETED (V5 ship) — the only surviving mounts are `InviteForm` on the creator-only Upload and Profile pages, POSTing `/api/invites/send` with their film's `filmId` as a prop. These retire with the A5 remainder.]
- **Creators are unlimited and may invite for any film they own** (the server requires `films.creator_id` to match the sender; a creator inviting to a film they don't own is rejected).
- **Send guards** (`/api/invites/send`): no invite to the film creator's own email (see Standing product rules / `server/shareRules.js`); **dedup is one invite per (film, email)** — a duplicate returns 409, and `InviteForm` also pre-checks it; there is **no test-email allowlist** on the send endpoint (allowlists live only in the maintenance scripts).
- **[SUPERSEDED 2026-07-06 — first-name-only design; surfaces retire with A5; columns stay dormant]** ~~Recipient first AND last name are required on every send path~~ — collected client-side and re-validated server-side (`/api/invites/send` rejects a blank/whitespace last name with 400). There are **three** client paths and all enforce it: the creator dashboard / Upload / Profile form (`src/components/InviteForm.jsx`), the screening **pass-it-on letter** (`src/pages/InviteScreening.jsx` `handleSendLetter` + `screening/DesktopPassItOn.jsx` / `screening/MobilePassItOn.jsx`), and the **viewer dashboard invite modal** (`src/pages/Dashboard.jsx` `handleSendModalInvite`). Add the field to ALL THREE if a fourth send surface ever appears.
- **The last name is stored separately, never in the first-name fields.** It goes in `invites.recipient_last_name` (and `users.last_name`); `invites.recipient_name` and `users.name` stay **first-name only**. The last name is read only when the invitee creates their account — `/api/invites/session` and `/api/invites/claim-account` copy it into the new `users.last_name` so the account holds the full name. [`users.last_name` write history: the claim-link flow wrote it 2026-07-31–08-06 (the claim form's full-name field, removed with the email-only form) and the dashboard editor wrote it 2026-07-31–08-06 (first-name-only since) — as of 2026-08-06 NO surface writes it; existing data stays untouched, and the column plus the server's full-name handling remain for compatibility. `invites.recipient_last_name` stays legacy-only.] **Every greeting and on-screen display stays first-name only** (the invite email is unchanged by this work — it still reads `recipient_name` / `sender_name`). Existing rows are grandfathered (`recipient_last_name` NULL, `users.last_name` empty) and behave exactly as before.
