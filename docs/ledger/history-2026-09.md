# Ledger — History: September 2026

> Moved verbatim out of CLAUDE.md on 2026-09-16 (the split). The CURRENT STATE header and the 2026-09-03 batch (landing copy reversal, film mode, network graph modal, creator shell, ghost removal, ship protocol and CI). Constellation batches are in constellation.md. Read by grep + the section you need, never whole.

## CURRENT STATE (updated 2026-09-03 — THE CREATOR DASHBOARD PASS, AND THE NETWORK IS FOUR GENERATIONS DEEP)

**The 2026-09-03 batch (six commits on `main`, founder-directed, every gate unmasked — see the flake ledger for the runs):**

- **The landing letter reads "{Receiver}, {Sharer} has gifted you a film." and its button "Watch for free"** — the founder's explicit reversal of the 07-31 "do not re-open" note (recorded as such in the copy ledger). Nothing else on the letter changed.
- **The filmmaker can open the REAL watch page for his own film** — "Watch page" on each film card → `/watch/film/{filmId}`, the same `ClaimWatch`, fed by `GET /api/films/:filmId/watch` on his verified session; both watch routes share one film-level payload builder. Details under FILM MODE in the claim-link section; the slug path is proven byte-identical by a recorded-baseline e2e.
- **"See network graph" per film replaced the Network map page** — the constellation in a modal (~~no-viewer layout mode + the map's `explore` prop~~ — since 2026-09-09 there is ONE layout and ONE renderer with no mode flags: hover/tap lights one person's lineage, solid = claimed / hollow = in flight, on every surface). `NetworkMap.jsx`, `/network`, and the sidebar link are gone; the known React-Compiler limitation went with them.
- **The creator dashboard's shell:** the Team members box is gone (UI only), the wordmark fits (it was unsized — the clipped "t"), the Profile link is gone, the sidebar reads Upload film · Set password · About · Sign out, and "Set password" lands on the form (root cause: the Profile page scrolled to the hash before the section existed). The viewer V5 dashboard is e2e-asserted unchanged.
- **Circles' 50 seeded ghosts: REMOVED 2026-09-03, founder-executed** (`server/remove-circles-ghosts.js`; the first execute aborted because the guard also checked `sender_email` — six first-ring ghosts carry the filmmaker as sender — fixed in `b525011` to check recipient and claimant only; backup at `~/deepcast-backups/2026-09-03-circles-ghosts/`; after-state verified independently: 33 rows, 0 ghosts); the seeder is retired. Read-only verification this day: exactly 50 ghost rows, none numbered, none claimed, none referenced by any real row or watch session; the film's real ledger is №1–№34 (33 real tickets, `ticket_seq` = 34 — the Films section has the verified breakdown). After-state re-verified at this reconciliation through `db-read.js`: 33 rows, 0 ghosts, 0 unnumbered, №2–№34, `ticket_seq` 34; per-film counts Circles 33 · A Sacred Pause 58 · The New Narrative 83 (the protocol's invariant baseline); the backup file holds 50 rows (`circles-ghost-invites-2026-09-03T02-30-18-433Z.json`).
- **The network kept growing on its own (2026-08-22 → 09-02, database-verified):** from 11 tickets to 33; SIX onward sharers now (Oliver, Yan, Arielle, Krist, Alexander, Charles), a four-generation chain (founder → Arielle → Krist → Alexander → five more), and Krist alone shared ten tickets. No protection-list change was needed.
- **THE EVENING: the ship protocol and CI (`a5f4721` … `e6342df`).** `docs/SHIP-PROTOCOL.md` is binding (see Git & deploy workflow) and Claude Code now pushes Tier 1 itself. CI shipped (`5dc8860`) and was made honest in two follow-ups (`3d6cbea`, `e6342df`) — the shared e2e harness, the Mux-beacon CORS root cause, the third-party-only error backstop, the reduced-motion pin, the environment probe (details in the Testing section). Run #3 is green on all four jobs. The verifier's own commits (`58fde44`, `c817a64`) corrected the backup note (Supabase PRO, daily backups) and recorded the "CI is a signal, not a lock — `main` stays unprotected" decision.
- **Working-tree note (report-only, deliberately uncommitted):** unchanged — `design-handoff/` and `e2e/scratch-safari-private-dashboard.spec.js` (6 tests; it still imports `@playwright/test` directly and is left alone). A full run now totals 237 (231 committed, including the 3 environment probes, + the scratch 6).
- **Observed, not changed (logged for a decision):** a claimant who has NEVER shared has no YOU on their constellation — `resolveViewerFocus` (`graphLayout.js`) finds the viewer by `recipient_email`, by token, or by the common parent of their SENT tickets, never by `claimed_by`; claim-link claims store no `recipient_email`. Pre-existing; the viewer dashboard was left untouched by this batch.
