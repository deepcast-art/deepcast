# Ledger — Films, real users and protection

> Moved verbatim out of CLAUDE.md on 2026-09-16 (the split). The three films, the real-user and film-level protection rulings, the A Sacred Pause demo stopgaps, and the test-film rule. Read before touching film rows, data scripts, deletions, or anything on Circles. Read by grep + the section you need, never whole.

## ⚠️ PRODUCTION HAS REAL USERS (June 2026)

> **RE-LIVE AS OF LAUNCH (2026-07-31 evening):** the platform has REAL users again. The 24 July "everything is test data" ruling still covers the old named users below and The New Narrative's test pile, but it does NOT apply to the launch-era real users: **Young Chi (`imyme2024@gmail.com`, the founder's mother — real ticket №36 on The New Narrative)** is in both protection lists as of tonight, and ~~Oliver's email joins the lists the moment he claims Faith Circle №2~~ *(that per-email step NEVER EXECUTED — see the 08-06 update below)*. The old queued "remove the stale protection lists" task is SUBSUMED: the lists now carry real protected users again — they are UPDATED (test-era entries pruned when the founder runs his cleanup), never blanket-deleted. The agent still never executes production deletions.

> **FILM-LEVEL PROTECTION (founder ruling 2026-08-06, standing law):** **Circles** (the film formerly "Faith Circle") is live and public — EVERY person associated with it, claimed accounts and unclaimed in-flight tickets alike, present and future, is a real protected user, and all test activity of any kind belongs to The New Narrative only. Enforced IN CODE via `PROTECTED_FILM_IDS` + `isProtectedFilm` in `server/deleteRules.js`: no person-delete may run ON a protected film, no unclaimed link belonging to one may be deleted, no delete on ANY film may remove the account of someone associated with one, and `reset-test-data.js` aborts outright if its delete-set touches one. This SUPERSEDES the per-email "add Oliver at first claim" mechanism — which never executed: Oliver claimed №2 on 2026-08-03 and Brian claimed №4 the same day, both unprotected until this rule shipped; that gap is why the film-level rule exists. Both are now ALSO named in `PROTECTED_EMAILS` (belt-and-braces; future claimants need no list entry). **LAUNCH-DAY DOCTRINE: adding a film's id to `PROTECTED_FILM_IDS` is a launch-day step for any future film going live.**

> *(Historical, 24 July 2026 ruling — recorded in `docs/PROJECT-BRIEF.md`: every account then existing, including the named users below, became deletable test data. The cleanup remains queued and FOUNDER-RUN.)*

**The database is no longer disposable.** Trace Bell (`contact@tracebelll.com`) is a real user — he must NEVER be deleted or modified destructively by any cleanup, reset, or migration, in any table. The filmmaker account (`filmmaker@gmail.com`) was always protected; Trace now is too.

- `server/reset-test-data.js` enforces this in code: real users live in its `PROTECTED_EMAILS` list and the script refuses to run if its allowlist ever includes one. **Every future data script must follow the same pattern** — explicit allowlists only (never pattern matches), real users in a hard-refusal guard, dry-run by default.
- Before ANY production deletion: SELECT and show the owner the exact rows first. No exceptions.
- The 50 seeded demo graph nodes (invites with `recipient_email LIKE '%@demo.invalid'`) are intentional and stay.

## Films (three films, August 2026)

The app is multi-film. There are currently three films:

- **"The New Narrative"** (`80df945a-6fb7-416b-ad73-3fab4b9cadf8`) — the REAL film, with real users and the real share graph. Covered by the real-users rule above. `show_ghosts` is false: its 50 seeded `…@demo.invalid` nodes appear on admin surfaces only.
- **"A Sacred Pause"** (`7c42093d-d5eb-4a38-a9fa-d28ca41d7b0f`, mux playback `6GMWj01CjP01Y1ee001Vd2qYqUPJtEOgUYz00nG02BYE9F9E`) — **PUBLIC-FACING** (since 2026-07-21; formerly the demo film), owned by the filmmaker (`filmmaker@gmail.com`), directed by Jon Bregel. Still carries its seeded ghost invites (recipients `…@demo-deepcast.invalid`); its `show_ghosts` flag is TRUE (owner-stated 2026-07-22), so those ghosts render on VIEWER surfaces too (constellation, journey counts, ticket rows) as ordinary indistinguishable nodes — they are still never ticket-numbered, and they always show on admin surfaces regardless. Its `description` and `gif_start=616` / `gif_end=624` remain set for the legacy invite email (legacy columns — see the GIF retirement note in the email section).
  - **Distinct from** the 50 `…@demo.invalid` seeded graph nodes on The New Narrative (real-users section above) — different film, different email domain. Do not conflate the two demo sets.
  - **Teardown script — NEVER RUN IT (owner decision 2026-07-21):** `server/teardown-demo-film.js` deletes this entire film (its invites, watch_sessions, then the film row). Its purpose EXPIRED when A Sacred Pause became the public-facing film — running it would destroy a live production film. It stays committed for the historical record only (dry-run by default, typed confirmation, protected-email guard including Jon), but there is no circumstance under which it should be executed.
- **"Circles"** (`6a9c0c79-24f6-427e-ba34-c113acf92d9f`; named "The Faith Dialogues" until 2026-07-24, "Faith Circle" until 2026-08-06 — the title lives in `films.title`, every surface data-driven, slugs never derived from it) — **LIVE AND FILM-LEVEL PROTECTED (see the ruling in the real-users header).** Launch history (2026-07-31): the founder minted and personally sent ticket №2 (`ticket-n2fcx`, recipient "Oliver") — the platform's first genuine human-to-human transmission. **Oliver claimed №2 on 2026-08-03, watched, and shared onward THREE times — the platform's first organic onward shares, unprompted** (see the ticket state below). **STANDING RULE: NO test claims, test links, or test data on this film ever again; NO deletions touching its real users (now enforced in code — film-level); NO change that affects an in-flight recipient's experience without founder approval in that session.** `show_ghosts` is **FALSE** (founder-executed 2026-07-31, database-verified): its ~50 seeded ghosts render on ADMIN surfaces only, exactly like The New Narrative's. The ghosts were seeded by `server/seed-faith-ghosts.js` (dry-run by default, `…fd##@demo-deepcast.invalid` recipients, never ticket-numbered; its playback-id identity check tracks the CURRENT video — currently the third cut's id). **The seeder is RETIRED (2026-09-03): it hard-refuses any film in `PROTECTED_FILM_IDS` before any other check — its old "already has ghosts" guard would otherwise have let it re-seed Circles once the ghosts are removed. It stays committed as the historical record and as the generator for a FUTURE, unprotected demo film.** The 50 ghosts' removal is the founder-run script `server/remove-circles-ghosts.js` (see the destructive-data rule and the queued task). `gif_start`/`gif_end` are NULL by design (the GIF mechanism is retired — see the email section). The `transmission_hook` is **FOUNDER-FINAL as stored in the database** (2026-07-31) — verbatim-protected like all founder copy.
  - **Video: the THIRD CUT (owner-approved production update 2026-08-22; database-verified live):** mux playback `QDUEUyF7WDjjsOtMfeVfqh6M2NVM02arzLHK3IJnwYC00`, mux asset `01FJ3LOuzSSuLU8XSgvaDY1AAqbUPqmmyPf73rq7q011E`, duration 1803.135633s (renders "30 minutes. Headphones recommended." — the line is data-driven from `films.duration_seconds` through the floor rule in `src/lib/runtime.js`, never hardcoded). The swap used a TRANSITION SAFETY NET: `filmStory.js` briefly keyed the story + poster pin under BOTH the outgoing and incoming playback ids so neither deploy order could strand the page; the old key was removed in a follow-up commit after the founder verified the swap live — future swaps should reuse this dual-key pattern. Prior videos, for the record: the SECOND recut (2026-08-06, later the same day as the first) was playback `wAGDrU3BiptEx7PfYESFTer00Q7FPasDrM01z0165L3fqA` / asset `XTSJfgen6d1cqgLqpn01WHyvqDnsIcxYXVxzA5Sbk8Hs` (1789.4553s, "29 minutes"); the FIRST recut (earlier on 2026-08-06) was playback `cy02v48fVuneae02gDZIkjsqfcTFP00tSLRd4owg1w8Y02U` / asset `V61qDORLQB9rMMPe01AcQgz4qpbVSph68IRdiSlB1Af4` (1781.155333s, also "29 minutes"); the 2026-07-24 video was playback `Kr00IsuqtWX301MCA2YX22gFm7IRrCfPiSwFeTcBlf8AY` / asset `fkEMaRi2hFGYRMX62puxehEOyJNr5ApWnWRyfCAy1NQ` (2042.2495s, "34 minutes"); the ORIGINAL video was playback `4HnHRG3NAf9YYR7V1fNs0143gGJnLUZ9F1umQuXsOaaQ` / asset `maKIC023p9tTzstYdjC7mbMPd6nAOA6iDacDBSm00uwss` (1983s).
  - **The watch-page PLAYER poster is a frame from the cut that is ACTUALLY PLAYING (founder decision 2026-09-09, branch `rail-onward`):** `POSTER_OVERRIDES` in `src/content/filmStory.js` pins Circles to the THIRD cut's Mux thumbnail at `time=5` (the founder chose t=5 on rendered frames) — `https://image.mux.com/QDUEUyF7…/thumbnail.png?time=5`. The pin is EXPLICIT (`CIRCLES_POSTER_SOURCE` + `CIRCLES_POSTER_TIME` are both named in the module), so a future playback-id swap re-keys the story entry but can never silently change the frame: the founder re-chooses the time on the new cut, or keeps this source. ~~STANDING DEPENDENCY — the poster is PINNED to the ORIGINAL video (owner direction 2026-07-24, held through four swaps); the original Mux asset (`maKIC023…` / playback `4HnHRG3N…`) must never be deleted~~ — that dependency is GONE as of 2026-09-09; nothing in the app references the original playback id any more (the retired ghost seeder's comment mentions it historically). The LANDING page's full-bleed still is `films.poster_url` — DATA the founder sets himself, untouched by code.
  - **Filmmaker story is FULLY REAL for this film — nothing TEMP remains** (`src/content/filmStory.js`, keyed by the CURRENT playback id — re-keyed 2026-08-22 with content byte-identical): real photo `/portrait-5.jpg` (the committed file is the owner's cropped ~186 KB version), Ien Chi, and since 2026-07-27 (commit 3ca71db) the founder's REAL director's statement (verbatim epigraph + four body paragraphs; founder-approved copy, never edit). The "Atlanta, Georgia" location was **founder-confirmed REAL on 2026-07-31** — it is no longer TEMP for this film (the other two films' locations remain TEMP).
  - **Claim-link flow ONLY.** The legacy email-invite and resend paths (`/api/invites/send`, `/api/invites/resend`, the `InviteForm` mounts on Upload/Profile, the Network Map resend button) must NEVER be used for this film: with NULL gif fields the email would embed a Mux default opening-seconds GIF, and the legacy screening's pre-film prologue (`src/pages/InviteScreening.jsx`, non-Sacred-Pause branch) shows copy written for The New Narrative.
  - **The five allowlisted test emails must NEVER be used on this film.** `server/reset-test-data.js` collects its delete-set by those emails across ALL films — a test claim or send here would be deleted on the next reset, leaving permanent ticket-number gaps. **Structurally enforced since 2026-08-06:** the script now selects `film_id` and hard-aborts if ANY collected row belongs to a protected film (verified by dry run: the five emails currently have ZERO rows on Circles).
  - **Ticket state as of 2026-07-31 — THE CLEAN REAL LEDGER (verified in the database, not narration):** the founder removed EVERY test ticket and test account on this film himself via admin Remove (the last batch — tickets through №57 and the `+test31/32/34…` accounts — on launch day; zero non-ghost invites and zero wallet rows remain, verified), and then **RESET `ticket_seq` to 1 by founder ruling.** The ruling's reasoning, recorded so it is never misread as a precedent against immutability: **the immutability law protects EXISTING tickets — with zero tickets existing, a counter reset renumbers nothing and breaks no law; it simply opens the film's real ledger.** State after the reset: filmmaker №1 (`creator_ticket_no`), `ticket_seq` = 1 — and the FIRST REAL SHARE then minted №2 the same evening. On a film with existing tickets a counter reset remains forbidden.
  - **Ticket state as of 2026-09-03 — THE REAL LEDGER, FOUR GENERATIONS DEEP (every row database-verified this day through `db-read.js`; supersedes the 2026-08-22 ledger, whose №1–№11 rows are unchanged within it):** 33 real tickets, №2–№34, `ticket_seq` = 34, no gaps, no voids. By status: 8 WATCHED (№2 Oliver, №4 Brian, №6 Yan, №11 Cal, №13 Krist, №14 Daniel, №15 Patti, №16 Alexander) · 5 claimed, not yet 70%-watched (№8 Arielle, №9 Marcus, №10 Joiselle, №12 Jan, №31 Charles) · 20 unclaimed in flight (№3 Steve, №5 Katie, №7 Christina, №17 Stacy, №18 Dalton, №19 Zeke, №20 Andrea, №21 Trey, №22 Monika, №23 Mark, №24 Mom, №25 Grace, №26 Rachael, №27 Brooks, №28 Taylor, №29 Themba, №30 Evan, №32 Jacob, №33 Evan, №34 Bianca). Who shared what (by `parent_invite_id`, verified): the FOUNDER sent 9 (№2, №6, №8, №9, №12 Jan, №29 Themba, №30 Evan, №31 Charles, №33 Evan) · Oliver → 3 (№3, №4, №5) · Yan → 1 (№7) · Arielle → 4 (№10 Joiselle, №11 Cal, №13 Krist, №34 Bianca) · **Krist → 10** (№14–№18, №24–№28 — the largest onward share by anyone, the founder included) · **Alexander → 5** (№19–№23) · Charles → 1 (№32). A FOUR-generation chain exists — founder → Arielle → Krist → Alexander → five more — and 24 of the 33 tickets were minted by someone other than the founder. Every claimant holds an account; all are film-level protected automatically (`PROTECTED_FILM_IDS`) — no `PROTECTED_EMAILS` entries were needed or added. The seeded ghosts are GONE: the founder executed `server/remove-circles-ghosts.js` on 2026-09-03 (after the `b525011` guard fix) — 50 rows deleted, JSON backup at `~/deepcast-backups/2026-09-03-circles-ghosts/`, independently verified afterwards by the Cowork verifier: 33 rows on the film, 0 ghost-domain rows, №2–№34 intact, 13 claimed, 8 watched, no dangling parents, The New Narrative (83) and A Sacred Pause (58) untouched.
- **Testing rule (owner decision 2026-07-20; hardened at launch 2026-07-31): ALL testing happens on "The New Narrative" — it is the designated test film. "A Sacred Pause" is public-facing and "Circles" is LIVE with real users and FILM-LEVEL PROTECTED: NEVER create test links, test claims, or any other test data on either.** The New Narrative now also carries ONE REAL USER — Young Chi, ticket №36 (`imyme2024@gmail.com`, claimed 2026-07-31) — protected in code; the queued test-pile cleanup must EXCLUDE her.

### A Sacred Pause demo stopgaps (tech debt — NOT general behaviour)

Three customizations are hardcoded behind `SACRED_PAUSE_FILM_ID === '7c42093d-…'` purely for the demo. Do NOT read them as general app behaviour:

- the custom pre-screening welcome message (`src/pages/InviteScreening.jsx`),
- the invite-email title + synopsis italics (`buildInviteEmailHtml`, `server/index.js`),
- the invite-email GIF at `fps=15` (`buildFilmGifUrl`, `server/index.js`).

These are per-film hardcodes pending the multi-film / editable-welcome work, where the surviving ones become per-film editable fields (editable welcome copy, synopsis). The GIF window/fps piece is NOT future work anymore — the GIF mechanism is retired (see the email section); the fps gate merely lingers until the legacy email path itself is removed. When the editable-welcome work lands, delete the `SACRED_PAUSE_FILM_ID` gates.

**Test discipline (standing):** The New Narrative is the designated test film; A Sacred Pause is public-facing — never create test links, claims, or data on it. **Localhost writes to PRODUCTION** — a test claim on localhost mints a real, permanent ticket number and a real account.
