# Ledger — The dashboards: viewer V5 and creator/admin

> Moved verbatim out of CLAUDE.md on 2026-09-16 (the split). The V5 viewer dashboard, the creator dashboard admin surfaces (film cards, sidebar, people table, ticket controls, Remove, popovers), dashboard behaviour, the canonical graph model, and the NetworkGraph component. Read before touching Dashboard.jsx, ViewerDashboardV5.jsx, or admin surfaces. Read by grep + the section you need, never whole.

3. **Dashboard `/dashboard`** (the payoff): the **V5 viewer dashboard** (`src/pages/ViewerDashboardV5.jsx`, LIVE since 2026-07-22; spec `design-refs/deepcast-dashboard-v5.html`) — ONE dashboard for every user, no tiers. Sidebar: name, gold "Ticket No. N", Tickets remaining/shared ("Tickets shared" since the 2026-07-25 vocabulary ruling), gold "Share this film" CTA (the quiet aside paragraph was REMOVED 2026-07-21, nothing replaces it), and ALL FIVE links for everyone, in this exact order (owner 2026-07-21): About Deepcast (opens the About POPUP — `AboutModal` → shared `AboutContent`; `/about` stays live for direct links) / Edit your name (label since 2026-08-06; was "Edit your full name" 2026-07-31–08-06, "Edit your first name" before) / Report a bug (mailto ien.chi96@gmail.com, subject "Deepcast bug report") / Contact / Sign out; pinned so they can never scroll out of sight; mobile = top-bar Menu overlay + fixed bottom share bar. Main: state-aware screening cards (`src/lib/screeningCard.js`); the journey line + CONSTELLATION (radial star map, `src/lib/constellationLayout.js` + `src/components/ConstellationMap.jsx` — **ONE GRAPH ON EVERY SURFACE (founder decision 2026-09-09): the viewer dashboard draws EXACTLY the drawing the creator's "See network graph" modal draws** — filmmaker at the exact center with name + "FILMMAKER" (the "Ticket No. 1" line is on the creator dashboard's film card, not on the map), the first ring EVEN around the full circle and every deeper generation a tight FAN at its parent's angle (the founder's 5 September 2026 reversal of the sunburst rule — see the constellation paragraph in "Dashboard behaviour"), every person the same small dot (solid = claimed, hollow = in flight) with its name radial at one size, no rotation for the viewer; **the one per-viewer difference is COLOUR: the viewer's own thread is gold in both directions — the path from the filmmaker to them and everything that grew from their own tickets, all generations — and YOU is marked by its label alone;** ~~gold path film→you→your invitees, dim twinkling web, hover lights the web gold~~ (the whole-web hover lighting is gone; hover/tap lights one person's lineage, exactly as in the modal), wheel/pinch zoom at the pointer + immediate drag; **label visibility is COLLISION-BASED since 2026-07-31 — founder principle, binding for this surface: the names ARE the product, a label hides only when it would physically collide with another, never by a blanket rule.** Every viewport: ~~gold-path names always render … dim-web names fill the remaining room closer-to-YOU-first~~ — since 2026-09-09 ONE collision rule for every name on every surface, the viewer's gold thread included (colour never buys a name room the modal would not give it — the builder's reading of the founder's "no stacked labels on phones", recorded as a supersession in the constellation paragraph under "Dashboard behaviour", pending his confirmation on the `-v2` phone render); the only always-on labels are the filmmaker's two center labels and YOU's marker (a collision among those is console-reported, never silently resolved); names appear progressively as zooming creates space, in the same order whoever is looking; every label counter-scales to a minimum readable on-screen size (9.5px since the v4 round, against the true scale), the filmmaker center-labels included (the same readability floor as the modal — on a phone the picture is the modal's picture scaled to the width, thinned by collisions, with the modal's zoom controls — and a viewer's phone opens on their thread, see the constellation paragraph under "Dashboard behaviour"). Rules + the knobs — `LABEL_SIZE_LADDER` (11 / 10.5 / 10 / 9.5 / 9 since 2026-09-11 — the size is chosen per film and per view, the largest rung at which every name paints; `MIN_LABEL_ON_SCREEN_PX` is the ladder's bottom, 9 — it was a fixed 11 on 2026-09-10, 10.5 under its evening amendment, 9.5 in the v4 round), `LABEL_GAP_PX` (6 since 2026-09-09, was 3: the renderer's on-screen hiding gap) and `LABEL_CLEARANCE` (6, the layout's HARD placement rule, 2026-09-09) — live in `src/lib/constellationLabels.js`, unit-tested; e2e proves sparse-shows-every-name and crowded-reveals-on-zoom on all three engines); "Tickets you've shared" rows (status + copy link; empty state "No tickets shared yet." since 2026-07-25). Solid `--color-bg-page` background — no grain; **since 2026-09-10 the screening cards and the map box sit on ink too ("one ground: ink", founder) with their hairlines.** Data rules live in shared libs only: `journeyLine.js`, `ticketRows.js`, `constellationLayout.js`, `inviteExistence.js`, `displayName.js`.

## Creator dashboard admin surfaces (Pieces A/A2/B/C/F — standing behavior)

- **Per film card (the action row right of the title, founder order 2026-09-03): Watch page · See network graph · Create an invitation · the status pill.** The card sits on `ink` with its hairline since 2026-09-10 ("one ground: ink" — `bg-card` is gone from it, from the team-member "Network impact" box and from the modal's map; the auth-form fields keep `bg-card` by scope; `--color-ink-2` stays for AboutModal/ShareLinkModal). "Watch page" (creator-only) opens `/watch/film/{filmId}` — the filmmaker's REAL watch page (see FILM MODE in the claim-link section). "See network graph" opens `NetworkGraphModal` (`src/components/`, native `<dialog>`, same behaviour contract as the pass-it-on modal): THE constellation for that film — the SAME drawing viewers see (founder decision 2026-09-09: one graph on every surface) — `includeGhosts` = the film's `show_ghosts`, voids nowhere, `displayName.js` names, collision-based labels, wheel/pinch zoom and drag. ~~In the layout's explicit NO-VIEWER mode (`noViewer: true` in `constellationLayout.js`: `viewerInviteId` ignored, no rotation, every person a web node, plus `claimed` per person and `fromId`/`toId` per edge, added ONLY in that mode — a golden snapshot asserts the viewer mode's output is byte-identical) and the map's `explore` prop~~ — since 2026-09-09 there are no modes: the one layout never rotates and stamps `claimed`/`fromId`/`toId` for everyone, and the one renderer (`ConstellationMap.jsx`) behaves the same everywhere: nothing lit at rest unless a viewer is looking (then their thread is gold), hover (mouse) or tap (press-and-release without movement, detected at the SVG because the drag handler holds pointer capture) lights ONE person's lineage gold — film → them → their entire downstream; solid dot = claimed, hollow = in flight; a lit person's label always renders. Caption = `buildJourneyLine` with the constellation's `inviteCount` (Y = 0). Empty state = the V5 "No tickets shared yet." "Create an invitation" → `CreatorLinkPanel` (`src/components/`, same create-link flow as the watch page's pass-it-on modal, session path). Below: the three-stat ticket funnel; the "People in this network" table. No email-send surfaces remain here. The old Network map page (`/network`, `NetworkMap.jsx`) and its sidebar link were DELETED 2026-09-03; the Profile page's film previews, which linked there, are plain frames now.
- **Sidebar (founder order 2026-09-03):** the stats block (Films / Tickets generated (all films) / Unlimited tickets), then Upload film · Set password · About · Sign out — nothing else. The Profile link is gone (`/profile` stays live). The "Team members" box (teammate email + name form) is gone from the top of the page — UI only: `/api/team/*`, `teamRules.js`, and existing teammates are intact; `loadTeamSection` still reads the members' ids for the people table's "∞" column. The wordmark is sized through `DeepcastLogo`'s `size` prop (`text-3xl` desktop sidebar, `text-xl` mobile bar): the old `h-7 w-auto` left the component's default `text-8xl` glyphs overflowing the 22% sidebar (the clipped final "t"). The viewer sidebar always passed a size and is unchanged.
- **Set password (root cause found 2026-09-03):** the link goes to `/profile#set-password` — the Profile page's own inline "Set your password" form (`updatePassword`; NO email is sent, so `resetPassword`, `VITE_PASSWORD_RESET_REDIRECT_URL`, and the Supabase redirect allowlist are not involved — no production config change was needed). The page's scroll-to-hash effect ran once at mount, while the page was still its loading spinner — the section only mounts after `loadData` finishes — so it found nothing and the founder was left at the top of the page at "Films you've watched". Fixed at the source: the effect waits for `loading` to finish, then scrolls to the section and focuses its first field (`e2e/creator-dashboard.spec.js`).
- **The people table** renders `buildNetworkPeople` rows (see Canonical stats): three statuses only, flat oldest-first, unclaimed links recoverable in-row (URL + "Copy the message").
- **Ticket controls** (`TicketControlsPopover`): the cell is quiet state text; the popover's top-up accumulates a pending amount and commits in ONE call; the unlimited slide toggle confirms inline; per-film since Piece F (film id travels with every call). Statuses arrive in ONE batched admin call PER FILM, deduped by content across reloads.
- **Remove** (`RemovePersonPopover`): quiet affordance → server preview (who re-points where, what deletes, account fate) → Delete gated on typing the email (click-confirm for unclaimed links). **The confirmation email renders as permanent visible text above the input** ("To confirm, type {email}", select-all enabled) — never placeholder-only, which vanished at the first keystroke (the "email disappears" defect, fixed 2026-07-24).
- **Popover bookkeeping (fixed 2026-07-24):** the open-popover key in `Dashboard.jsx` is **film-scoped — film AND person** — so a person claimed on two films opens exactly ONE popover (the old person-only key mounted stacked twins whose click-outside watchers dismissed each other; exposed by the two-film "Kanye" row). Both popovers are fixed-positioned, flip above the anchor near the viewport bottom, and **close only on real user scrolling — `wheel`/`touchmove` listeners, never the bare `scroll` event** — so the page's own layout shifts (image loads, scroll anchoring) can never dismiss them; the 300ms open-guard for the opening click's trailing events stays. E2E coverage in `e2e/remove-popover-email.spec.js`.
- Only the pinned owner ever sees controls — the admin endpoints 401/403 everyone else and the UI degrades to em dashes silently.

## Dashboard behaviour (viewer role — the V5 dashboard, LIVE 2026-07-22)

The old viewer diptych (sidebar stats + NetworkGraph + sent-invitations list, the mobile hamburger sidebar, the stash/claimant variant) is GONE. The V5 dashboard's full description lives in the claim-link section (page 3). Behavioural rules that carry over or replace the old ones:

- **Resume & return:** screening cards route by `link_slug` to `/watch/{slug}` (claim flow) or by token to `/i/{token}?play=1` (legacy), via React Router `navigate()`. Resume-position rules are unchanged (`src/lib/resumePosition.js`, slug-scoped keys, near-end saves ERASED); card state from `src/lib/screeningCard.js`.
- **Mobile:** top bar with a Menu overlay (starts closed, never auto-opens; entries in the fixed order About Deepcast (opens the About popup) / Edit your name / Report a bug / Contact / Sign out) and a fixed bottom "Share this film" bar. Desktop: the 18rem sidebar with the links block PINNED to its bottom edge (it can never scroll out of sight; e2e asserts on-screen boxes at 900px and 660px heights).

- **Share** = the link modal (`ShareLinkModal`), session-identified, same `create-link` flow as the watch page's pass-it-on modal. No email is ever entered on the dashboard.
- The `e2e/viewer-dashboard-v5.spec.js` mocked-session harness is the verification surface for this page (its mocks must expose `Content-Range` via CORS or ViewerShareGate bounces to /profile).

### Canonical graph model (`buildGraphLayout` in `src/lib/graphLayout.js`)
- The filmmaker IS the central film node — there is never a separate filmmaker user node. Any invite **sent by** the film's creator (`creatorId`) attaches its recipient directly to the central node, regardless of the stored `parent_invite_id` (self-healing against historical bad parents).
- Team members (unlimited-share users) render as their own ring-1 nodes (`type: 'member'`); their invitees attach beneath them. Pass `teamMemberIds` where available; senders that can't be placed are derived structurally.
- Everyone else chains to whoever shared with them: `parent_invite_id` first, then an email-match repair (the invite through which the sender's email received the film).
- Server-side, `/api/invites/send` never records a `parent_invite_id` for unlimited senders (creator/team) — this is what permanently prevents the phantom-intermediate-node bug.
- `/api/invites/validate` returns `creatorId` + `teamMemberIds` for the screening surfaces; the dashboard/profile (and, until its deletion 2026-09-03, the network map) pass `films.creator_id` from their own queries (films are publicly readable; users rows are NOT readable cross-role under RLS — use `maybeSingle()` for creator-name lookups).
- The central node displays the filmmaker's name with "(filmmaker)" beneath it: `buildGraphLayout` sets `creatorLabel` on the root node (caller-supplied `creatorName`, falling back to the `sender_name` on a creator-sent invite when RLS hides the users row), and `FilmNode` renders it below the camera icon on every surface.

## Network graph component (`src/components/NetworkGraph.jsx`)

- `focusNodeId` prop: when changed to a new value, the graph smoothly scrolls to centre that node in the pan container. Uses a `lastFocusedNodeId` ref so it only fires once per new value.
- Scroll math: `scale = graphPx.w * zoom / vbW`; offset from viewBox centre `(vbW/2, viewBoxH/2)` converted to scroll pixels, added to the centred scroll position.
