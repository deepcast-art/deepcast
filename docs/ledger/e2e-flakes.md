# Ledger — E2E flake ledger

> Moved verbatim out of CLAUDE.md on 2026-09-16 (the split). Every recorded e2e flake sighting and the widened investigation. Read when a suite fails on a documented-flake spec. Read by grep + the section you need, never whole.

- **E2E flakes (environment, not product):** the real-media resume specs fail intermittently under full-suite load (passes solo; webkit mostly, chromium under heavy load — through 2026-07-25's ~30 back-to-back runs a webkit auth-link spec flickered once too), and 9 legacy `/i/…` failures on 2026-07-24 passed in later runs — all reclassified as environment flake; one widened investigation is queued (see Parked). A unit-side data point joined 2026-07-27: the `server/emailDelivery.test.js` throttle-timing assertion failed once in a full `npm run test:unit` run (measured gap 20ms vs the ≥25ms floor) and passed solo and on rerun — same timing-under-load family. 2026-07-31: the webkit `auth-link-expired` spec fired twice in quick succession (one full-suite run + one file-level run just after it) then passed three consecutive file runs and the next full suite — same family. Launch evening, ~15 suites into the day: four consecutive full runs each failed exactly ONE documented-flake spec (webkit resume, chromium resume, webkit auth-link — alternating), every one passing solo; one solo resume failure occurred against a dev server that had lived through many suites and vanished on a fresh server (fresh-server solo runs green ×5) — the long-lived-server correlation is a new lead for the investigation. 2026-09-17 (branch `viewer-polish`, full three-engine run after the design-gate changes): the webkit `resume-skip-regression` "never skips to pass-it-on — playback heals to the start" case failed once (received 9.8s, expected < 6s), the other 374 passed; rerun solo on webkit with `--repeat-each=2`: 4 of 4 passed — same real-media-under-load family; nothing on the branch touches resume. 2026-09-17 again (branch `email-attribution`, full three-engine run): the same webkit `resume-skip-regression` case failed once (1 of 375), rerun solo on webkit with `--repeat-each=4`: 8 of 8 passed, exit 0 — logged BEFORE the push; nothing on the branch touches resume or the player. Third sighting the same day (the founder's skip amendment, full run): the same case, the same 9.8 s reading, 1 of 378; solo webkit `--repeat-each=4`: 8 of 8 passed, exit 0, logged before the push.

- **E2E flake investigation (WIDENED 2026-07-25; new data 2026-08-06)** — 9 legacy `/i/…` failures observed 2026-07-24 then passing unchanged, AND the full-suite load flicker seen through 2026-07-25's ~30 back-to-back runs: mostly the real-media resume specs (webkit, twice chromium), once the webkit auth-link spec — every one passing solo and full runs alternating green/red under load. One investigation covers both; do not treat a green run as the investigation. **2026-08-06 data points (FIVE full-suite single-spec failures across the day's branches, every one a documented-flake spec, every one passing solo, every branch ending on a fully green run):** webkit resume ×3 (`rename-circles`, `circles-protection-and-recon`, `simplify-name-editor`) and webkit auth-link ×2 (`circles-protection-and-recon` — on a FRESHLY started dev server — and `recon-aug-recut2`), so the launch-evening "long-lived server" correlation is a contributing factor at most, not the whole cause. Several same-day suites ran fully green first try (both `faith-circle-recut` runs, the first reconciliation's, `circles-recut-2`'s) — the flicker is intermittent, not constant. 2026-08-22: both full suites (the `circles-recut-3` and `circles-recut-3-cleanup` gates) ran fully green first try — 201 passed each (a count that includes the uncommitted scratch spec's 6 tests; see the working-tree note in CURRENT STATE). **2026-09-03 (six full suites across the day's phases):** Phase 1's first run had 3 real failures (a stale assertion, fixed) + the webkit resume flake; Phase 1's final run and Phase 2's run each failed exactly the two documented webkit specs (auth-link-expired, resume) — both passing solo immediately after; Phases 3 and 4 ran fully green first try (228 / 234 passed); Phase 5's run failed exactly the resume-skip spec — on FIREFOX (233 passed), the first time that spec has flickered on firefox rather than webkit/chromium — passing solo immediately after. Same family; the firefox sighting is a new data point for the investigation, not a new lead. **CI data (2026-09-03):** run #2's webkit resume-spec timeout (the prologue still on screen past 20s, 60s timeout) looked like the reduced-motion hold, but the runner's WebKit natively reports `prefersReducedMotion: false` (environment probe, run #3), so that hypothesis is NOT confirmed and the failure joins this ledger as the resume flake seen on a fresh runner; run #3 passed the spec with no retry.
