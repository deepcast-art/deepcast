# CLAUDE.md

## Project overview

Deepcast optimizes for depth over reach. It is an invite-only network where substantive films spread person to person through trusted relationships — each viewer receives a ticket from someone who thought of them, watches, and passes the film onward — toward deep impact and resonance rather than raw numbers. Deepcasting, not broadcasting. Depth is the heart of the platform; everything else — the tickets, the constellation, invite-only — exists to serve it.

## ⚠️ PRODUCTION HAS REAL USERS (June 2026)

> **RE-LIVE AS OF LAUNCH (2026-07-31 evening):** the platform has REAL users again. The 24 July "everything is test data" ruling still covers the old named users below and The New Narrative's test pile, but it does NOT apply to the launch-era real users: **Young Chi (`imyme2024@gmail.com`, the founder's mother — real ticket №36 on The New Narrative)** is in both protection lists as of tonight, and ~~Oliver's email joins the lists the moment he claims Faith Circle №2~~ *(that per-email step NEVER EXECUTED — see the 08-06 update below)*. The old queued "remove the stale protection lists" task is SUBSUMED: the lists now carry real protected users again — they are UPDATED (test-era entries pruned when the founder runs his cleanup), never blanket-deleted. The agent still never executes production deletions.

> **FILM-LEVEL PROTECTION (founder ruling 2026-08-06, standing law):** **Circles** (the film formerly "Faith Circle") is live and public — EVERY person associated with it, claimed accounts and unclaimed in-flight tickets alike, present and future, is a real protected user, and all test activity of any kind belongs to The New Narrative only. Enforced IN CODE via `PROTECTED_FILM_IDS` + `isProtectedFilm` in `server/deleteRules.js`: no person-delete may run ON a protected film, no unclaimed link belonging to one may be deleted, no delete on ANY film may remove the account of someone associated with one, and `reset-test-data.js` aborts outright if its delete-set touches one. This SUPERSEDES the per-email "add Oliver at first claim" mechanism — which never executed: Oliver claimed №2 on 2026-08-03 and Brian claimed №4 the same day, both unprotected until this rule shipped; that gap is why the film-level rule exists. Both are now ALSO named in `PROTECTED_EMAILS` (belt-and-braces; future claimants need no list entry). **LAUNCH-DAY DOCTRINE: adding a film's id to `PROTECTED_FILM_IDS` is a launch-day step for any future film going live.**

> *(Historical, 24 July 2026 ruling — recorded in `docs/PROJECT-BRIEF.md`: every account then existing, including the named users below, became deletable test data. The cleanup remains queued and FOUNDER-RUN.)*

**The database is no longer disposable.** Trace Bell (`contact@tracebelll.com`) is a real user — he must NEVER be deleted or modified destructively by any cleanup, reset, or migration, in any table. The filmmaker account (`filmmaker@gmail.com`) was always protected; Trace now is too.

- `server/reset-test-data.js` enforces this in code: real users live in its `PROTECTED_EMAILS` list and the script refuses to run if its allowlist ever includes one. **Every future data script must follow the same pattern** — explicit allowlists only (never pattern matches), real users in a hard-refusal guard, dry-run by default.
- Before ANY production deletion: SELECT and show the owner the exact rows first. No exceptions.
- The 50 seeded demo graph nodes (invites with `recipient_email LIKE '%@demo.invalid'`) are intentional and stay.

## Films (three films, August 2026)

The app is multi-film. There are currently three films:

- **"The New Narrative"** (`80df945a-6fb7-416b-ad73-3fab4b9cadf8`) — the REAL film, with real users and the real share graph. Covered by the real-users rule above. `show_ghosts` is false: its 50 seeded `…@demo.invalid` nodes appear on admin surfaces only.
- **"A Sacred Pause"** (`7c42093d-d5eb-4a38-a9fa-d28ca41d7b0f`, mux playback `6GMWj01CjP01Y1ee001Vd2qYqUPJtEOgUYz00nG02BYE9F9E`) — **PUBLIC-FACING** (since 2026-07-21; formerly the demo film), owned by the filmmaker (`filmmaker@gmail.com`), directed by Jon Bregel. Still carries its seeded ghost invites (recipients `…@demo-deepcast.invalid`); its `show_ghosts` flag is TRUE (owner-stated 2026-07-22), so those ghosts render on VIEWER surfaces too (constellation, journey counts, ticket rows) as ordinary indistinguishable nodes — they are still never ticket-numbered, and they always show on admin surfaces regardless. Its `description` and `gif_start=616` / `gif_end=624` remain set for the legacy invite email (legacy columns — see the GIF retirement note in the email section).
  - **Distinct from** the 50 `…@demo.invalid` seeded graph nodes on The New Narrative (real-users section above) — different film, different email domain. Do not conflate the two demo sets.
  - **Teardown script — NEVER RUN IT (owner decision 2026-07-21):** `server/teardown-demo-film.js` deletes this entire film (its invites, watch_sessions, then the film row). Its purpose EXPIRED when A Sacred Pause became the public-facing film — running it would destroy a live production film. It stays committed for the historical record only (dry-run by default, typed confirmation, protected-email guard including Jon), but there is no circumstance under which it should be executed.
- **"Circles"** (`6a9c0c79-24f6-427e-ba34-c113acf92d9f`; named "The Faith Dialogues" until 2026-07-24, "Faith Circle" until 2026-08-06 — the title lives in `films.title`, every surface data-driven, slugs never derived from it) — **LIVE AND FILM-LEVEL PROTECTED (see the ruling in the real-users header).** Launch history (2026-07-31): the founder minted and personally sent ticket №2 (`ticket-n2fcx`, recipient "Oliver") — the platform's first genuine human-to-human transmission. **Oliver claimed №2 on 2026-08-03, watched, and shared onward THREE times — the platform's first organic onward shares, unprompted** (see the ticket state below). **STANDING RULE: NO test claims, test links, or test data on this film ever again; NO deletions touching its real users (now enforced in code — film-level); NO change that affects an in-flight recipient's experience without founder approval in that session.** `show_ghosts` is **FALSE** (founder-executed 2026-07-31, database-verified): its ~50 seeded ghosts render on ADMIN surfaces only, exactly like The New Narrative's. The ghosts were seeded by `server/seed-faith-ghosts.js` (dry-run by default, idempotent — aborts if the film already has ghosts, owner-run only, `…fd##@demo-deepcast.invalid` recipients, never ticket-numbered; its playback-id identity check tracks the CURRENT video — re-pointed at every swap, currently the third cut's id). `gif_start`/`gif_end` are NULL by design (the GIF mechanism is retired — see the email section). The `transmission_hook` is **FOUNDER-FINAL as stored in the database** (2026-07-31) — verbatim-protected like all founder copy.
  - **Video: the THIRD CUT (owner-approved production update 2026-08-22; database-verified live):** mux playback `QDUEUyF7WDjjsOtMfeVfqh6M2NVM02arzLHK3IJnwYC00`, mux asset `01FJ3LOuzSSuLU8XSgvaDY1AAqbUPqmmyPf73rq7q011E`, duration 1803.135633s (renders "30 minutes. Headphones recommended." — the line is data-driven from `films.duration_seconds` through the floor rule in `src/lib/runtime.js`, never hardcoded). The swap used a TRANSITION SAFETY NET: `filmStory.js` briefly keyed the story + poster pin under BOTH the outgoing and incoming playback ids so neither deploy order could strand the page; the old key was removed in a follow-up commit after the founder verified the swap live — future swaps should reuse this dual-key pattern. Prior videos, for the record: the SECOND recut (2026-08-06, later the same day as the first) was playback `wAGDrU3BiptEx7PfYESFTer00Q7FPasDrM01z0165L3fqA` / asset `XTSJfgen6d1cqgLqpn01WHyvqDnsIcxYXVxzA5Sbk8Hs` (1789.4553s, "29 minutes"); the FIRST recut (earlier on 2026-08-06) was playback `cy02v48fVuneae02gDZIkjsqfcTFP00tSLRd4owg1w8Y02U` / asset `V61qDORLQB9rMMPe01AcQgz4qpbVSph68IRdiSlB1Af4` (1781.155333s, also "29 minutes"); the 2026-07-24 video was playback `Kr00IsuqtWX301MCA2YX22gFm7IRrCfPiSwFeTcBlf8AY` / asset `fkEMaRi2hFGYRMX62puxehEOyJNr5ApWnWRyfCAy1NQ` (2042.2495s, "34 minutes"); the ORIGINAL video was playback `4HnHRG3NAf9YYR7V1fNs0143gGJnLUZ9F1umQuXsOaaQ` / asset `maKIC023p9tTzstYdjC7mbMPd6nAOA6iDacDBSm00uwss` (1983s).
  - **STANDING DEPENDENCY — the watch-page poster is PINNED to the ORIGINAL video:** `POSTER_OVERRIDES` in `src/content/filmStory.js` points at the original playback id's Mux thumbnail (owner direction 2026-07-24; deliberately unchanged through ALL FOUR swaps — each swap re-keys the entry to the new playback id with the pinned URL byte-identical). The original Mux asset (`maKIC023…` / playback `4HnHRG3N…`) must NEVER be deleted from the Mux account, or the poster 404s.
  - **Filmmaker story is FULLY REAL for this film — nothing TEMP remains** (`src/content/filmStory.js`, keyed by the CURRENT playback id — re-keyed 2026-08-22 with content byte-identical): real photo `/portrait-5.jpg` (the committed file is the owner's cropped ~186 KB version), Ien Chi, and since 2026-07-27 (commit 3ca71db) the founder's REAL director's statement (verbatim epigraph + four body paragraphs; founder-approved copy, never edit). The "Atlanta, Georgia" location was **founder-confirmed REAL on 2026-07-31** — it is no longer TEMP for this film (the other two films' locations remain TEMP).
  - **Claim-link flow ONLY.** The legacy email-invite and resend paths (`/api/invites/send`, `/api/invites/resend`, the `InviteForm` mounts on Upload/Profile, the Network Map resend button) must NEVER be used for this film: with NULL gif fields the email would embed a Mux default opening-seconds GIF, and the legacy screening's pre-film prologue (`src/pages/InviteScreening.jsx`, non-Sacred-Pause branch) shows copy written for The New Narrative.
  - **The five allowlisted test emails must NEVER be used on this film.** `server/reset-test-data.js` collects its delete-set by those emails across ALL films — a test claim or send here would be deleted on the next reset, leaving permanent ticket-number gaps. **Structurally enforced since 2026-08-06:** the script now selects `film_id` and hard-aborts if ANY collected row belongs to a protected film (verified by dry run: the five emails currently have ZERO rows on Circles).
  - **Ticket state as of 2026-07-31 — THE CLEAN REAL LEDGER (verified in the database, not narration):** the founder removed EVERY test ticket and test account on this film himself via admin Remove (the last batch — tickets through №57 and the `+test31/32/34…` accounts — on launch day; zero non-ghost invites and zero wallet rows remain, verified), and then **RESET `ticket_seq` to 1 by founder ruling.** The ruling's reasoning, recorded so it is never misread as a precedent against immutability: **the immutability law protects EXISTING tickets — with zero tickets existing, a counter reset renumbers nothing and breaks no law; it simply opens the film's real ledger.** State after the reset: filmmaker №1 (`creator_ticket_no`), `ticket_seq` = 1 — and the FIRST REAL SHARE then minted №2 the same evening. On a film with existing tickets a counter reset remains forbidden.
  - **Ticket state as of 2026-08-22 — THE REAL LEDGER, THREE ORGANIC BRANCHES (every row database-verified this day; supersedes the 2026-08-06 ledger, whose №1–№5 rows are unchanged within it):** filmmaker №1 · **№2 Oliver** (`ticket-n2fcx`, `oliver@marionecological.com`, sent by the founder 07-31, claimed 2026-08-03, WATCHED, has account) · **№3 Steve** (`ticket-wbct3`, unclaimed in flight — shared by OLIVER) · **№4 Brian** (`ticket-ceq3j`, `bmahan@uchicago.edu`, claimed 2026-08-03, WATCHED, has account — shared by OLIVER) · **№5 Katie** (`ticket-hgafg`, unclaimed in flight — shared by OLIVER) · **№6 Yan** (`ticket-rgvse`, `yantkach1@gmail.com`, sent by the founder, claimed 2026-08-08, WATCHED, has account) · **№7 Christina** (`ticket-8sye5`, unclaimed in flight — shared by YAN, the SECOND organic onward sharer) · **№8 Arielle** (`ticket-rqgnn`, `arielle@goshenglobal.co`, sent by the founder, claimed 2026-08-09, status claimed — not yet 70%-watched, has account) · **№9 Marcus** (`ticket-9q9e3`, `metavzn1@gmail.com`, sent by the founder, claimed 2026-08-09, status claimed, has account) · **№10 Joiselle** (`ticket-z2wwf`, `dr.joiselle@goshenglobal.co`, claimed 2026-08-17, status claimed, has account — shared by ARIELLE) · **№11 Cal** (`ticket-svh6p`, `ckent.ong@gmail.com`, claimed 2026-08-14, WATCHED, has account — shared by ARIELLE). `ticket_seq` = 11, no gaps, no voids. Three organic onward branches confirmed in the data by `sender_id`/`parent_invite_id`: founder → Oliver → {Steve, Brian, Katie} (2026-08-03, the first) · founder → Yan → {Christina} (the second) · founder → **Arielle → {Joiselle, Cal}** (the third — notable: Arielle shared onward twice while herself below the 70% watched bar). Every claimant holds an account; all are film-level protected automatically (`PROTECTED_FILM_IDS`) — per doctrine, no new `PROTECTED_EMAILS` entries were needed or added. The ~50 seeded ghosts (never numbered) are unchanged and admin-only (show_ghosts false).
- **Testing rule (owner decision 2026-07-20; hardened at launch 2026-07-31): ALL testing happens on "The New Narrative" — it is the designated test film. "A Sacred Pause" is public-facing and "Circles" is LIVE with real users and FILM-LEVEL PROTECTED: NEVER create test links, test claims, or any other test data on either.** The New Narrative now also carries ONE REAL USER — Young Chi, ticket №36 (`imyme2024@gmail.com`, claimed 2026-07-31) — protected in code; the queued test-pile cleanup must EXCLUDE her.

### A Sacred Pause demo stopgaps (tech debt — NOT general behaviour)

Three customizations are hardcoded behind `SACRED_PAUSE_FILM_ID === '7c42093d-…'` purely for the demo. Do NOT read them as general app behaviour:

- the custom pre-screening welcome message (`src/pages/InviteScreening.jsx`),
- the invite-email title + synopsis italics (`buildInviteEmailHtml`, `server/index.js`),
- the invite-email GIF at `fps=15` (`buildFilmGifUrl`, `server/index.js`).

These are per-film hardcodes pending the multi-film / editable-welcome work, where the surviving ones become per-film editable fields (editable welcome copy, synopsis). The GIF window/fps piece is NOT future work anymore — the GIF mechanism is retired (see the email section); the fps gate merely lingers until the legacy email path itself is removed. When the editable-welcome work lands, delete the `SACRED_PAUSE_FILM_ID` gates.

## Platform principles (use these when writing any user-facing copy or making product decisions)

- Deepcast is private, human-to-human film sharing. No algorithms, no feed, no AI curation. Films travel person to person, by invitation only — "the modern-day version of storytelling around the fire."
- The core idea: depth over reach — going from broadcasting to deepcasting. Millions of views mean nothing without resonance; what matters is substantive films reaching people through deep relationships and trust built person to person, spreading through trusted networks toward deep impact. We optimize for humanity and substance, never for attention, virality, extraction, polarization, or spectacle. Depth is the heart; tickets, the constellation, and invite-only all exist to serve it.
- Origin: the founder (Ien Chi) is a lifelong filmmaker; at Jubilee he helped grow a YouTube channel to 5M subscribers / 1B+ views and watched platform incentives push everything toward clickbait. Deepcast is the counter-bet — preserving real human curation and connection in the age of AI slop.
- Deepcast is NOT: a gatekeeper, a content factory, a quantity play, or an attention business.
- It's for people who live for higher ideals — artists, creators, mission-driven people — and for filmmakers making bold, substantive work that might not survive gatekeepers: "ideas like mustard seeds" that grow once championed.
- The network graph exists so people FEEL their impact: connected to everyone who led a film to them, and everyone who'll receive it because of them. A movement, not a passive audience.
- A share is a gift — chosen for one specific person (their values, their time of life), not broadcast to everyone.
- Long-term vision: the home for substantive filmmakers and audiences; a place where great IP is born.
- This is an early MVP built by Ien. We actively want filmmakers, collaborators, advisors, investors, engineers with taste, designers, curators, and community builders to reach out: ien.chi96@gmail.com.

## Voice and copy rules

- All user-facing copy is written in Ien's founder voice: personal, heartfelt, direct, human. Never corporate, never marketing-speak.
- It speaks to the heart, not the mind — but stays simple, crystal clear, and concise. Short sharp answers over long explanations.
- Copy approved by Ien is verbatim-only. Never rewrite, expand, or "improve" approved copy without his explicit approval. The current About copy (founder-approved 2026-07-21, three sections: "What is Deepcast?" / "Who is it for?" / "Who made this?") lives in ONE place — `src/components/AboutContent.jsx` — rendered by both the `/about` page and the dashboard's About popup so the two can never drift.

## Tech stack

- **Frontend:** React 19 + Vite 7, Tailwind CSS 4, React Router 7
- **Backend:** Express 5 (`server/index.js`), Supabase (auth + Postgres), Mux (video), Resend (email)
- **Deployment:** Vercel (frontend) + Render (API at `deepcast-virginia.onrender.com` — the Virginia service, live since 2026-07-23; `vercel.json` rewrites `/api/*` to it). The old Oregon service (`deepcast.onrender.com`) is **SUSPENDED** (founder-executed 2026-08-06 after a read-only audit confirmed nothing points at it; verified answering 503 — config preserved, resumable in the Render dashboard for a rollback only).

## Project structure

```
deepcast/
├── server/index.js          # Express API (Mux, Resend, Supabase service-role)
├── src/
│   ├── App.jsx              # Routes + auth guards
│   ├── pages/               # Page components (Dashboard, InviteScreening, Login, etc.)
│   ├── pages/screening/     # Desktop/Mobile screening sub-views
│   ├── components/          # Shared components (DeepcastLogo, FilmForm, NetworkGraph, etc.)
│   ├── lib/                 # Utilities (api.js, auth.jsx, supabase.js, graphLayout.js, etc.)
│   ├── styles/              # Branding tokens CSS
│   ├── index.css            # Design tokens + .dc-* utilities
│   └── fonts.css            # Font-face declarations
├── public/                  # Static assets (logo, fonts, vite.svg)
├── e2e/                     # Playwright smoke tests
├── supabase/migrations/     # SQL migrations
├── scripts/                 # Utility scripts (DNS check, DB reset)
└── docs/                    # Internal docs (invite flow, staging deploy)
```

## Key commands

```bash
npm run dev              # Start Express API + Vite dev server
npm run dev:client       # Vite only (no API)
npm run dev:server       # Express only
npm run build            # Run unit tests + Vite build
npm run lint             # ESLint
npm run test:unit        # Vitest (fast, no browser)
npm run test:e2e         # Playwright smoke tests (starts dev server automatically)
npm test                 # Unit + E2E
```

## Git & deploy workflow

- **Work directly on `main`. COMMIT ONLY — never push.** The owner pushes manually; production auto-deploys from `main` (Vercel frontend, Render API).
- One commit per phase/feature, with a plain-English commit message.
- The owner is **non-technical**: final reports, commit summaries, and anything they will read must be plain English, not jargon.

## Testing

- **Unit tests:** `*.test.js` colocated with modules, uses Vitest. Run with `npm run test:unit`.
- **E2E tests:** `e2e/*.spec.js`, uses Playwright on **all three browser engines** — chromium, webkit (Safari), firefox. Run with `npm run test:e2e`.
  - **Every commit that touches user-facing flows must have a green e2e run on all three engines** (or on every engine that can be installed, with the gap named in the final report).
  - **Playwright's browser installer hangs on this machine** (downloads reach 100%, then its extractor stalls at ~15 MB — do NOT keep retrying `npx playwright install`). Manual install works instantly: download the build zip with `curl` from `https://cdn.playwright.dev` (paths/revisions in `node_modules/playwright-core/browsers.json` + `lib/server/registry/index.js`), `unzip` it into `~/Library/Caches/ms-playwright/<browser>-<revision>/`, then `touch INSTALLATION_COMPLETE` in that directory. Also beware: a partial `npx playwright install` run may garbage-collect existing browser builds it thinks are stale.
  - `npx playwright test --config playwright.local.config.js` (local-only file, not committed) is the fallback that launches full Chromium instead of headless-shell.
- Build includes unit tests: `npm run build` runs `vitest run && vite build`.
- Local dev: Vite on port **3000**, Express API on port **3001** (Vite proxies `/api/*` to 3001). `npm run dev` starts both.
- **Fresh manual-test links:** `node server/reset-test-data.js` (dry-run BY DEFAULT since 2026-07-17; `--execute` + typed phrase to write) deletes ONLY the allowlisted test emails' data and mints five fresh, unopened filmmaker invites — one per allowlisted email. **Its delete-set is collected by those emails across ALL films** (not film-scoped), which is why the five test emails must never touch Circles or A Sacred Pause — and since 2026-08-06 it hard-aborts if any collected row belongs to a protected film (`PROTECTED_FILM_IDS`). These are the five standard scenarios used to manually walk the invite → watch → pass-it-on → dashboard journey from five separate identities (including the already-signed-in relink case and the R5 no-relink case).
- **Email rendering:** `node server/preview-email.js` writes `server/email-preview.html` to inspect the invite email without sending anything.
- **Read-only database inspection:** ALL read-only inspection (checking, comparing, verifying data) must go through `node server/db-read.js "select ..."` — never the Supabase MCP connection — so the owner is only ever prompted for genuine database WRITES. The script rejects anything that isn't a single SELECT / WITH...SELECT at the code level (tested in `server/db-read.test.js`), and the backing `db_read` Postgres function runs in a READ ONLY transaction as a second layer.
- Setup for new clone: `npm install && npx playwright install chromium webkit firefox` (on this machine, use the manual curl+unzip install above instead).

## Standing doctrine (every session)

- **End-of-day reconciliation (founder rule, 2026-07-24):** at the end of every working day, CLAUDE.md and `docs/PROJECT-BRIEF.md` are BOTH reconciled against shipped reality and committed — every claim verified against the code, never against anyone's narration, including the founder's. CLAUDE.md stays the IMPLEMENTATION layer (how to build safely); PROJECT-BRIEF.md stays the STRATEGY layer (what was decided, why, and the state of the venture) — new content goes to whichever file matches its reader.
- **Plan before editing.** Read the relevant files and write a short plan (files per phase, order, risks) before changing anything.
- **Diagnose root cause before fixing.** Never patch a symptom; explain the cause, then fix it at the source.
- **One commit per phase.** After each phase run unit tests, `npm run build`, and the e2e suite, and fix any regression *before* committing.
- **Test/build gates run UNMASKED, with exit codes checked (founder doctrine, 2026-07-31).** Never pipe a gate command's output through `tail`, `grep`, or any filter that can swallow its exit code or truncate its failure summary — a pipeline's exit code is the LAST command's, so `npm run build | tail` reports success even when the build failed. Capture full output (e.g. to a scratch file), read the REAL summary lines, and verify the command's own exit status. Reports must quote the actual summary lines, never paraphrase them. (Incident, 2026-07-31: a unit test written in fc71c53 never passed — every local gate through 6187d08 piped build output through `tail`, masking both the failure text and the exit code, so "unit green / build green" was reported unverified across two sessions. Vercel's deploy gate caught it; fixed in be75ea0.)
- **Never trust comments over code.** Verify behaviour in the code itself; comments may be stale.
- The screening page mounts desktop AND mobile sub-views simultaneously (src/pages/screening/DesktopPassItOn.jsx, MobilePassItOn.jsx) with unused variants hidden — the same copy can exist in 3+ styled variants at once. When matching or measuring styles, always identify the variant actually visible at the target viewport by rendering the live app, never by searching the code alone.
- **Destructive-data rule:** any script that writes to or deletes production data must default to dry-run and require an owner-run `--execute` (with typed confirmation) — never execute such an operation yourself. Every data script now complies: `reset-test-data.js`, `teardown-demo-film.js`, `backfill-claimant-accounts.js`, `backfill-film-tickets.js`, `seed-faith-ghosts.js`. Admin deletions in-app go through the delete-with-splice preview/typed-confirm flow instead.
- **Prefer single simple commands** over compound shell chains (`;`, `&&`, `|`) when feasible, so permission prompts stay rare.
- **Before ending any session, check whether your preview tooling started any duplicate dev servers, and kill them.** (Preview launchers inject `PORT=3000` into `npm run dev`, which put a second Express API on Vite's port on 2026-07-21 — localhost share links then 404'd with "Cannot GET /{slug}". `server/apiPort.js` refuses that port now, but stray processes still need cleanup.)
- **Prefer allowlisted read-only routes over approval-prompting tools.** For any read-only action use what's already allowed: `node server/db-read.js` for database reads, `grep`/`cat`/`git diff` for code, `npx eslint` for lint, and the allowlisted read-only MCP tools (Supabase/Vercel `list_*`/`get_*`/`search_docs` in `.claude/settings.local.json`) for infra inspection. Never reach for a tool that can write when a read-only route answers the question.
- **Never print `.env` contents or any secret values into chat. Reference secrets by variable name only.** Checking which env vars exist or how a service connects never requires displaying their values — grep for the variable *name*, don't `cat`/`Read` the file. This applies to every secret (service-role keys, API keys, encryption secrets), not just passwords. (Incident: full `.env` contents, including `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, `MUX_TOKEN_SECRET`, and `INVITE_CTX_SECRET`, were printed to a chat transcript on 2026-07-06 while investigating a database connection — see `deepcast-mvp-rework.md` E7.)
- **Pre-existing lint never blocks work.** Verify a lint issue pre-dates your changes (e.g. lint the file at HEAD), report it in the final summary, and move on — fix it only if asked.
- **All browser storage access goes through `src/lib/safeStorage.js`** (`safeLocalStorage` / `safeSessionStorage`) — never raw `localStorage`/`sessionStorage` calls, never at module scope, in render, or mid-handler. Safari can block storage entirely (SecurityError on access) or fail every write (private-mode QuotaExceededError); the helper feature-detects per call and falls back to in-memory state for the visit, so a storage failure can never crash a screen or change what the user sees.
- **New code that touches storage must include a restricted-storage test:** unit coverage against the missing / access-throws / write-throws modes (see `src/lib/safeStorage.test.js`) and, for user-visible flows, an e2e case in `e2e/storage-restricted.spec.js` (which runs both Safari restriction modes on all three engines).

## Security doctrine — privileged endpoints

- **The verified-session pattern is REQUIRED for every privileged endpoint:** read the `Authorization: Bearer` token, verify it cryptographically with `supabase.auth.getUser(jwt)`, and take the caller's identity ONLY from the verified token. **Never trust a client-sent user/creator ID** — that was the old `/api/team/remove-member` hole, closed June 2026. Reference implementations: `/api/invites/relink`, `/api/team/remove-member`, and the `/api/admin/*` endpoints.
- Authorization decisions live in small unit-tested modules (`server/adminAuth.js`, `server/teamRules.js`, `server/deleteRules.js`) so every rejection path has a test. Delete-with-splice hard-refuses: protected real users, **protected FILMS (2026-08-06 — any person-delete on the film, any of its unclaimed links, and any plan that would delete the account of someone associated with one)**, creators (by role AND film ownership — the films CASCADE landmine), team members, and the caller's own account; execute re-verifies everything independent of preview (both routes call the one shared resolver, which carries the film-level params).
- **The sign-in-link endpoint answers identically whether or not an account exists — keep it that way.** A different answer for unknown emails would let anyone probe which addresses have Deepcast accounts. Unknown emails get the same neutral "check your inbox" response and NO account is ever created (sign-in links are generated only for existing accounts; signup happens exclusively through the invite-acceptance flow). Do not "helpfully" add an explicit refusal for unknown emails.
- **Owner-only admin endpoints** (`/api/admin/ticket-controls` + `/status`, `/api/admin/delete-person` + `/preview`) are pinned to the `ADMIN_USER_ID` env var — an exact user-ID match against Ien's account, NOT a role check (a hypothetical second creator must be rejected; role is belt-and-suspenders only). They **fail closed**: when `ADMIN_USER_ID` is unset, everyone gets 503, including a valid creator session. The variable must be set in Render's environment and local `.env`.

## Email-sending doctrine

- **Every outgoing email goes through the one dispatcher** (`deliverEmail` in `server/index.js`, built on `server/emailDelivery.js`): sends are strictly sequential and throttled below Resend's rate limit, and each send is automatically retried with backoff before giving up. Never call Resend directly from a route.
- **Acceptance is verified, per recipient.** The dispatcher resolves only once Resend confirmed it accepted the email. `/api/invites/send` awaits this before answering; on permanent failure it rolls back the invite row and the allocation and returns an error, so a retry starts clean (not blocked by the duplicate-invite check).
- **The UI never claims success for a recipient whose email was not confirmed accepted.** Multi-recipient sends report per-recipient truth: failures are shown clearly and the failed recipients stay in the form for retry (`handleSendLetter` in `InviteScreening.jsx`, `InviteForm.jsx`). `server/emailDelivery.test.js` proves the throttling, the retry, and the honest-failure behaviour.
- The Resend API key is **send-only** — it cannot read send history, so past sends can't be audited through the API. Acceptance must be captured at send time: the `emailId` in the `/api/invites/send` response and the `[email] Resend accepted` server log line.

## THE CLAIM-LINK FLOW (CURRENT — how films travel now, July 2026)

Invites are LINKS, not emails. A sharer enters only the invitee's first name and gets a link; the invitee claims it with their email alone (email-only form since 2026-08-06) and watches immediately (the claim creates their account — see the claim route). Recon + full design history: `PLAN.md` and `deepcast-mvp-rework.md` (its decisions log is binding).

### The three pages (one job each; the network idea at three intensities)

1. **Landing `/{slug}`** (`src/pages/ClaimLanding.jsx`, public, KEEP LAST in `src/App.jsx` routes): the letter over a full-bleed film still (`films.poster_url`, falling back to the film's Mux poster frame, then the dark bg; the img needs its inline height — the global `img{height:auto}` in `src/index.css` defeats Tailwind height utilities). Order is fixed (founder reorder + rewording 2026-07-25, supersedes the 2026-07-21 letter): the stamp "By private invitation only · Ticket No. {N}" at the TOP of the letter (ticket segment only when `ticketNo` exists in the payload; "No. " format per owner revision 2026-07-21) / **"{Receiver}, {Sharer} has gifted you a film."** (founder decision 2026-09-03, replacing "shared a ticket with you"; ONE uniform type style — never bold a name or word; both names first-word-trimmed; missing receiver drops the prefix, missing sharer reads "Someone") / the lineage thread (whisper) / film title / transmission hook (`films.transmission_hook`, renders NOTHING when NULL — no box, no placeholder) / the inline claim form + **"Watch for free"** (the CTA since 2026-09-03; was "Claim your ticket" 2026-07-31–09-03, "Accept your invite" before). **The claim form is EMAIL-ONLY since 2026-08-06** (founder decision): one email field (placeholder "Your email" since 2026-07-31, was "you@example.com") + the button. ~~The two-field form (2026-07-31–08-06): "Your full name" above the email~~ — the full-name field was REMOVED from this page 2026-08-06; the claim request carries no typed name and the server's legacy fallback names the account (recipient_name → email local part). `fullNameInputError` stays live in the dashboard's name editor ("Edit your name", first-name-only since 2026-08-06); `splitFullName` stays for the server's compatibility path. The headline's 2026-07-31 "do not re-open" note is SUPERSEDED: on 2026-09-03 the founder explicitly re-opened it and chose "has gifted you a film" (an explicit founder reversal, recorded as such). NOTHING renders below the button — "This invitation admits one person, once." was CUT from this page (2026-07-25; the 2026-07-24 plan to move it above the email field was superseded by the cut). The old "Dear {first name}," greeting is GONE; "saw this and thought of you" lives in the post-claim prologue now. NOT on this page: constraint line, ordinal, conditions line, graph.
2. **Watch `/watch/{slug}`** (`src/pages/ClaimWatch.jsx`): **REDESIGNED 2026-07-23/24 (watch-redesign-2) — THE BINDING SPEC + full amendment record is `docs/watch-page-spec.md`; read it before touching this page** (pixel ground truth `design-refs/watch-page_24.html`, local-only). Desktop ≥900px: header (wordmark + "Your dashboard →" — **the header link renders at ALL widths since 2026-07-31**: the old below-540px hiding is removed; under 540px its tracking tightens one step so it clears 360px beside the wordmark, e2e-measured; the footer link stays) / masthead (title, explicit italic + "14 minutes. Headphones recommended." via `filmConditionsLine`) / the hero grid — the Mux player left (poster from `src/content/filmStory.js` per-film — Mux thumbnail fallback, one-line override; flat on ink, never autoplay), the RAIL right: the tier bar toward the next ladder goal (`src/lib/viewerTiers.js` — fixed 100→1,000,000 ladder, no percentages ever), the TRUE film-wide TICKETS-SHARED count (founder metric switch 2026-07-25: `filmSharesCount` from the link route, shared rule `countFilmShares` in `src/lib/filmClaims.js` — non-void generated links; replaces the claims count, which the route still serves; label "Tickets shared of {goal} goal"), "Milestones passed" hallmarks (the WHOLE block absent until the first tier — no placeholder, no celebration; separators are spacing only), the gold "Pass it on" CTA (the page's ONLY solid-filled object; its resting keyline was brightened to `#d5c9a6` on 2026-07-31 — the accent-on-gold ring was invisible on phones — founder verified on device, CLOSED; fill/hover unchanged, the ring color is the one knob on the button's className in `ClaimWatch.jsx`), and the pairs-of-hands rule line (`src/lib/handsChain.js`, "1 pair" singular, "— or its last." in accent, NBSP-bound). Below: the three-statement creed band and the per-film filmmaker story (`src/content/filmStory.js` — Circles' entry is FULLY REAL since 2026-07-31; the other two films keep clearly-marked TEMP placeholder copy and TEMP locations; a film with no entry renders NO story section). **The pass-it-on flow lives in a MODAL opened by the CTA** (founder override of the 2026-07-19 always-docked decision): native <dialog>, the lineage emblem (fixed geometry, REAL first names, near-branch forks lit ONLY by the server's `lineageForks` booleans — `src/lib/lineageForks.js`, never invented people; INTERIM translate-centering for chains <3 pending the sparse design-ref), ticket stubs, "{n} tickets left.", the charge, the first-name form, and the REPLACEMENT-MODEL reveal (the link renders where the field was — zero scrolling; "Share another ticket" cycles the window; "See where your ticket went →" exits to the dashboard). **The personalized constraint line is CUT from this page** — `constraintLine.js` stays lib-only with no rendering surface; its designated future home is the claim/invitation flow. Unchanged by the redesign: the arrival fade (`.dc-watch-arrival`, in-memory marker), resume via `resumePositionToSave` (slug-scoped keys, near-end ERASED, `?again=1` starts clean), the ≥70% watched mark, and phone fullscreen/rotate-on-play.
3. **Dashboard `/dashboard`** (the payoff): the **V5 viewer dashboard** (`src/pages/ViewerDashboardV5.jsx`, LIVE since 2026-07-22; spec `design-refs/deepcast-dashboard-v5.html`) — ONE dashboard for every user, no tiers. Sidebar: name, gold "Ticket No. N", Tickets remaining/shared ("Tickets shared" since the 2026-07-25 vocabulary ruling), gold "Share this film" CTA (the quiet aside paragraph was REMOVED 2026-07-21, nothing replaces it), and ALL FIVE links for everyone, in this exact order (owner 2026-07-21): About Deepcast (opens the About POPUP — `AboutModal` → shared `AboutContent`; `/about` stays live for direct links) / Edit your name (label since 2026-08-06; was "Edit your full name" 2026-07-31–08-06, "Edit your first name" before) / Report a bug (mailto ien.chi96@gmail.com, subject "Deepcast bug report") / Contact / Sign out; pinned so they can never scroll out of sight; mobile = top-bar Menu overlay + fixed bottom share bar. Main: state-aware screening cards (`src/lib/screeningCard.js`); the journey line + CONSTELLATION (sunburst star map, `src/lib/constellationLayout.js` + `src/components/ConstellationMap.jsx` — filmmaker at center with name + "TICKET NO. 1", gold path film→you→your invitees, dim twinkling web, hover lights the web gold, wheel/pinch zoom at the pointer + immediate drag; **label visibility is COLLISION-BASED since 2026-07-31 — founder principle, binding for this surface: the names ARE the product, a label hides only when it would physically collide with another, never by a blanket rule.** Every viewport: gold-path names always render (a gold-gold collision is console-reported, never silently resolved), dim-web names fill the remaining room closer-to-YOU-first and appear progressively as zooming creates space; every label counter-scales to a minimum readable on-screen size, the filmmaker center-labels included. Rules + the two knobs — `MIN_LABEL_ON_SCREEN_PX` (11) and `LABEL_GAP_PX` (3) — live in `src/lib/constellationLabels.js`, unit-tested; e2e proves sparse-shows-every-name and crowded-reveals-on-zoom on all three engines); "Tickets you've shared" rows (status + copy link; empty state "No tickets shared yet." since 2026-07-25). Solid `--color-bg-page` background — no grain. Data rules live in shared libs only: `journeyLine.js`, `ticketRows.js`, `constellationLayout.js`, `inviteExistence.js`, `displayName.js`.

### Server routes (`server/index.js`)

- `POST /api/invites/create-link` — two sharer-identity paths (both set `parent_invite_id`, unified lineage model): a **verified session** (Bearer → `getUser()`, reusing the exact parent-resolution fallbacks of the legacy send route), or a **claimed invite reference** (`server/claimIdentity.js` — the claim IS the identity; no session needed). Since Pieces E/F the wallet fork keys on ACCOUNT existence, never on the session: a `claimed_by`-backed sharer spends their per-film `film_tickets` wallet (server-resolved, `sender_id` stamped on the child — unified lineage), and only account-less rows fall back to the invite wallet's CAS. Slug generation in `server/inviteSlug.js` (owner ruling 2026-07-21 — neutral slugs): the fixed word `ticket` + hyphen + a 5-char suffix from the unambiguous alphabet (no 0/o/1/l/i), 3 retries then widen to 6; **the typed first name never feeds the URL** (names come only from `invites.recipient_name`); reserved-route blocklist kept, plus a startup guard that refuses if `ticket` ever becomes a route. Old name-based slugs (pre-2026-07-21) are never rewritten and resolve forever.
- `GET /api/invites/link/:slug` — public landing/watch payload (names, lineage, hook, posterUrl, muxPlaybackId, tickets, and `ticketNo` — the permanent ticket number, present for unclaimed and claimed links alike, null when the row has none). Never exposes the legacy `token`. Never transitions status. **Owner revisit of a claimed link routes by progress (2026-07-21):** the stash-recognized owner is bounced to `/dashboard` once the film is completed (the shared `isInviteWatched` rule in `src/lib/filmStats.js` — 70%-watched, the same bar as the ticket statuses), else to their watch page; strangers still get the dead-link page.
- `POST /api/invites/claim` — the email IS the claim, accompanied by silent context (2026-07-31). **Full name (SERVER PATH ONLY since 2026-08-06):** the landing page no longer sends a name (email-only form — founder decision 2026-08-06), so every claim-link claim takes the legacy naming fallback (recipient_name → email local part). The server-side `fullName` handling stays intact for compatibility (old tabs, future surfaces): when present it is re-validated (`fullNameInputError`), its FIRST word becomes the account's first name and the remainder becomes `users.last_name` via `findOrCreatePasswordlessAccount`. Last names are DATA, never display — every surface stays first-name-only. **Silent context capture (owner-approved in-session 2026-07-31):** the browser's timezone, language, and coarse device class ride the claim and stamp `invites.claim_timezone` / `claim_locale` / `claim_device` (migration `20260731_claim_context.sql`, applied via the approved path and INDEPENDENTLY verified through `information_schema`) — sanitized by `sanitizeClaimContext` (`src/lib/claimContext.js`, device restricted to phone/tablet/desktop), non-fatal at every layer: blocked browser APIs send nothing, junk is dropped, missing columns fall back to claiming without capture. Atomic race-guarded UPDATE (`eq status 'created'` + `is claimed_email null`); stamps `claimed_email`, `claimed_at`, `status='claimed'`, the frozen `claim_ordinal` (+ the context fields); creates (or attaches to) a passwordless account, stamps `claimed_by`, initializes the film's `film_tickets` wallet at 5. **The claim SIGNS THE NEW USER IN (Fix A, 2026-07-21):** for a freshly CREATED account only, the server mints an in-band single-use sign-in (`generateLink` → client `verifyOtp`, no email) — an ATTACHED pre-existing account NEVER gets a session from a typed email (they use the normal sign-in flow). **The client's `verifyOtp` exchange is deadline-bound at 8s since 2026-07-31** (`withTimeout` in `src/lib/withTimeout.js`, unit-tested): a hung exchange can no longer strand the claimer on "One moment…" — past the deadline the claim proceeds exactly as a failed exchange always did (non-fatal by construction). **Duplicate-claim guard (Fix B + amendment):** if the email already holds this film, the fresh link is VOIDED (`status='void'`, race-safe — can never destroy a real claim; its ticket number dies with it, permanent gap), the sender's ticket auto-refunds (`server/voidRules.js` — skipped for role/wallet-unlimited senders; `team_creator_id` only ever read), and the claimer sees "You already hold this film." before routing to their dashboard. The sharer's own session never claims; the film never travels back to its maker (`server/shareRules.js`). Routes to the watch page through the once-per-claim PROLOGUE (founder spec 2026-07-21, `ClaimPrologue` in `ClaimLanding.jsx`): a full-screen interstitial on the brand ink background (softened 2.5x + timing polish, owner revisions 2026-07-21: 2.5s line fades at 0/3/6s, ~3s hold, 3s dissolve beginning ~11.5s in), three staggered italic serif lines (each staying once shown, ONE uniform style, no bolding) — "{Receiver}, {Sharer} saw this and thought of you." / "No algorithm sent you this. A person did." / "After watching, you'll do the same — choose the few people who need it next." (wording revised by founder approval 2026-07-24) — tap/Enter/Space to advance (first tap reveals all, next skips), reduced-motion renders all three lines statically and HOLDS until a tap/Enter/Space — no timed auto-advance (2026-07-31; the old ~4s hold was field-verified too short to read) — and it warms the watch chunk + Mux preconnects + poster while holding. NO storage: it exists only in the claim-success state, so duplicates, sharer-view, dead links, voids, and revisits structurally never see it. Its dissolve hands off to the watch page's 1s arrival fade via the in-memory `fromPrologue` router marker (cosmetic only — the once-per-claim guarantee never depends on it).
- `POST /api/invites/replenish-check` — the +3-per-3-watched viewer replenish, per-film, server-recomputed (Piece F; the old client-side write of another user's row was RLS-dead). Called from the legacy screening where `checkReplenish` ran.
- `GET /return` (client route, `src/pages/ReturnGate.jsx`) — where magic sign-in links land: routes to the most recently claimed UNWATCHED film's watch page; all watched or no claims → `/dashboard`. Never dead-ends for any authenticated user; `claimed_by` primary, `claimed_email` fallback.

### Tickets (PER-FILM economy — Piece F, 2026-07-18)

One wallet per (person, film): **`film_tickets(user_id, film_id, balance, unlimited)`**, migration `20260717_film_tickets.sql` (applied). Rows are LAZY — a missing row reads as the virtual full grant of 5 and materializes on first write; claiming a film initializes that film's wallet at 5. Spent at link GENERATION, no refunds (a failed generation is refunded — that's not a spend); race-safe spend/grant/refund live in `server/filmWallet.js`; canonical display is `filmTicketsRemaining(profile, wallet)` in `src/lib/shares.js`. EVERY spend path goes through the film wallet: legacy `/api/invites/send`, both create-link branches (session, and stash-based via `claimed_by` — `server/claimantWallet.js`), the admin grant, and the server-side replenish. **`users.invite_allocation` and `users.unlimited_shares` are DORMANT** — still written at account creation, never read on ticket paths. The legacy invite wallet (`invites.tickets_remaining`, `src/lib/ticketRules.js`) survives ONLY as the degradation path for claimed rows with no account (claimed_by NULL). Zero state: "You've shared all your tickets for this film." (verb per the 2026-07-25 vocabulary ruling) — no upsell, and now truthfully per-film.

### Ticket numbers — LIVE, permanent, immutable (2026-07-22)

Per-film sequential numbers, minted atomically at GENERATION by `next_ticket_no()` (`films.ticket_seq`; service-role only; non-fatal — numbering can never block a share). **The filmmaker always holds №1** (`films.creator_ticket_no`, migration `20260722_creator_ticket_no.sql`, applied; new films are born with the counter at 1 by column default), shown on the creator dashboard's film cards and under FILMMAKER at the constellation's center. **Invitees start at №2. Numbers are IMMUTABLE: deletions leave permanent gaps; NOTHING ever renumbers an invite.** Ghosts and voided links are never numbered (`needsTicketNumber`). Assigned as of the 2026-07-22 backfill: The New Narrative — Trace №2, Austin №3, Tina №4, Georgie №5. A Sacred Pause — №2 was Jon's original test invite, removed by the owner via admin Remove on 2026-07-21 ahead of the live demo claim; №2 is now a permanent gap and the next generated ticket mints №3. **`server/backfill-ticket-numbers.js` and `server/delete-orphan-rows.js` are COMPLETED one-time operations — do not re-run** (both are dry-run-by-default and state-guarded if ever touched).

### Ordinal freeze

`invites.claim_ordinal` is stamped ONCE at claim ("Nth person invited to this film" at that moment) and never recomputed; the dashboard displays it via `formatOrdinal` (`src/lib/ordinal.js`). Pre-claim displays may compute live.

### Lineage thread (`src/lib/lineageThread.js` — unit-tested grammar)

Built from `parent_invite_id` ancestry (walked server-side in the link route, one film-scoped query). Label above it: "How this reached you". Total nodes ≤ 4 → every first name (`[Ien] —— [Dan] —— [you]`); ≥ 5 → collapse the middle to "⋯ N hands ⋯" keeping three anchors (origin, direct sharer, you), N = total − 3. First-naming happens in the lib; the slug is routing only and never a display source.

### One tier — everyone is a signed-in user (Fix A, 2026-07-21; the claimant tier is GONE)

There is no account-less claimant and no stash-based dashboard admission anymore. Claiming creates the account AND signs the browser in (see the claim route above), so `/dashboard` has ONE path for everyone: `ProtectedRoute` + `ViewerShareGate` (which admits never-shared claimants by `claimed_by`). The old pseudo-profile, `DashboardGate` stash admission, and every claimant-only branch were deleted — do not reintroduce them. Verified before removal: ZERO account-less claimants existed in production. The claim stash (`src/lib/claimStash.js`) survives ONLY as the watch page's same-visit ownership recognition and resume-key scoping — it never grants dashboard access; a stash-only browser reaches the sign-in page (never a blank one — the no-blank-page rule holds via ProtectedRoute's spinner/redirects).

**Return visits:** magic sign-in links work for everyone (Login submits with `redirectPath: '/return'`; `/return` routes to the most recent unwatched claim's watch page, else `/dashboard`). Supabase Auth → Redirect URLs must include `{site}/return`.

### Who exists — ONE definition for every surface (`src/lib/inviteExistence.js`)

The single source of truth for which invite rows count: voided links count NOWHERE (constellation, journey line, tickets given, funnel, admin table, backfill — but stay visible in the sender's ticket list as ledger history), and demo ghosts are excluded everywhere except admin surfaces (the module's `includeGhosts` parameter, owner decision). **New surfaces must read this module — never write a private filter.** **Per-film ghost visibility (`films.show_ghosts`, migration `20260722_show_ghosts.sql`, applied; owner ruling 2026-07-22):** default false = exactly the behavior above; when the owner flips it true for a STAGING/DEMO film, the viewer surfaces (constellation, journey counts, ticket rows/Shared-to-N, tickets given) include that film's ghosts as ordinary, indistinguishable nodes — threaded as `includeGhosts` from the film row through `Dashboard.jsx` → `ViewerDashboardV5` → the libs. Ghosts are NEVER ticket-numbered regardless of the flag (`needsTicketNumber` ignores it), admin surfaces are unaffected, and flag-off films are asserted byte-identical to pre-flag behavior in the lib tests. `needsTicketNumber` is the numbering rule (unnumbered + exists); `countTicketsGiven` is the given-count; `safeFirstName` (`displayName.js`) is the display-name rule (an email or any fragment of one is NEVER rendered as a name — placeholder "Someone").

### Whose name — the canonical-name rule (ratified 2026-07-23)

**A person's own account name is their one true name everywhere; the name their inviter typed is only a placeholder until they claim.** On every surface — landing headline, lineage chain, constellation and graph labels, ticket rows — a CLAIMED invite's person renders as their account's current name, and renames propagate everywhere (including into OTHER people's views of them — ratified, no softening). UNCLAIMED invites keep showing the typed `recipient_name` (the only name that exists yet).

- **Mechanism is materialize-on-write, NEVER resolve-at-render.** RLS makes render-time resolution impossible (users rows are readable own-row/team-only — verified in `pg_policies`), and a second resolution path would let surfaces disagree. `invites.recipient_name`/`sender_name` are the ONE render source every surface reads (unchanged readers: `displayName.js`, `ticketRows.js`, `constellationLayout.js`, `graphLayout.js`, `networkPeople.js`, the server lineage builder); `users.name` is the authority that writes them, at exactly three moments:
  1. **Creation** stamps the sender's account name (`/api/invites/create-link`, `/api/invites/send`).
  2. **Claim** re-stamps `recipient_name` from the claiming account's name (`server/claimNameRule.js`, unit-tested): an ATTACHED account's current name always stamps, and — since the full-name-at-claim work, 2026-07-31 — a CREATED account stamps too **when the claimant themselves typed the name it was born with** (`claimantNamed`). *(Dormant on the claim-link flow since 2026-08-06: the email-only landing form sends no typed name, so created accounts take the legacy no-name path — which never stamps, its name may be email-derived. The rule stays live for attached accounts and any future name-carrying surface.)* Blank or @-containing account names never stamp on any path — an email fragment is never written as a name.
  3. **Rename** (`handleSaveName`, `Dashboard.jsx`) propagates to sent rows by `sender_id` and to received rows by `claimed_by` + `claimed_email` (+ legacy `recipient_email`). Claim-flow invites store `recipient_email` NULL, so the old email-only match reached nothing — that was the 2026-07-23 fix.
- **Every propagation write is row-filtered — never a bare table-wide update** (e2e-guarded in `viewer-dashboard-v5.spec.js`): ghosts and strangers are structurally unreachable. Ghosts are never claimed (`claimed_by`/`claimed_email` NULL on all 149 seeded rows, verified), so they keep their typed names automatically — no guard code exists or is needed.
- **Pre-claim surfaces greet the typed name by design** (landing letter, prologue — "the letter was addressed to that name"); the account name takes over at the claim (owner decision 2026-07-23).
- RESOLVED leftover (2026-07-25): Faith Circle ticket №2 ("Kanye", claimed by the `ien.chi96+test18` test account before this rule shipped) was deleted by the founder via admin Remove after the 2026-07-24 EOD check — the anticipated by-hand cleanup happened. Historical note kept because that two-film row is what exposed the stacked-popover bug (see the popover-bookkeeping section).

### Reach (decided 2026-07-16 — do not change)

"People you've reached" counts opened/watched/signed_up only. Claimed-but-unwatched does NOT count — a ticket given is its own stat. `src/lib/reach.js` stays as-is.

### Founder-approved copy (verbatim, do-not-edit; the copy layer is CLOSED — changes come only as explicit requests from Ien)

- Constraint line (**CUT from the watch page by the 2026-07-23 redesign — NO surface renders it today**; the lib and tests stay, and its designated future home is the claim/invitation flow; the share modal's copy was removed and the sidebar aside deleted back on 2026-07-21): personalized "{Receiver}, this film reached you because {Sharer} thought of you. No algorithm, no feed. Films here spread by private invite & real humans only." — generic fallback "This film reached you because someone thought of you. No algorithm, no feed. Films here spread by private invite & real humans only." (`src/lib/constraintLine.js`). **The old ending "pass through human hands only" is retired everywhere.**
- Landing letter (founder rewording + reorder 2026-07-25; claim-form additions 2026-07-31; headline + CTA reversal 2026-09-03): **"{Receiver}, {Sharer} has gifted you a film."** (founder decision 2026-09-03 — an EXPLICIT founder reversal of the 2026-07-31 "do not re-open" note, which is superseded; replaces "shared a ticket with you" (2026-07-25–09-03), which had itself replaced "gifted you a film"; uniform type, no bolding) · "By private invitation only · Ticket No. {N}" (bare "By private invitation only" when no number) — the stamp at the TOP of the letter · **"Watch for free"** (the CTA, 2026-09-03, replacing "Claim your ticket" (2026-07-31–09-03), which replaced "Accept your invite") · ~~"Your full name" / "Just your name — no email needed."~~ (the claim form's name field and its refusal line — RETIRED FROM THIS PAGE 2026-08-06 with the email-only form; the mechanical validation and its self-facing refusal line live on in the dashboard's name editor — "Edit your name", first-name-only since 2026-08-06) · **"Your email"** (the email placeholder, 2026-07-31, replacing "you@example.com" on this form only — login/signup/reset keep theirs). "This invitation admits one person, once." no longer renders anywhere (cut from the landing page 2026-07-25, its last surface).
- Prologue (founder spec 2026-07-21, wording revised 2026-07-24): "{Receiver}, {Sharer} saw this and thought of you." / "No algorithm sent you this. A person did." / "After watching, you'll do the same — choose the few people who need it next."
- Reveal (STAMPED 2026-07-23, superseding the 2026-07-21 wording): the watch modal's line 1 is "Here's {name}'s ticket link. Send it to them with why they came to mind." (the dashboard share modal keeps its own line 1). The tickets line (`revealTicketsLine.js`, ONE rule for BOTH share surfaces — the dashboard modal picking up the new copy was owner-intended): "{N} tickets left. Who else needs it?" / "1 ticket left. Who else needs it?" / N=0: "That was your last ticket for this film." / unlimited: "Who else needs it?" (numerals always). New watch-page founder copy (all verbatim, ledger in `docs/watch-page-spec.md` §7; 2026-07-25 revisions folded in): "Pass it on" (the CTA, and now also the modal eyebrow alone — "Pass it on. Make an impact." was cut 2026-07-25) · "Tickets shared of {goal} goal" (2026-07-25, replacing "Viewers reached of {goal} goal") · "Milestones passed" · "This film passed through {n} pair(s) of hands to reach you. You are its newest link — or its last." · the three creed statements (line 3 revised 2026-07-25: "Share intentionally. Each ticket admits one person only.") · "Who needs to see this? Not anyone — the one it will matter to." · "{n} tickets left." · "Share another ticket" (2026-07-25, was "Create another invitation") · story header "Filmmaker · {name} · {location}" (2026-07-25, replacing "From the filmmaker · {location}"; the "— {name}, director" sign-off is cut — the name lives in the header only). The dashboard share modal's own line 1 was aligned the same day: "…it admits one person only." (was "…once."). **The "one uniform type style" rule is AMENDED (not deleted): exactly three approved in-copy emphasis exceptions, all on the watch page — "or its last." (rule line), "you" (creed 2), "needs" (the charge).**
- "How this reached you" · ~~"Accept your invite"~~ · ~~"Claim your ticket"~~ (both superseded — since 2026-09-03 the landing CTA is "Watch for free"; see the landing letter above) · "Share it with them" (2026-07-25, was "Create their invitation") · "Copy their ticket link" (2026-07-25, was "Copy their invitation" — EVERY copy button, viewer and creator surfaces alike, including the V5 ticket rows' formerly separate "Copy invitation link" label, folded in by founder stamp the same day; every copy is the BARE link, the pre-written "I watched this…" message is retired) · "You can share N tickets for this film. Each admits one person, once." (reconciliation note 2026-07-25: this line no longer renders on any surface — its share-panel home was removed by the watch redesign; wording kept for the record) · "You've shared all your tickets for this film." (2026-07-25, was "You've given…") · "See where your ticket went →" · "Your dashboard →" · "14 minutes. Headphones recommended." · share modal heading "Who needs to see this film?" (the creator link panel alone still says "Who is this film for?").
- **V5 dashboard (2026-07-21; vocabulary unified 2026-07-25 — "ticket" is the noun, "shared" is the verb, "Pass it on" reserved for the CTA):** heading "Tickets you've shared" · empty state "No tickets shared yet." · sidebar stat "Tickets shared" · mobile identity line "…tickets remaining · N shared" · journey line Y>0: "This film has reached [X] people. [Y] of them through you." (revised 2026-07-22) · Y=0: "This film has reached [X] people. Grow that number by sharing the film." (always numerals; Y = the viewer's ENTIRE downstream subtree, counted by the constellation — a documented deviation from opened-based reach) · section heading "Where this film has traveled" · ticket statuses "Unopened / Opened / Watched / Shared to N people" · duplicate claim: "You already hold this film." and the sender's row "Already held this film — ticket returned." · first-name input refusal: "Just their first name — no email needed." (client + server, one shared module — `src/lib/firstNameRule.js`, MECHANICALLY HARDENED 2026-07-31: also refuses digits, URL-ish fragments including the domain SHAPE — a dot followed by 2+ letters, so bit.ly/t.co/deepcast.art refuse while "J.R." passes, and a dotted name like "tom.compton" is refused BY DESIGN as domain-shaped — and anything past 40 chars; a real name must never be rejected, held by a required-to-pass test list) · "Report a bug" · About copy = `AboutContent.jsx` (see Voice and copy rules).
- Visitor screen: "This page belongs to invited viewers." / "If someone passed you a film, open the link they sent — it's your way in."
- Dead link: "This invitation has already been accepted." / not-found: "This invitation link doesn't lead anywhere."
- Error screen (`src/components/ChunkErrorBoundary.jsx`, 2026-07-31): reuses the approved error-state lines "Something went wrong on our side." / "Please try again in a moment." plus the founder-stamped button **"Reload the page"**.

### Standing content rules

- **Real films ship with bar-free masters and a hand-picked `poster_url`.** Baked letterbox bars poison the poster frame and player (and the legacy email GIF, while that path survives), and nothing downstream can remove them.
- **Real films need real Ien-authored transmission hooks** before they ship; the demo film's hook is demo copy.
- **The watch page's TEMP-content ledger lives in `src/content/filmStory.js`** — the filmmaker story epigraphs/body (**Circles' are REAL since 2026-07-27** — the founder's verbatim director's statement, commit 3ca71db, do-not-edit; The New Narrative's and A Sacred Pause's remain layout-holding placeholders, deliberately self-describing), the filmmaker locations ("Atlanta, Georgia" is TEMPORARY for The New Narrative and A Sacred Pause; **Circles' is REAL — founder-confirmed 2026-07-31**), the photo frames (The New Narrative and A Sacred Pause still render the empty circle frame; **Circles has its real photo, `/portrait-5.jpg`**), and the player poster rule (Mux auto-thumbnail `?time=1`; a hand-picked frame is one line in `POSTER_OVERRIDES` — Circles' entry PINS the original video's thumbnail, see the Films section). The remaining placeholders swap in one pass when the founder provides the real content; a film with no story entry renders NO story section by design.
- **Story avatar (2026-07-24; header restructured 2026-07-25):** the portrait circle is 96px desktop (≥900px) / 80px mobile, the "FILMMAKER · {NAME} · {LOCATION}" header is BOTTOM-aligned to the circle (founder preference — centering was tried and reverted), the photo gets a CSS `scale-[1.12]` zoom inside the frame (the image file is never edited for crop), and each of the header's `·{NBSP}phrase` segments (name, location) is one unbreakable span so narrow screens break cleanly at the "·" boundaries, never mid-phrase. The name moved into the header from the sign-off, which is cut.

## CURRENT STATE (updated 2026-08-22 — THE NETWORK GREW ON ITS OWN, AND THE FILM GOT ITS THIRD CUT)

**The 2026-08-22 batch (branches `circles-recut-3` and `circles-recut-3-cleanup`, both merged and pushed):**

- **Circles plays the THIRD CUT** (playback `QDUEUyF7…`, asset `01FJ3LO…`, 1803.135633s → **"30 minutes. Headphones recommended."**; full video history in the Films section). The founder ran the database UPDATE himself and verified the swap live; the swap was database-verified, deploy-verified (both playback ids present in the shipped bundle during the transition), and live-payload-verified end to end. The swap used the dual-key transition net in `filmStory.js` (both old and new playback ids keying the identical story + pinned poster, so neither deploy order could strand the page), removed in the cleanup commit after live verification — the pattern to reuse for future swaps. The ghost seeder's identity guard follows the new id; the poster still pins the ORIGINAL video's thumbnail (fourth swap it has held through — that Mux asset must never be deleted); the Circles e2e spec now mocks the new id and asserts the 30-minute line.
- **The network grew organically between reconciliations (2026-08-08 → 2026-08-17, database-verified today):** three founder-sent tickets (№6 Yan, №8 Arielle, №9 Marcus) and three MORE organic onward shares from two NEW sharers — Yan → №7 Christina, Arielle → №10 Joiselle + №11 Cal. `ticket_seq` = 11. Full verified ledger in the Films section. No protection-list changes were needed: film-level protection covers every claimant automatically.
- **Working-tree note (report-only, deliberately uncommitted):** `design-handoff/` (the 2026-07-23/24 watch-page design handoff — reference HTML, fonts, zip; local design assets) and `e2e/scratch-safari-private-dashboard.spec.js` (dated 2026-08-06, the Phase 1 diagnostic for the Safari-private dashboard bounce on the unmerged `safari-private-session` branch). **The scratch spec IS picked up by the e2e suite** (`testDir: './e2e'`, no ignore pattern) and adds 6 tests (2 × 3 engines) to every full run — a "201 passed" total includes them; the committed suite alone is 195. Its RESTRICTED-storage test asserts the BUG behavior (bounce to /login), so it passes on `main` today and will FAIL the moment the Safari-private session fix merges — expect that, don't misread it as a regression.

**The 2026-08-06 batch (branches `faith-circle-recut`, `rename-circles`, `circles-protection-and-recon`, `circles-recut-2`, `simplify-name-editor`, `recon-aug-recut2`), since merged and pushed:**

- **THE FIRST ORGANIC ONWARD SHARE IS CONFIRMED IN THE DATA:** Oliver claimed Circles №2 on 2026-08-03, watched, and shared three tickets onward — №3 Steve (in flight), №4 Brian (claimed and watched the same day), №5 Katie (in flight) — with zero prompting from the founder. `ticket_seq` was 5 at that reconciliation (11 as of 2026-08-22). Full verified ledger in the Films section.
- **The recut is live — and was recut AGAIN the same day:** Circles played the SECOND recut from this batch (playback `wAGDrU3B…`, asset `XTSJfgen…`, 1789.4553s → "29 minutes. Headphones recommended."; the first recut `cy02v48f…` served for part of the day; superseded 2026-08-22 by the THIRD cut — full video history in the Films section); the story and poster-pin entries re-keyed at each swap with content byte-identical; the poster still pins the ORIGINAL video's thumbnail (that Mux asset must never be deleted).
- **The name editor is FIRST-NAME-ONLY** ("Edit your name", one "Your name" field prefilled with the current first name, helper line unchanged): saving writes `users.name`/`first_name` only — NO surface writes `users.last_name` anymore (existing data untouched, column and server handling kept for compatibility); the four row-filtered propagation writes are unchanged.
- **Oregon is SUSPENDED** (founder-executed 2026-08-06 after the read-only audit found nothing pointing at it; verified 503 — config preserved, resumable for rollback only).
- **The film was renamed "Circles"** (2026-08-06; formerly Faith Circle, formerly The Faith Dialogues). The title lives in `films.title` — every surface is data-driven; slugs and links unaffected.
- **The claim form is email-only again** (the landing page's full-name field removed 2026-08-06; server name-fallback and the dashboard's full-name editor unchanged).
- **FILM-LEVEL PROTECTION shipped** (see the ruling in the real-users header): `PROTECTED_FILM_IDS` in `server/deleteRules.js`, enforced in the admin Remove flow (both routes), the account-deletion backstop, and reset-test-data's hard abort. Oliver and Brian joined `PROTECTED_EMAILS`. Adding a film here is a launch-day step for every future live film.
- **The ghost seeder's identity guard tracks the current video** (re-pointed at each swap — at that batch the second recut, since 2026-08-22 the third cut — it fails safe for the right reason).

**THE LAUNCH HAPPENED (2026-07-31 evening):** the founder personally minted and sent Faith Circle ticket №2 to Oliver — the first genuine human-to-human transmission — and Young Chi (the founder's mother) claimed a real ticket (№36) on The New Narrative. Both are protected: Young Chi in code tonight, Oliver at first claim (see the real-users header and the Films section). Post-launch same-day fixes, all live: the reduced-motion prologue holds until a tap (`e31b574`), dashboard name editing is full-name through the one shared split rule (`274a016`), and the editor's helper reads the founder-stamped "Only your first name appears on the network." (`4a5ac9a`). Confirmed unchanged: the watch rail counts tickets shared (the 07-25 metric) with honest sparse numbers by design.

**The 2026-07-31 launch-prep batch is merged to `main` and deployed** (branches `launch-prep-batch` + `fix-name-rule`, twelve commits, `d04b015`…`be75ea0`). What it added, atop the V5 ship below:

- **The app can no longer blank-screen on a stale deploy.** One error boundary wraps the whole router (`src/components/ChunkErrorBoundary.jsx` + `src/lib/chunkReloadGuard.js`): a failed lazy page/player-chunk fetch triggers ONE self-healing reload (loop-guarded via sessionStorage AND history.state, so blocked storage can never loop); a second failure renders the on-brand error screen — never a bare ink page. Root cause of the 2026-07-28 mobile blank screen: two production deploys landed mid-visit, the old hashed chunk 404'd, and React unmounted the app with no boundary. E2e-proven (`e2e/chunk-failure-recovery.spec.js`) on all three engines.
- **The claim asks for the claimant's full name** and silently captures timezone/language/device (see the claim route — new columns `claim_timezone`/`claim_locale`/`claim_device`); the in-band sign-in is deadline-bound at 8s; the landing CTA became "Claim your ticket" (superseded 2026-09-03 by "Watch for free") and the email placeholder "Your email".
- **Name validation is mechanically hardened** on all four name surfaces via the one shared module (digits, URL/domain shapes, 40-char cap — see the copy section). The domain-shape rule (`be75ea0`) fixed the launch-day Vercel build failure.
- **The watch page's keyline reads on phones** (`#d5c9a6`, founder-verified, closed) and its header dashboard link renders at every width.
- **Constellation labels are collision-based** — the names ARE the product (see the dashboard description in the claim-link section).
- **Faith Circle is launch-ready:** fully real content, ghosts admin-only, clean ledger reset to №1 (see the Films section).
- **Data-state truth note (supersedes the 2026-07-20/22 "production data is CLEAN" claim below; re-verified 2026-08-22):** The New Narrative carries the founder-minted test-ticket pile (`ticket_seq` = 36 — re-verified 08-22 — Young Chi's real №36 among the rows) — its cleanup is a QUEUED, founder-run task that must EXCLUDE Young Chi (`imyme2024@gmail.com`, protected in code). Circles carries only real data (№1–№11, the verified ledger in the Films section — film-level protected); A Sacred Pause is untouched.

The V5 ship (2026-07-22), still the foundation:

- **The V5 viewer dashboard is the ONLY viewer dashboard** — one tier, every user signed in, all five sidebar links for everyone (About popup / Edit your name / Report a bug / Contact / Sign out), constellation + journey line + "Tickets you've shared", solid background (grain removed, sidebar aside removed), ticket numbers displayed. The creator/admin dashboard keeps its own view (link panel, funnel, people table, ticket controls, Remove) plus "Ticket No. 1" on each film card.
- **Shares are custom unique links only — BARE links with neutral slugs (2026-07-21).** New links are `ticket-xxxxx` (the recipient's name never appears in the URL), every share surface hands over the link alone (no pre-written message anywhere), and every copy button says "Copy their ticket link" (ticket-verb sweep 2026-07-25). The email-invite system is retired: no viewer-facing surface creates email invites. (Truth note: two legacy `InviteForm` mounts survive on the creator-only Upload and Profile pages — the A5 remainder, harmless but pending removal.) Legacy `/i/:token` links and old name-based slugs still resolve forever.
- **Ticket numbers are live and immutable** (see the Ticket numbers section — filmmaker №1, invitees from №2, gaps are permanent).
- **Claiming signs the new user in**; duplicate claims void + refund + recognize (see the claim route).
- **The who-exists module governs every surface** (see its section — new surfaces read it, never write private filters).
- ~~**Production data is CLEAN (2026-07-20/22 cleanup)**~~ — historical; superseded by the data-state truth note above (test pile on The New Narrative pending founder cleanup) and by the 24 July all-test-data ruling.

**Completed one-time operations — DO NOT RE-RUN:** `server/backfill-film-tickets.js`, `server/backfill-claimant-accounts.js`, `server/backfill-ticket-numbers.js`, `server/delete-orphan-rows.js`.

**Test discipline (standing):** The New Narrative is the designated test film; A Sacred Pause is public-facing — never create test links, claims, or data on it. **Localhost writes to PRODUCTION** — a test claim on localhost mints a real, permanent ticket number and a real account.

**Parked (approved, not built — do not re-debate):**
- **Ticket expiry refund** — approved spec in `docs/ticket-expiry-refund-spec.md` (14 days unclaimed → sender refunded; the link never dies; late claims never re-charge).
- ~~Share-modal "human hands" wording~~ — RESOLVED 2026-07-21, then OVERTAKEN 2026-07-23: the unified wording lives only in `src/lib/constraintLine.js` now — the watch page's redesign cut the line's last rendering surface (see the claim-link section), so NO surface shows it today.
- ~~V5 dashboard: internally-scrolling columns~~ — DONE 2026-07-25: the viewer dashboard's desktop layout (≥768px) now uses the creator dashboard's architecture — viewport-height grid (`md:h-dvh`, clipped), the MAIN column scrolls internally (`md:overflow-y-auto`), and the sidebar genuinely holds still; the inert `position: sticky` was removed from the aside. Mobile (<768px) keeps natural page flow, untouched. The app-wide `overflow-x-hidden` guards in `src/main.jsx` and `#root` were NOT touched — they remain the watch page's sideways-scroll protection. The formerly-parked done-definition test lives in `e2e/viewer-dashboard-v5.spec.js` ("the sidebar stays PINNED while the main column scrolls"), green on all three engines; `docs/parked-tests/` is gone.
- **Sparse lineage-emblem composition — COMPOSITION CHOSEN, FOUNDER-BLESSED 2026-07-25 (v8.1, commit e7d4d46); the LIVE-MODAL IMPLEMENTATION is now a QUEUED task for its own session.** The reference is the dev-only harness at `/dev/emblem` (`src/pages/dev/DevEmblemHarness.jsx`, v8 2026-07-25: the founder-directed TREE grammar — one origin left, growth rightward, second-generation off-path forks; each panel composed ALONE; THREE panels — depths 1/2 approved, and **the v6 depth-3 composition is THE FINAL FORM for every depth ≥3 (v8 founder approval)**: for deeper chains the ONLY variable is the condensed node's label suffix — 1 hidden hand → bare name ("PRIYA"), 2+ → "{name} + {n}" ("PRIYA + 4", numerals always) — demonstrated by a label toggle on the unchanged drawing; the deep panel's separate Lena-as-sharer composition is RETIRED. The condensed label matches the named-label style exactly (9px/2.0; was 8.5/1.5) and sits centered beneath its dot (v8.1 micro-nudge 2026-07-25 — was hanging left; render-verified that both label states clear the dotted under-limb). v7's one-drawing/label-position/label-above rules stay rescinded). **v5 STRUCTURAL RULING (founder, 2026-07-25) — THE NAMED-CLUSTER COLLAPSE, binding for the eventual implementation:** at most THREE articulated stops on the gold path — filmmaker (origin), the viewer's DIRECT SHARER, and YOU (+ the dotted-gold "?"); origin and direct sharer NEVER compress. Hidden hands between filmmaker and direct sharer render as ONE SINGLE gold node ON the uniform gold line (v6 2026-07-25 — the v5 three-dot cluster is dead; same treatment family as the articulated nodes, subtly smaller than the direct sharer's node, NEVER larger — the label does the counting), labeled with the NEAREST hidden hand's real first name: 0 hidden → no node; 1 hidden → name alone ("PRIYA"); 2+ → "{name} + {n}" ("PRIYA + 4", numerals always; the v5 "+ n more" suffix is dead). The named person is the hand who shared TO the direct sharer — real lineage data, never invented; same grammar family as `lineageThread.js`. The v4 "N hands"/numeral count treatments are DEAD. Carried v4 rule: the gold path is VISUALLY UNIFORM end to end — no receding/faded segments ever. **The LINE GRAMMAR is founder-stamped (2026-07-25) and binds the eventual implementation:** realized path filmmaker→…→YOU solid gold; YOU→"?" dotted gold; all off-path branches dotted gray, faded, UNLABELED (never invented people); the "?" hollow, slightly larger, gold-ringed at the growing tip. Until the implementation ships, the INTERIM translate-centering stays live (commented INTERIM in `ClaimWatch.jsx`, recorded in `docs/watch-page-spec.md` §5) — replace the translate when the new composition lands, don't layer on it. **Implementation riders (recorded 2026-07-25):** (1) the dashboard constellation still renders bare "FILMMAKER" (`ConstellationMap.jsx`) — the approved "(filmmaker)" parentheses land there with this work; (2) the link route's `lineageNames` carries the FULL origin→direct-sharer chain (verified in `server/index.js` today), so the nearest hidden hand's name for the collapse label is derivable client-side — confirm at implementation, including legacy rows with odd name data (`safeFirstName` applies); (3) candidate ride-along (founder add): the name-validation hardening item below.
- ~~Render region migration (Oregon → Virginia)~~ — DONE 2026-07-23; Oregon served as the rollback net until its suspension 2026-08-06 (see Tech stack).
- **Session-based duplicate detection** — if a signed-in user opens a claim link for a film they already hold, treat it as a duplicate claim immediately: recognition message, void the link, refund the sender. Extends the existing duplicate machinery with a second trigger. IP-based and name-based detection explicitly REJECTED (false positives; membership privacy leak).
- ~~Landing-page reorder~~ — SHIPPED 2026-07-25, amended in-session: the stamp opens the letter; "This invitation admits one person, once." was CUT entirely (superseding the 2026-07-24 move-above-the-field plan); nothing renders below the button.
- **"(filmmaker)" parentheses (founder-approved 2026-07-24):** the filmmaker label gains parentheses wherever it renders.
- ~~**Name-validation hardening**~~ — SHIPPED 2026-07-31 (`fc71c53` + the `be75ea0` domain-shape fix): mechanical checks only in the one shared module (see the copy section); the emblem-implementation ride-along rider is satisfied.
- ~~**`show_ghosts` → false for Faith Circle**~~ — DONE 2026-07-31, founder-executed as sequenced: flipped on launch day, right before the first real share; database-verified.
- ~~**Mobile constellation names**~~ — SHIPPED 2026-07-31 in two steps, the second superseding the first's blanket rule: labels counter-scale to a minimum readable on-screen size everywhere, and visibility is COLLISION-BASED (founder principle: the names ARE the product; hiding is a last resort). See the dashboard description in the claim-link section; rules + knobs in `src/lib/constellationLabels.js`.
- **Constellation aesthetic pass (queued 2026-07-31, three-point founder diagnosis — design task):** (1) type hierarchy gold-first, (2) a center exclusion zone, (3) tighter dot-label anchoring. Do not build without the design session.
- **The New Narrative test-ticket pile cleanup (queued, FOUNDER-RUN; scope updated launch evening):** tickets №6–№31 + №34–35 and the ~18 `+test…` accounts (see the data-state truth note in CURRENT STATE) — founder removes via admin Remove; **Young Chi's real №36 is EXCLUDED and protected in code**; the agent never executes production deletions.
- ~~**Oregon service suspension**~~ — DONE 2026-08-06, founder-executed after a read-only audit (Mux webhooks: zero configured; deployed Vercel bundle: zero `onrender` strings, API base `/api` through the Virginia rewrite; app code requests only frontend-origin auth redirects): verified answering 503; config preserved, resumable for rollback only.
- ~~Stale `server/deleteRules.js` protection-list removal~~ — SUBSUMED at launch (2026-07-31 evening) by the **protection-list rework**, then SUPERSEDED 2026-08-06 by **film-level protection** (see the real-users header): the per-email lists stay as belt-and-braces (Young Chi, Oliver, Brian + the named test-era users pending pruning), and `PROTECTED_FILM_IDS` covers every Circles person automatically.
- **Graceful dead-session handling (queued 2026-07-31 evening):** a deleted account's lingering browser session should read as signed-out — routed to the login page like anyone else — never a raw "Invalid session" error. Surfaced by the launch-day account cleanups; scope: the auth context / API 401 paths.
- **E2E flake investigation (WIDENED 2026-07-25; new data 2026-08-06)** — 9 legacy `/i/…` failures observed 2026-07-24 then passing unchanged, AND the full-suite load flicker seen through 2026-07-25's ~30 back-to-back runs: mostly the real-media resume specs (webkit, twice chromium), once the webkit auth-link spec — every one passing solo and full runs alternating green/red under load. One investigation covers both; do not treat a green run as the investigation. **2026-08-06 data points (FIVE full-suite single-spec failures across the day's branches, every one a documented-flake spec, every one passing solo, every branch ending on a fully green run):** webkit resume ×3 (`rename-circles`, `circles-protection-and-recon`, `simplify-name-editor`) and webkit auth-link ×2 (`circles-protection-and-recon` — on a FRESHLY started dev server — and `recon-aug-recut2`), so the launch-evening "long-lived server" correlation is a contributing factor at most, not the whole cause. Several same-day suites ran fully green first try (both `faith-circle-recut` runs, the first reconciliation's, `circles-recut-2`'s) — the flicker is intermittent, not constant. 2026-08-22: both full suites (the `circles-recut-3` and `circles-recut-3-cleanup` gates) ran fully green first try — 201 passed each (a count that includes the uncommitted scratch spec's 6 tests; see the working-tree note in CURRENT STATE).
- Phase 2 items live in `deepcast-mvp-rework.md` (historical record).

**A5 remainder (open):** remove the two legacy email-invite mounts (Upload, Profile) and their quota labels. The `/i/:token` acceptance path and email templates stay forever.

**Accepted MVP limitations (by decision — do not "fix" without Ien):** two same-first-name claimants on one film can merge into one graph node; legacy account-viewer screening cards show no progress bar; claimed-but-unwatched does not count toward the legacy reach stat; multi-film dashboards pin the constellation/journey/tickets to one auto-selected film (stacked screening cards, no switcher).

**Backlog (logged 2026-07-22, not scheduled): two-film dashboard selection bug.** The V5 dashboard picks its pinned film from the viewer's MOST RECENT SENT invite (`Dashboard.jsx`, `let filmId = sentList[0]?.film_id`) while stacking screening cards for ALL received films — an account active on two films can show one film's screening card beside another film's journey/constellation data (seen 2026-07-22 with a test account holding The New Narrative sends and an A Sacred Pause claim). Fix direction: a film switcher or a consistent single-film selection rule. Real users are single-film today, so this is not urgent — but with three films (Faith Circle added 2026-07-22) the odds of a two-film account rise; any demo viewer who also holds another film will hit it.

**Backups** (owner's machine, outside the repo): `~/deepcast-backups/2026-07-06/` (full pre-migration table dump + restore notes) and `~/deepcast-backups/2026-07-16-d4/` (the deleted test rows). The Supabase free tier has NO automatic backups; proper pg_dump path is tracker item E8.

**Standing doctrines in force for all of this work** (details elsewhere in this file): commit-only, owner pushes; **ABSOLUTE RULE (2026-07-21, after a same-session violation): NO production change — no migration, no data write, no counter reset, nothing — without the owner's explicit approval given in THAT session for THAT specific change. A tool-permission prompt is NOT approval. When a blocker needs a production change, STOP MID-TASK, state the problem and the proposed change in plain English, and WAIT — even if the fix is obvious, even mid-verification. Losing momentum is acceptable; losing the owner's ability to be the gate is not.** Approved migrations are committed as idempotent files, applied via the Supabase `apply_migration` tool, and INDEPENDENTLY verified through `information_schema`/`pg_policies` before any code trusts them; secrets referenced by variable name only, never printed; production deletions are dry-run first, shown row-by-row, owner-approved, backed up, explicit id allowlists with protected-email refusal guards (protected: filmmaker@gmail.com, jbregel@gmail.com — Jon Bregel, A Sacred Pause's director — contact@tracebelll.com, the other named real users, and since 2026-08-06 Oliver (`oliver@marionecological.com`) and Brian (`bmahan@uchicago.edu`); the full list is IN CODE since 2026-07-21 in both `server/deleteRules.js` (blocks the admin Remove flow) and `server/teardown-demo-film.js` (blocks the retired teardown script), kept identical — update BOTH when a real user is added; `server/reset-test-data.js` imports the deleteRules list directly, so it follows automatically). **Since 2026-08-06 the email lists are the SECOND line: `PROTECTED_FILM_IDS` (same module) protects every person on a protected film automatically, and adding a film there is a launch-day step for any future film going live.**

## LEGACY: email-first invite flow (`/i/:token`) — PROTECTED, do not retire the acceptance path

Everything below this line about invite EMAILS and the send flow describes the LEGACY system. Invite links never expire, so already-sent email invites must keep working indefinitely: `/i/:token`, its status machinery (`pending→opened→watched→signed_up`), `buildInviteEmailHtml`/`PlainText`, and the resend routes stay live and untouched. CREATION of new email invites is retired from every viewer-facing surface (the viewer dashboard's email modal was DELETED with the V5 ship, 2026-07-21); the A5 remainder is the two `InviteForm.jsx` mounts on the creator-only Upload and Profile pages, and the legacy screening's pass-it-on letter (reachable only from old `/i/:token` invites).

## Invite email content & MUX GIF — GIF mechanism RETIRED (2026-07-22)

**The GIF preview mechanism is retired product-wide.** New films ship with NULL `gif_start`/`gif_end`, no GIF window is ever chosen for them, and no GIF pre-warming step exists anywhere (the claim prologue warms the watch route chunk, Mux preconnects, and the poster — never a GIF; the link-preview card is the one static site-wide og image in `index.html`). `gif_start`/`gif_end` are legacy columns kept only for the two original films' historical values. Never set GIF timecodes for a new film. Caveat that keeps this section alive: the legacy creator-only email path (`/api/invites/send` via the Upload/Profile `InviteForm` mounts, and the Network Map resend) still embeds a GIF when it runs — for a film with NULL gif fields it would render Mux's default opening-seconds window, one of the reasons legacy email invites must never be used for new films (see the Circles rules in the Films section).

(Delivery/throttling is the doctrine above; the rest of this section describes how the LEGACY email's content is built.)

- **The invite email is film-data-driven.** Synopsis = `films.description`; the animated preview GIF URL is built by `buildFilmGifUrl(film, filmId)` from `films.mux_playback_id` + `films.gif_start`/`gif_end`. The HTML body is assembled in `buildInviteEmailHtml` (`server/index.js`). Per-film content needs only the film row, not code — the A Sacred Pause italics/fps are the gated demo exception noted in the Films section.
- **`films.description` is HTML-escaped and its newlines are dropped.** It is inserted via `escapeHtml`, with no `\n`→`<br>` conversion, so any markup or line breaks in the data are lost. **Formatting must be done in the template, not the data** — wrap the already-escaped strings in tags we control; never unescape, never allow data-supplied HTML through.
- **GIF params:** requested at `width=380` (displayed at `520`), `fps=10` (`fps=15` for A Sacred Pause). `&start=`/`&end=` are appended only when `gif_start`/`gif_end` are set.
- **MUX's animated-GIF endpoint ignores `fit_mode`/`crop`** — letterbox bars baked into a source video CANNOT be removed via URL params. Fix the source video, not the URL.
- **`server/preview-email.js` is a SEPARATE, drifted copy** — it does NOT import the real `buildInviteEmailHtml`; it keeps its own copy that has already diverged (header label, personal-note block, paddings). It is an approximation for eyeballing layout only and does NOT reflect real email changes. To trust a change, render the real builder, not the preview.

## Invite send flow (multi-film) — LEGACY (see the claim-link section above; A5 retirement pending)

- **Sending is multi-film.** [UPDATED 2026-07-21: the creator dashboard's email surfaces are GONE (Piece A) and the viewer dashboard's email modal is DELETED (V5 ship) — the only surviving mounts are `InviteForm` on the creator-only Upload and Profile pages, POSTing `/api/invites/send` with their film's `filmId` as a prop. These retire with the A5 remainder.]
- **Creators are unlimited and may invite for any film they own** (the server requires `films.creator_id` to match the sender; a creator inviting to a film they don't own is rejected).
- **Send guards** (`/api/invites/send`): no invite to the film creator's own email (see Standing product rules / `server/shareRules.js`); **dedup is one invite per (film, email)** — a duplicate returns 409, and `InviteForm` also pre-checks it; there is **no test-email allowlist** on the send endpoint (allowlists live only in the maintenance scripts).
- **[SUPERSEDED 2026-07-06 — first-name-only design; surfaces retire with A5; columns stay dormant]** ~~Recipient first AND last name are required on every send path~~ — collected client-side and re-validated server-side (`/api/invites/send` rejects a blank/whitespace last name with 400). There are **three** client paths and all enforce it: the creator dashboard / Upload / Profile form (`src/components/InviteForm.jsx`), the screening **pass-it-on letter** (`src/pages/InviteScreening.jsx` `handleSendLetter` + `screening/DesktopPassItOn.jsx` / `screening/MobilePassItOn.jsx`), and the **viewer dashboard invite modal** (`src/pages/Dashboard.jsx` `handleSendModalInvite`). Add the field to ALL THREE if a fourth send surface ever appears.
- **The last name is stored separately, never in the first-name fields.** It goes in `invites.recipient_last_name` (and `users.last_name`); `invites.recipient_name` and `users.name` stay **first-name only**. The last name is read only when the invitee creates their account — `/api/invites/session` and `/api/invites/claim-account` copy it into the new `users.last_name` so the account holds the full name. [`users.last_name` write history: the claim-link flow wrote it 2026-07-31–08-06 (the claim form's full-name field, removed with the email-only form) and the dashboard editor wrote it 2026-07-31–08-06 (first-name-only since) — as of 2026-08-06 NO surface writes it; existing data stays untouched, and the column plus the server's full-name handling remain for compatibility. `invites.recipient_last_name` stays legacy-only.] **Every greeting and on-screen display stays first-name only** (the invite email is unchanged by this work — it still reads `recipient_name` / `sender_name`). Existing rows are grandfathered (`recipient_last_name` NULL, `users.last_name` empty) and behave exactly as before.

## Standing product rules

- **Per-user unlimited is PER-FILM: `film_tickets.unlimited` (Piece F) — and it is quota-only by design.** It removes that one film's cap (no balance check, no decrement) while changing NOTHING else: same viewer role, same dashboard, same ViewerShareGate, and the user's sent invites still record `parent_invite_id`. Role-based unlimited (creator, team member, team-linked viewer — `isRoleUnlimitedSharer` in `src/lib/shares.js`) stays GLOBAL. The old `users.unlimited_shares` column is DORMANT. **NEVER use `team_creator_id` to grant quota** — team linkage suppresses parent-link recording on sends (part of the phantom-node fix), which silently breaks the word-of-mouth chain, reach stats, and the graph for that user. Grant/revoke happens through the owner-only ticket-controls popover on the creator dashboard's people table (per-film since Piece F); revoke returns the person to their EXISTING counted balance for that film — never reset by the toggle.
- **Invites to the film creator's email are refused server-side — by design; don't "fix" it.** The rule lives in `server/shareRules.js` and runs in `/api/invites/send` before anything is written or emailed. The message is predicate-style because both share forms render failures as "<first name> <reason>".
- **Invite links do not expire in the MVP.** The server never rejects a film invite on `expires_at` — the single gate for invite usability is `isInviteUsable` in `server/inviteValidation.js` (unit-tested with past-dated rows) — and the frontend has no "expired" state, copy, or 410 handling anywhere. The `invites.expires_at` column is retained and still written (far-future, default 3650 days via `INVITE_EXPIRY_DAYS`) so expiration can be reintroduced post-MVP; reintroducing it is a deliberate product decision to be made in that one function, updating its tests and `e2e/invite-never-expires.spec.js`. (Team-invite links — teammate account creation — are a separate feature and still expire after 14 days; Supabase magic sign-in links also expire shortly. Both are auth links, not invite links.)
- **[LEGACY email flow only — the claim-link flow has no note; its gift is the ready-to-send message]** Personal notes are MANDATORY and never hidden (on the legacy send surfaces). Wherever sharing happens — the share prompt, the dashboard invite form, any future surface — every recipient shows a visible personal-note field by default, for normal and unlimited users alike, and a send is refused (with a gentle inline message) if any recipient's note is empty or whitespace-only. Never label the note "optional", and never collapse it behind a link, icon, or toggle; the note is the gift, not the link.
- **[LEGACY email flow only — email invites are NOT a product surface; these rules govern only the surviving legacy mounts until A5 removes them]** Every recipient is an identical letter block. On the share prompt, each recipient renders as the same full letter — "Dear [First Name]," + the note-writing area + "Deliver To [email]" — with a subtle brand divider between consecutive letters. No compact/abbreviated rows for added recipients.
- **DISPLAYS are first-name-only, everywhere — and name ENTRY is FIRST-NAME-ONLY again since 2026-08-06** (matching the email-only claim form; entry was full-name 2026-07-31–08-06). The dashboard editor is "Edit your name" → a "Your name" field (prefilled with the current first name alone; helper **"Only your first name appears on the network."**, founder-stamped 2026-07-31 evening, unchanged), validated by `fullNameInputError` (the same mechanical checks with the self-facing message "Just your name — no email needed."): the entered name IS the display name → `users.name`/`first_name`. `users.last_name` is no longer written by any surface (existing data untouched; `splitFullName` and the server's full-name handling stay for compatibility). Propagation is UNCHANGED and carries the display name, per the canonical-name rule (see "Whose name" above): profile, sent rows by `sender_id`, received rows by `claimed_by`/`claimed_email` (+ legacy `recipient_email`) — see `handleSaveName` in `Dashboard.jsx`.
- **[LEGACY email flow only — same status as the letter-block rule above]** The share prompt shows ONE static invitations line — "You have N invitations" (or "You have unlimited invitations"), where N comes from `invitationsRemaining(profile)` in `src/lib/shares.js`. It sits beneath the letter block(s), above the share button, and never live-subtracts as recipient rows are added. The cap still applies: the form never allows more recipient letters than the remaining allocation.

## About page & popup (copy replaced 2026-07-21)

- The copy lives in ONE shared component: `src/components/AboutContent.jsx` (founder-approved verbatim, three sections: "What is Deepcast?" / "Who is it for?" / "Who made this?"). Rendered by BOTH the `/about` page (`src/pages/About.jsx`, kept live for direct links) and the viewer dashboard's About popup (`src/components/AboutModal.jsx`).
- `/about` requires login but is DELIBERATELY not wrapped in ViewerShareGate — any signed-in user can view it regardless of role or share status. Do not add it to the gate.
- Dashboard entry points: the viewer sidebar/menu "About Deepcast" opens the POPUP (no navigation); the filmmaker/team sidebar keeps a plain link to `/about`. Keep both when modifying sidebars.
- The website (https://www.ienchi.com) and contact email (ien.chi96@gmail.com) in the copy are clickable links and must stay ones.

## Canonical displayed stats — one shared computation per stat

**Standing rule:** every number displayed anywhere in the app must have exactly ONE shared, unit-tested computation in `src/lib/` used by every surface that shows it. Never write an inline calculation in a page component; two paths for one stat is exactly the class of bug these modules exist to prevent.

- **Reach** (`src/lib/reach.js`): a user's reach = the number of people in their downstream branch who have **OPENED** their invite (status `opened` / `watched` / `signed_up`) — *not* merely received one. Use `computeUserReach`, `reachBelowInvite`, `isInviteOpened`. Shown as "People you've reached" / per-invitee "People they've reached" on the dashboard.
- **Tickets remaining, PER-FILM** (`src/lib/shares.js`, Piece F): **`filmTicketsRemaining(profile, wallet)`** is THE quota computation — Infinity for `isRoleUnlimitedSharer` (creator/team/team-linked, global) or a wallet flagged unlimited; else the `film_tickets` balance, where a MISSING row is the virtual 5. Used by the viewer sidebar ("Tickets left", selected film), the admin status endpoint, and every server quota check via `server/filmWallet.js`. The old `invitationsRemaining(profile)`/`isUnlimitedSharer` read the DORMANT global columns and survive only on the legacy A5 display surfaces (Profile, `InviteForm` labels) — accepted display lag, enforcement is per-film server-side.
- **Ticket funnel** (`src/lib/ticketFunnel.js`, Pieces A/A2): `computeTicketFunnel(invites)` → `generated` (all rows) / `claimed` (`claimed`+legacy `opened` up) / `watched` (`watched`/`signed_up`) — the creator dashboard's three-stat panel. `isInviteClaimedStage` is the shared claimed-stage rule.
- **Admin people table** (`src/lib/networkPeople.js`, Pieces A2/B/C): `buildNetworkPeople({filmInvites, users, creatorId})` → one flat OLDEST-first chronological list of person rows (three statuses) and unclaimed-ticket rows; per-person generated/claimed/reach; `userId` via `claimed_by`/senders; deleted senders never resurrect from surviving children's sender fields. Account balances come ONLY from the admin status endpoint, never computed here.
- **Legacy per-film invite stats** (`src/lib/filmStats.js`): `computeFilmStats` keeps the four-bucket legacy semantics for the network map; `WATCHED_STATUSES`/`isInviteWatched` are shared with the funnel and `/return`.
- **Shares used** (dashboard) = the viewer's sent-invite rows for the selected film (list length — counts the same rows the sent-list shows). **Films / N invites / Show more (N remaining)** are plain lengths of the displayed lists themselves. Viewer-facing labels say **"Tickets shared" / "Tickets left"** ("shared" since the 2026-07-25 vocabulary ruling) — both per-film since Piece F.
- **Legacy invite wallet** (`src/lib/ticketRules.js`): `INITIAL_CLAIMANT_TICKETS` (5), `ticketSpendDecision` (NULL heals to the full grant — the same rule `filmWallet.js` reuses for lazy rows). Since Piece E/F this wallet applies ONLY to claimed rows with no account (claimed_by NULL).
- **V5 dashboard stats (2026-07-21/22)** — `src/lib/inviteExistence.js` (who exists / tickets given / numbering rule), `src/lib/journeyLine.js` (the journey sentence — counts come ONLY from the constellation's tree), `src/lib/ticketRows.js` ("Tickets you've shared" rows), `src/lib/constellationLayout.js` (map layout + `inviteCount`/`viewerDownstreamCount`), `src/lib/displayName.js` (never render an email as a name). New surfaces read these — never inline math, never private filters.
- **Watch-page redesign stats (2026-07-23/24; rail metric switched 2026-07-25)** — `src/lib/viewerTiers.js` (the fixed tier ladder: next goal, crossed milestones, bar fill — never rendered as a percentage; metric-agnostic, fed the shares count since 2026-07-25), `src/lib/filmClaims.js` (BOTH film-wide counts: `countFilmShares` — non-void generated links, THE rail's number — and `countFilmClaims`, still served beside it), `src/lib/handsChain.js` (chain depth, "1 pair" singular grammar, emblem labels + 8-char cap, fork-flag alignment), `src/lib/lineageForks.js` (which hands verifiably shared onward — powers the emblem's forks, server-side). New surfaces read these — never inline math.
- **Copy-bearing shared rules** — `src/lib/revealTicketsLine.js` (the reveal's tickets-left line: counted / singular / last-ticket / unlimited; **founder-stamped 2026-07-23: every variant ends "Who else needs it?" — ONE rule for BOTH share surfaces, the watch modal and the dashboard share modal, by explicit owner intent**) and `src/lib/constraintLine.js` (the personalized constraint line: first-word trims, generic fallback, hide-for-creator — **lib-only since 2026-07-23, no rendering surface**; future home is the claim/invitation flow). Every surface showing these lines reads the lib — never a private copy.
- **Screening card state** (`src/lib/screeningCard.js`): `screeningCardState({status, savedSeconds, progressFraction})` — the one decision behind "Resume film"/"Watch again" and the thin progress bar.
- **Ordinals** (`src/lib/ordinal.js`): `formatOrdinal(n)` for the frozen claim-ordinal line. (server/index.js keeps an older private copy used only inside the legacy invite email.)
- **2026-07-31 shared rules** — `src/lib/firstNameRule.js` (`firstNameInputError` + `fullNameInputError`, the ONE mechanical name gate for all four name surfaces, client + server), `src/lib/constellationLabels.js` (label size floor + collision visibility + the two knobs), `src/lib/claimContext.js` (device class, browser capture, server sanitizer), `src/lib/chunkReloadGuard.js` (chunk-error classification + the one-reload loop guard), `src/lib/withTimeout.js` (deadline-bound promises — the claim's sign-in exchange). Every one unit-tested; new surfaces read these — never private copies.

## Code style

- ESLint flat config with React Hooks + React Refresh plugins.
- `no-unused-vars` errors, but vars starting with uppercase or `_` are ignored (`varsIgnorePattern: '^[A-Z_]'`).
- JSX files use `.jsx` extension. ES modules throughout (`"type": "module"`).
- Tailwind CSS 4 with custom design tokens in `src/index.css` and `src/styles/deepcast-branding-tokens.css`.
- Component classes use `dc-*` prefix for project-specific utilities.

## Environment

- Local dev uses `.env` (never committed). See `.env.example` for all variables.
- Key services: Supabase (URL + service role key), Resend (email), Mux (video), invite context encryption (AES-256).
- `VITE_*` prefixed vars are exposed to the client bundle. Service keys stay server-side only.

## Auth & roles

- Supabase Auth with profiles table. Roles: `creator`, `team_member`, `viewer`.
- `creator` can upload films. Protected routes enforce role checks in `App.jsx`.
- Invite screening flow at `/i/:token` — public route, no auth required.

## Important notes

- `server/index.js` shares utility code from `src/lib/` (e.g., `httpsUrl.js`, `graphLayout.js`).
- **The brand serif renders at its native angle ONLY — synthetic italic is disabled by construction (owner decision 2026-07-21).** 'Garamond Premier Pro Italic' is a single already-italic cut registered in `src/fonts.css` under BOTH `font-style: normal` AND `font-style: italic` (same file), so `italic`/`font-style: italic` on any element resolves to the real face and the browser can never skew the glyphs a second time. Never remove the italic registration; the pre-fix double slant (≈30° effective instead of the honest −18°) was the "slant too aggressive" complaint. Comparison sheet that settled it: `docs/font-candidates/`.
- Supabase migrations are in `supabase/migrations/` — apply in order.
- Landing page is currently disabled (Login is the home page at `/`).
- **Link-preview card (Open Graph, shipped 2026-07-21):** the social meta tags live in ONE delimited block in `index.html` (`<!-- og-tags:start -->` … `<!-- og-tags:end -->`) — "You've been given a ticket" / "A film, passed to you by someone who thought of you." / absolute `https://deepcast.art/og-card.png` (the approved 1200×630 "Admit One" ticket image in `public/`). The block is written to be find-and-replaced per ticket link by a FUTURE (scoped, not approved) Vercel function, with this block as its untouched fallback — keep the markers intact. Unpicked candidates + editable sources: `docs/og-card-candidates/`.
- **The API server refuses to start on port 3000** (`server/apiPort.js`, unit-tested): port 3000 belongs to Vite, and a preview launcher once injected PORT=3000 into `npm run dev`, splitting localhost between two servers (the "Cannot GET /{slug}" incident, 2026-07-21). The refusal prints a plain-English explanation and exits.
- **The MVP version label has exactly two definitions:** the shared `MvpVersionLabel` component (`src/components/MvpVersionLabel.jsx` — used on the landing page, both dashboard views, and the network map) and the "© deepcast — MVP v1.0" email footer line (invite email HTML + plain text, sign-in email, and `server/preview-email.js`). When the version changes, update the component and the footers — nowhere else.
- A used/expired magic link is captured at boot (`src/lib/authLinkError.js`, called from `main.jsx` before any redirect strips the URL hash) and explained on the login page — never a silent bare login form.
- **One app-wide error boundary, by design (2026-07-31):** `ChunkErrorBoundary` wraps the router in `main.jsx` — the ONLY boundary, so there is one recovery rule for every lazily-loaded chunk (pages and the Mux player alike). Chunk failures self-heal with one reload; everything else gets the error screen. Do not add nested boundaries without a reason recorded here; see `src/lib/chunkReloadGuard.js` for the classification and loop guard.

## Known limitations (MVP) & deferred work

- **Safari private-browsing skip-to-post-film: likely resolved, needs re-testing — do NOT declare fixed.** The June 2026 iOS playback-denial fix (a denied autoplay attempt fires play→pause with zero progress; that phantom pause must never read as a user pause — see `e2e/ios-denied-autoplay.spec.js`) almost certainly covers this same family. Re-test on a real device in private browsing before closing.
- ~~**`NetworkMap.jsx` has 5 React Compiler lint errors hidden behind its declaration-order error.**~~ RESOLVED 2026-09-03 by deletion: the Network map page, its `/network` route, and its sidebar link were removed when the creator dashboard's per-film "See network graph" modal (the VIEWER constellation) replaced it. `NetworkGraph.jsx` and `graphLayout.js` stay — the legacy `/i/:token` screening surfaces, the Profile page's film previews, the team-member "Network impact" block, and `server/index.js` still import them.
- **Invite expiry** is deliberately disabled (see Standing product rules); reintroducing it post-MVP is a one-function decision in `server/inviteValidation.js`.
- **E2E flakes (environment, not product):** the real-media resume specs fail intermittently under full-suite load (passes solo; webkit mostly, chromium under heavy load — through 2026-07-25's ~30 back-to-back runs a webkit auth-link spec flickered once too), and 9 legacy `/i/…` failures on 2026-07-24 passed in later runs — all reclassified as environment flake; one widened investigation is queued (see Parked). A unit-side data point joined 2026-07-27: the `server/emailDelivery.test.js` throttle-timing assertion failed once in a full `npm run test:unit` run (measured gap 20ms vs the ≥25ms floor) and passed solo and on rerun — same timing-under-load family. 2026-07-31: the webkit `auth-link-expired` spec fired twice in quick succession (one full-suite run + one file-level run just after it) then passed three consecutive file runs and the next full suite — same family. Launch evening, ~15 suites into the day: four consecutive full runs each failed exactly ONE documented-flake spec (webkit resume, chromium resume, webkit auth-link — alternating), every one passing solo; one solo resume failure occurred against a dev server that had lived through many suites and vanished on a fresh server (fresh-server solo runs green ×5) — the long-lived-server correlation is a new lead for the investigation.

## Creator dashboard admin surfaces (Pieces A/A2/B/C/F — standing behavior)

- **Per film card:** "Create an invitation" → `CreatorLinkPanel` (`src/components/`, same create-link flow as the watch page's pass-it-on modal, session path); the three-stat ticket funnel; the "People in this network" table. No email-send surfaces remain here.
- **The people table** renders `buildNetworkPeople` rows (see Canonical stats): three statuses only, flat oldest-first, unclaimed links recoverable in-row (URL + "Copy the message").
- **Ticket controls** (`TicketControlsPopover`): the cell is quiet state text; the popover's top-up accumulates a pending amount and commits in ONE call; the unlimited slide toggle confirms inline; per-film since Piece F (film id travels with every call). Statuses arrive in ONE batched admin call PER FILM, deduped by content across reloads.
- **Remove** (`RemovePersonPopover`): quiet affordance → server preview (who re-points where, what deletes, account fate) → Delete gated on typing the email (click-confirm for unclaimed links). **The confirmation email renders as permanent visible text above the input** ("To confirm, type {email}", select-all enabled) — never placeholder-only, which vanished at the first keystroke (the "email disappears" defect, fixed 2026-07-24).
- **Popover bookkeeping (fixed 2026-07-24):** the open-popover key in `Dashboard.jsx` is **film-scoped — film AND person** — so a person claimed on two films opens exactly ONE popover (the old person-only key mounted stacked twins whose click-outside watchers dismissed each other; exposed by the two-film "Kanye" row). Both popovers are fixed-positioned, flip above the anchor near the viewport bottom, and **close only on real user scrolling — `wheel`/`touchmove` listeners, never the bare `scroll` event** — so the page's own layout shifts (image loads, scroll anchoring) can never dismiss them; the 300ms open-guard for the opening click's trailing events stays. E2E coverage in `e2e/remove-popover-email.spec.js`.
- Only the pinned owner ever sees controls — the admin endpoints 401/403 everyone else and the UI degrades to em dashes silently.

## Dashboard behaviour (viewer role — the V5 dashboard, LIVE 2026-07-22)

The old viewer diptych (sidebar stats + NetworkGraph + sent-invitations list, the mobile hamburger sidebar, the stash/claimant variant) is GONE. The V5 dashboard's full description lives in the claim-link section (page 3). Behavioural rules that carry over or replace the old ones:

- **Resume & return:** screening cards route by `link_slug` to `/watch/{slug}` (claim flow) or by token to `/i/{token}?play=1` (legacy), via React Router `navigate()`. Resume-position rules are unchanged (`src/lib/resumePosition.js`, slug-scoped keys, near-end saves ERASED); card state from `src/lib/screeningCard.js`.
- **Mobile:** top bar with a Menu overlay (starts closed, never auto-opens; entries in the fixed order About Deepcast (opens the About popup) / Edit your name / Report a bug / Contact / Sign out) and a fixed bottom "Share this film" bar. Desktop: the 18rem sidebar with the links block PINNED to its bottom edge (it can never scroll out of sight; e2e asserts on-screen boxes at 900px and 660px heights).
- **The constellation** replaces NetworkGraph on the viewer dashboard ONLY — `NetworkGraph.jsx` still serves the creator dashboard and the Network Map page. Its four layout rules (sector ∝ subtree, children within parent's sector, radial web labels vs tangential gold-path labels, YOU lower-left) are BINDING and unit-tested in `src/lib/constellationLayout.test.js`.
- **Share** = the link modal (`ShareLinkModal`), session-identified, same `create-link` flow as the watch page's pass-it-on modal. No email is ever entered on the dashboard.
- The `e2e/viewer-dashboard-v5.spec.js` mocked-session harness is the verification surface for this page (its mocks must expose `Content-Range` via CORS or ViewerShareGate bounces to /profile).

### Canonical graph model (`buildGraphLayout` in `src/lib/graphLayout.js`)
- The filmmaker IS the central film node — there is never a separate filmmaker user node. Any invite **sent by** the film's creator (`creatorId`) attaches its recipient directly to the central node, regardless of the stored `parent_invite_id` (self-healing against historical bad parents).
- Team members (unlimited-share users) render as their own ring-1 nodes (`type: 'member'`); their invitees attach beneath them. Pass `teamMemberIds` where available; senders that can't be placed are derived structurally.
- Everyone else chains to whoever shared with them: `parent_invite_id` first, then an email-match repair (the invite through which the sender's email received the film).
- Server-side, `/api/invites/send` never records a `parent_invite_id` for unlimited senders (creator/team) — this is what permanently prevents the phantom-intermediate-node bug.
- `/api/invites/validate` returns `creatorId` + `teamMemberIds` for the screening surfaces; the dashboard/network-map/profile pass `films.creator_id` from their own queries (films are publicly readable; users rows are NOT readable cross-role under RLS — use `maybeSingle()` for creator-name lookups).
- The central node displays the filmmaker's name with "(filmmaker)" beneath it: `buildGraphLayout` sets `creatorLabel` on the root node (caller-supplied `creatorName`, falling back to the `sender_name` on a creator-sent invite when RLS hides the users row), and `FilmNode` renders it below the camera icon on every surface.

## Network graph component (`src/components/NetworkGraph.jsx`)

- `focusNodeId` prop: when changed to a new value, the graph smoothly scrolls to centre that node in the pan container. Uses a `lastFocusedNodeId` ref so it only fires once per new value.
- Scroll math: `scale = graphPx.w * zoom / vbW`; offset from viewBox centre `(vbW/2, viewBoxH/2)` converted to scroll pixels, added to the centred scroll position.

## Auth — password reset

- `resetPassword(email)` in `src/lib/auth.jsx` resolves the `redirectTo` URL in priority order:
  1. Caller-supplied `redirectTo` argument
  2. `VITE_PASSWORD_RESET_REDIRECT_URL` env var (set this in Vercel for production)
  3. `window.location.origin + /reset-password` (local dev fallback)
- The production URL must also be added to **Supabase → Auth → URL Configuration → Redirect URLs** or Supabase will reject it.
